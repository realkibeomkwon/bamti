# 작업 지시서: 파이프 연결과 토글 되돌림 결함

3단계를 마친 뒤 실사용 검증에서 세 가지 결함이 나왔습니다. 하나는 코드로 확정했고, 하나는 재현했지만 원인을 좁히지 못했으며, 하나는 증상만 확인했습니다. 확정된 것부터 고치고, 나머지는 측정으로 원인을 찾아 주십시오.

## 0. 증상 요약

| 번호 | 증상 | 상태 |
|---|---|---|
| A | `examples/ticker.py`가 15초 만에 연결이 끊기고 항목이 사라진다 | **원인 확정** |
| B | 파이프에 연결이 몇 초 동안 거부된다(`ERROR_FILE_NOT_FOUND`) | 재현됨, 원인 미확정 |
| C | 패널의 토글을 끄면 잠시 뒤 다시 켜짐으로 돌아간다 | 증상 확인, A와 B의 결과일 가능성이 높음 |

---

## 1. 결함 A: 예제 클라이언트가 하트비트를 보내지 않는다

`pipe_server.cpp:19`의 `kHeartbeatMs`는 15000입니다. `DropStale`이 1초 타이머에서 돌면서, 마지막 수신 이후 15초가 지난 클라이언트를 끊고 그 클라이언트가 소유한 항목을 제거합니다.

그런데 `examples/ticker.py`는 **`ping`을 한 번도 보내지 않습니다.** 유일한 송신은 30초 주기의 `patch`뿐입니다. 15초보다 깁니다. 따라서 다음 순서가 반드시 일어납니다.

1. `ticker.py`가 붙어서 항목을 올린다.
2. 15초 뒤 bamti가 클라이언트를 끊고 항목을 제거한다.
3. `ticker.py`의 `pipe.read(1)`이 빈 값을 받고 종료 코드 0으로 끝난다.

실제로 확인했습니다. 백그라운드로 띄운 `ticker.py`가 약 15초 만에 종료 코드 0으로 끝났고, 그 뒤 상단바에서 항목이 사라졌습니다.

`examples/status_push.py`에는 `ping_loop`가 있어서 이 문제가 없습니다. `ticker.py`를 만들 때 그 부분이 빠졌습니다.

### 고칠 것

- `ticker.py`에 5초 주기 `ping` 스레드를 넣습니다. `status_push.py`의 `ping_loop`를 그대로 따르십시오.
- 하트비트 주기를 문서와 코드가 다르게 말하고 있습니다. `docs/STATUS-PROTOCOL.md`는 코드와 같은 15초로 적혀 있으나, 계획 문서 3-8절은 30초입니다. **코드 값을 기준으로 계획 문서 3-8절을 15초로 고치십시오.** 값을 바꾸지 말고 문서를 맞추십시오.
- 명세에 **클라이언트는 하트비트 주기의 절반 이하 간격으로 `ping`을 보내야 한다**는 문장을 넣으십시오. 지금 명세에는 이 의무가 적혀 있지 않아서 같은 실수가 반복됩니다.

---

## 2. 결함 B: 파이프 연결이 간헐적으로 거부된다

### 관측된 사실

- 클라이언트가 `\.\pipe\bamti-status`를 열 때 `ERROR_FILE_NOT_FOUND`(errno 2)로 실패하는 구간이 있습니다.
- 실패 구간에서도 파이프 이름은 `\.\pipe\` 목록에 보입니다. 즉 인스턴스는 존재하는데 수신 대기 상태인 것이 없습니다.
- 8초 동안 0.15초 간격으로 재시도해도 계속 실패했습니다. 짧은 경합이 아닙니다.
- 잠시 뒤에는 저절로 회복됩니다. `.NET NamedPipeClientStream`과 파이썬 `open()` 모두 같은 시점에 실패하고 같은 시점에 성공하므로, 클라이언트 쪽 문제가 아닙니다.
- bamti를 새로 시작한 직후에도 실패한 적이 있습니다. 오래 켜 둔 뒤에만 생기는 문제가 아닙니다.
- `bamti.log`에는 이 구간에 관한 기록이 하나도 없습니다.

### 확인된 관련 결함

`PipeServer::clients_`에서 원소를 지우는 코드가 없습니다. `erase`도 `remove_if`도 없습니다. 끊긴 클라이언트의 `Client` 객체와 `std::thread`가 프로세스가 살아 있는 동안 계속 쌓입니다. 파이프 인스턴스 자체는 `CloseClientPipe`가 닫으므로 이것이 곧바로 B의 원인이라고 단정할 수는 없지만, 정리되지 않는 자원이 있다는 것은 확실합니다.

### 해야 할 일

**먼저 로그를 넣으십시오.** 지금은 파이프 서버가 아무 기록도 남기지 않아서 밖에서 관찰할 방법이 없습니다.

- `ListenLoop`가 새 수신 인스턴스를 만들 때마다 한 줄 남깁니다. `CreateNamedPipeW` 실패 시에는 `GetLastError()` 값을 함께 남깁니다.
- 클라이언트가 붙고 끊길 때 각각 한 줄 남깁니다. 현재 살아 있는 클라이언트 수와 `clients_.size()`를 함께 적으십시오. 이 두 값이 벌어지는 것이 보이면 그 자체가 답입니다.
- `DropStale`이 클라이언트를 끊을 때 사유와 함께 남깁니다.

그다음 다음 순서로 좁히십시오.

1. 클라이언트를 붙였다 끊기를 30회 반복하면서 위 로그를 봅니다. 수신 인스턴스가 매번 다시 만들어지는지 확인합니다.
2. `CreateNamedPipeW`가 실패한다면 `GetLastError()`가 무엇인지 봅니다. `ERROR_PIPE_BUSY`라면 인스턴스 상한(`kMaxPipeInstances = 16`)에 걸린 것이므로 인스턴스가 회수되지 않는 것입니다.
3. 실패가 없는데도 연결이 거부된다면, `ListenLoop`가 `mu_`를 기다리며 막혀 있는지 확인하십시오. `mu_`를 쥔 채 파이프에 쓰는 경로가 있으면 상대가 읽지 않을 때 그대로 멈춥니다. `WriteLine` 호출 자리마다 잠금을 쥐고 있는지 점검하십시오.

**원인을 확정하기 전에 고치지 마십시오.** 후보를 여러 개 한꺼번에 손보면 무엇이 효과가 있었는지 알 수 없습니다.

### 함께 고칠 것

원인과 무관하게 다음은 확정적으로 고쳐야 합니다.

- 끊긴 클라이언트를 `clients_`에서 제거하고 스레드를 정리하십시오. 스레드가 자기 자신을 지우지 않도록 순서에 주의하십시오. 끊긴 클라이언트를 별도 목록으로 옮겨 두었다가 다음 `DropStale`에서 `join` 후 제거하는 방식이 안전합니다.

---

## 3. 결함 C: 토글이 되돌아간다

사용자가 패널의 토글을 껐을 때 곧 다시 켜짐으로 돌아갔습니다.

### 배제한 것

`open_panel_id_`는 패널 열기, 패널 닫기, 토글 시간 초과에서만 쓰입니다. 상태 변경 메시지에서 패널을 다시 만드는 경로는 없습니다. 따라서 **패널이 통째로 다시 만들어져 낙관적 갱신이 지워지는 것은 아닙니다.**

`StatusPanelContent::StickyInvoke`는 `target.on`을 뒤집은 뒤 그 **새 값**을 이벤트와 `arm_toggle`에 함께 넘깁니다. `OnToggleTimeout`은 `revision`이 그대로일 때만 `!pending_toggle_.on`으로 되돌립니다. 이 논리 자체는 맞습니다.

### 유력한 설명

결함 A 때문에 `ticker.py`의 클라이언트가 이미 끊겨 있었고, 그래서 `toggle` 이벤트에 응답할 상대가 없었습니다. `revision`이 오르지 않으니 2초 뒤 `OnToggleTimeout`이 정상 동작으로 되돌린 것입니다.

즉 **C는 그 자체로 버그가 아니라 A의 증상일 가능성이 높습니다.** 다만 확정하지 못했습니다.

### 해야 할 일

A와 B를 고친 뒤 다음을 확인하십시오.

- `ping`을 제대로 보내는 `ticker.py`로 토글을 눌렀을 때 상태가 유지되는가.
- 클라이언트를 죽인 뒤 눌렀을 때 2초 뒤에 되돌아가는가. **2초보다 빨리 되돌아가면 그것은 별도의 결함이므로 보고하십시오.**
- 되돌릴 때 항목이 `kError` 상태로 표시되는가.

`OnToggleTimeout`에 로그를 한 줄 넣어 두십시오. 되돌렸는지, 응답을 받아서 넘어갔는지, 항목을 찾지 못했는지를 구분할 수 있어야 합니다. 이 로그가 없어서 이번에 원인을 확정하지 못했습니다.

---

## 4. 하지 말아야 할 것

1. **하트비트 값을 늘려서 A를 덮지 마십시오.** 예제가 규약을 지키지 않은 것이 원인입니다.
2. **B의 원인을 확정하기 전에 고치지 마십시오.**
3. **`clients_` 정리를 넣으면서 스레드를 자기 자신이 `join`하게 만들지 마십시오.**
4. 3단계에서 만든 프로토콜과 패널 구조를 바꾸지 마십시오. 이 작업은 결함 수정입니다.

---

## 5. 검증

- [ ] `/W4` 경고 없이 Debug와 Release가 빌드됩니다.
- [ ] `ticker.py`를 5분 이상 띄워도 항목이 사라지지 않습니다.
- [ ] 클라이언트를 붙였다 끊기를 30회 반복해도 매번 즉시 연결됩니다.
- [ ] 그 30회 동안 `clients_.size()`가 무한히 늘지 않습니다.
- [ ] 토글이 유지되고, 응답 없을 때만 2초 뒤 되돌아갑니다.
- [ ] 파이프 서버 로그로 연결과 해제와 수신 재무장을 밖에서 관찰할 수 있습니다.

## 6. 커밋

| 순서 | 커밋 메시지 |
|---|---|
| 1 | `fix: 예제 티커가 하트비트를 보내도록 고친다` |
| 2 | `chore: 파이프 서버의 연결과 해제를 기록한다` |
| 3 | `fix: 끊긴 파이프 클라이언트를 정리한다` |
| 4 이후 | 결함 B의 원인을 확정한 뒤 그 원인에 맞는 커밋 |
| 마지막 | `docs: 하트비트 주기와 클라이언트 의무를 명세에 맞춘다` |
