# 작업 지시서 14: 드래그할 때 옆 아이콘이 밀려나는 표현

**이 지시서는 13번(메뉴 구조)이 끝난 뒤에 착수하십시오.** 두 작업을 겹치지 마십시오.

---

## 1. 목표

> 독 아이콘을 드래그해서 이동할 때, macOS에서 아이콘 위치를 바꿀 때처럼 이동시키는 아이콘이 옆의 아이콘을 밀어내는 효과를 넣고 싶습니다.

지금은 `DisplayOrder()`(dock.cpp:1960)가 드롭 슬롯이 바뀌는 순간 순서를 즉시 회전시킵니다. 아이콘이 **한 프레임 만에 순간 이동**합니다. 이것을 슬롯 사이를 미끄러지는 움직임으로 바꿉니다.

---

## 2. 먼저 해결해야 하는 것: 프레임 비용

### 2.1 이미 측정된 값

반투명 고스트를 넣었을 때 잰 값입니다.

```
[perf] drag render n=25 avg=11.7ms max=24ms
```

애니메이션은 초당 60프레임이 필요하고, 한 프레임의 예산은 **16.7ms**입니다. 지금 `RenderLayered()` 한 번이 평균 11.7ms이고 최대는 이미 예산을 넘습니다. **이 상태로 애니메이션을 얹으면 코어의 70% 이상을 태우고 움직임도 끊깁니다.**

그래서 순서가 있습니다. 비용을 먼저 낮추고, 그다음에 애니메이션을 넣습니다.

### 2.2 어디가 비싼지 먼저 재십시오 (필수, 선행)

**추측해서 고치지 마십시오.** `RenderLayered()` 한 번을 다음 네 구간으로 나눠 재고 보고하십시오.

1. DIB와 렌더 타깃 준비 (`CreateDIBSection`, `CreateDCRenderTarget` 등)
2. 배경과 테두리 그리기
3. 아이콘 변환 (`CreateBitmapFromHBITMAP` → `CreateBitmapFromWicBitmap` → `DrawBitmap`)
4. `UpdateLayeredWindow`

```cpp
Log(L"perf", L"render split prep=%.1f bg=%.1f icon=%.1f present=%.1f total=%.1fms", ...);
```

- `QueryPerformanceCounter`로 재십시오.
- 아이콘이 13개일 때와 5개일 때를 각각 재십시오. 아이콘 수에 비례하는 구간이 어디인지 드러납니다.
- 이 로그는 **최적화 뒤에 제거**합니다. 우선 몇 줄만 남기고 확인하십시오.

**이 측정 결과를 보고한 뒤에 2-3으로 넘어가십시오.**

### 2.3 지배적인 구간을 줄인다 (측정 결과에 따름)

측정에서 아이콘 변환이 지배적으로 나오면, 프레임마다 다시 만드는 것을 없앱니다.

- `ID2D1Bitmap`을 항목별로 캐시하십시오. 지금은 프레임마다 `HBITMAP`을 WIC를 거쳐 D2D 비트맵으로 새로 만듭니다.
- **캐시 무효화 시점을 먼저 정하십시오.** `ID2D1Bitmap`은 렌더 타깃에 묶입니다. 렌더 타깃을 다시 만들면 캐시도 전부 버려야 합니다. `EnsureIcons()`가 `HBITMAP`을 바꿀 때도 버려야 합니다.
- `icon_cache_`(`HBITMAP` 캐시)와 혼동하지 마십시오. 별개의 캐시입니다. `REFACTOR-NOTES.md` 4장에 `icon_cache_`의 생명 주기가 정리되어 있으니 참고하십시오.
- 캐시를 넣은 뒤 2-2와 같은 방식으로 다시 재고, **전후 값을 나란히 보고하십시오.**

다른 구간이 지배적으로 나오면 그 구간을 다루십시오. 이 문서의 처방을 억지로 적용하지 마십시오.

### 2.4 진행 기준

한 프레임이 **5ms 미만**이 되면 3장으로 넘어갑니다.

5ms를 넘는데 더 줄일 방법이 보이지 않으면, **애니메이션을 넣지 말고 측정값과 함께 보고하십시오.** 끊기는 애니메이션은 지금의 순간 이동보다 나쁩니다.

---

## 3. 밀어내는 움직임

2장의 기준을 통과한 뒤에만 착수하십시오.

### 3-1. 구조

지금 `DisplayOrder()`는 슬롯 배정을 즉시 바꿉니다. 이것을 **목표 위치**로 삼고, 화면에 그리는 위치는 목표를 향해 따라가게 만듭니다.

- 항목마다 현재 그려지는 x 좌표(`float`)를 들고 있으십시오. `std::vector<float> anim_x_`처럼 `items_`와 크기가 같은 배열입니다.
- 매 프레임 목표 x(= `DisplayOrder()`가 정한 슬롯의 중앙)를 구하고, 현재 x를 목표 쪽으로 일정 비율만큼 당깁니다.

  ```
  x += (target - x) * k        // k 는 프레임당 계수
  ```

- `k`는 **약 150ms 만에 목표에 닿는** 값으로 잡으십시오. 60fps 기준으로 0.2 근처입니다. 정확한 곡선보다 자연스러움이 중요합니다.
- 목표와의 차이가 **0.5px 미만이면 목표값으로 맞추고 그 항목은 정지**로 봅니다.

### 3-2. 프레임 타이머

- 드래그를 시작할 때 타이머를 켜고, **드래그가 끝나고 모든 항목이 정지하면 끄십시오.** 드래그 중이 아닐 때 계속 도는 타이머를 남기면 안 됩니다.
- 주기는 16ms로 하십시오. `SetTimer`의 실제 해상도는 이보다 거칠지만 충분합니다.
- **새 타이머 ID를 쓰십시오.** `kPollTimerId`, `kHideTimerId`, `kRebuildTimerId`를 재사용하지 마십시오.
- `WM_TIMER`는 큐에서 우선순위가 가장 낮습니다. 메시지가 몰리면 밀립니다. 프레임 간격을 시간으로 재서 보정하되, **밀렸다고 해서 여러 프레임을 몰아 계산하지 마십시오.** 그냥 그 시점의 실제 경과 시간을 계수에 반영하면 됩니다.

### 3-3. 드롭한 뒤

- 버튼을 떼면 `EndDrag`가 `items_`와 `icons_`를 회전시킵니다(커밋 `cd1701f`). 이때 `anim_x_`도 **같은 회전**을 적용하십시오. 하지 않으면 드롭 직후에 아이콘이 튑니다.
- 회전한 뒤에도 애니메이션을 몇 프레임 더 돌려 제자리에 안착시키십시오. 타이머는 그때 끕니다.
- 드래그가 취소되면(`EndDrag(false)`) 원래 자리로 미끄러져 돌아가야 합니다. 순간 이동하면 안 됩니다.

### 3-4. 지켜야 할 것

- 끌고 있는 아이콘 자체는 애니메이션 대상이 **아닙니다.** 지금처럼 슬롯에 그대로 그리십시오. 반투명 고스트는 사용자가 제거를 요청했으므로 다시 넣지 마십시오.
- 호버 강조는 드래그 중에 그리지 않는 조건을 유지하십시오.
- `Rebuild()`가 `items_`를 새로 만들면 `anim_x_`의 크기가 달라집니다. **크기를 맞추고, 새 항목은 목표 위치에서 시작**시키십시오. 0에서 시작하면 화면 왼쪽 끝에서 날아옵니다.

### 3-5. 비용 확인 (필수)

- 드래그하며 아이콘을 다섯 칸 옮기는 동안의 CPU를 재십시오. **0.3 코어를 넘으면 보고하십시오.**
- 드래그가 끝나고 5초 뒤 CPU가 유휴 수준으로 돌아오는지 확인하십시오. 타이머가 남아 돌면 여기서 드러납니다.
- 애니메이션 프레임 수와 평균 프레임 시간을 드래그 한 번마다 한 줄로 남기십시오.

  ```cpp
  Log(L"perf", L"drag anim frames=%u avg=%.1fms", ...);
  ```

---

## 4. 하지 말아야 할 것

- **2-2의 측정 없이 캐시나 최적화를 넣지 마십시오.**
- 2-4의 기준을 통과하지 못했는데 애니메이션을 넣지 마십시오.
- 반투명 고스트를 다시 넣지 마십시오. 사용자가 제거를 요청했습니다.
- 애니메이션 타이머를 드래그가 끝난 뒤에도 남기지 마십시오.
- 기존 타이머 ID를 재사용하지 마십시오.
- `DisplayOrder()`가 정하는 **논리적 순서**를 애니메이션이 바꾸게 하지 마십시오. 애니메이션은 그리는 위치만 다룹니다. 드롭 결과는 지금과 같아야 합니다.
- 지금까지의 안정성 수정과 계측을 되돌리지 마십시오.
- 크기 확대(magnification)나 튕김(bounce) 같은 다른 효과를 함께 넣지 마십시오. 이번 요청은 밀어내는 움직임뿐입니다.

---

## 5. 검증

**비용 (먼저)**
- [ ] `render split` 측정값을 아이콘 13개와 5개에 대해 보고한다.
- [ ] 최적화 전후의 프레임 시간을 나란히 보고한다.
- [ ] 한 프레임이 5ms 미만이다. 아니면 그 사실을 보고하고 3장을 중단한다.

**움직임**
- [ ] 아이콘을 옆 칸으로 끌면 옆 아이콘이 미끄러지며 자리를 비운다. 순간 이동하지 않는다.
- [ ] 여러 칸을 빠르게 지나가도 움직임이 끊기지 않는다.
- [ ] 버튼을 떼면 아이콘이 제자리에 부드럽게 안착한다. 튀지 않는다.
- [ ] 드래그를 취소하면 원래 자리로 미끄러져 돌아온다.

**비용 (나중)**
- [ ] 다섯 칸을 옮기는 동안 CPU가 0.3 코어 미만이다.
- [ ] 드래그가 끝나고 5초 뒤 CPU가 유휴 수준이다.
- [ ] `drag anim frames=` 줄의 평균 프레임 시간을 보고한다.

**회귀**
- [ ] 드롭한 순서가 저장되고 재시작 후에도 유지된다.
- [ ] `order after-drop`과 `order after-rebuild`가 같다.
- [ ] 클릭으로 앱이 활성화된다.
- [ ] 호버 강조가 동작하고, 드래그 중에는 그려지지 않는다.
- [ ] 우클릭 메뉴와 서브메뉴가 정상이다.
- [ ] 유휴 60초 누적 CPU가 1초 미만이다.
- [ ] `[perf] msg flood` 줄이 남지 않는다.
- [ ] Release 빌드가 `/W4` 경고 없이 통과한다.

---

## 6. 커밋

셋으로 나누십시오.

```
chore: 독 렌더링의 구간별 소요 시간을 잰다
```
2-2입니다. 측정값을 본문에 적으십시오.

```
perf: 아이콘 비트맵을 프레임마다 다시 만들지 않는다
```
2-3입니다. 전후 프레임 시간을 본문에 적으십시오. 다른 구간이 원인이면 커밋 제목을 그에 맞게 바꾸십시오.

```
feat: 드래그할 때 옆 아이콘이 미끄러져 자리를 비운다
```
3장입니다. `drag anim` 측정값을 본문에 적으십시오.
