# 작업 지시서: 예제 클라이언트의 이벤트 처리와 동시 연결

3단계 결함 세 건 중 C(토글 되돌림)의 원인을 확정했습니다. **bamti가 아니라 `examples/ticker.py`가 원인입니다.** B(연결 거부)는 재현 조건을 좁혔습니다.

---

## 1. 결함 C: 원인 확정

### 측정으로 확정한 것

두 항목을 만들어 갈라서 쟀습니다. 둘 다 토글 행에 `"on": true`를 보냈습니다.

| 항목 | 보낸 것 | 화면 |
|---|---|---|
| `t.upsert` | `upsert` 한 번만 | 켜짐(오른쪽) |
| `t.patch` | `upsert` 뒤에 같은 값으로 `patch` 한 번 | 켜짐(오른쪽) |

**둘 다 정상입니다.** 따라서 파서, 저장, `patch` 병합, 렌더러는 전부 올바르게 동작합니다. `GetBool`도, `ApplyPanelPatch`도, 토글 그리기도 문제가 없습니다. **이 경로들을 건드리지 마십시오.**

### 진짜 원인

`examples/ticker.py`의 수신 루프입니다.

```python
while not stop.is_set():
    n = peek_bytes(pipe)
    if n == 0:
        time.sleep(0.1)
        continue
    chunk = pipe.read(n)
    buf += chunk
    if b"\n" not in buf:
        continue
    raw, buf = buf.split(b"\n", 1)     # <-- 한 바퀴에 한 줄만 처리한다
    ...
```

반복 한 바퀴에 **줄 하나만** 꺼냅니다. 버퍼에 줄이 여러 개 들어와도 나머지는 그대로 남고, 다음에 새 바이트가 도착할 때까지 처리되지 않습니다.

여기에 결정적인 요인이 겹칩니다. **bamti는 연결 직후 `hello`를 한 줄 보냅니다.** 그래서 버퍼의 첫 줄은 언제나 `hello`이고, 그 뒤에 오는 첫 이벤트(토글)는 한 박자 밀립니다. 이후 모든 이벤트가 한 줄씩 밀린 상태로 처리됩니다.

토글을 눌렀을 때 벌어지는 일은 다음과 같습니다.

1. 패널이 낙관적으로 스위치를 뒤집습니다.
2. bamti가 `toggle` 이벤트를 보냅니다.
3. `ticker.py`는 아직 버퍼에 남아 있던 `hello`를 처리하고 `toggle`은 버퍼에 남겨 둡니다.
4. 새 바이트가 오지 않으므로 `toggle`은 처리되지 않고, `patch` 응답도 나가지 않습니다.
5. 2초 뒤 `OnToggleTimeout`이 응답 없음으로 판단해 되돌립니다.

사용자가 본 "잠깐 뒤집혔다가 다시 돌아온다"가 정확히 이 순서입니다. `panel_open`과 `panel_close` 이벤트가 새 바이트로 도착하면 밀려 있던 줄이 뒤늦게 처리되므로, 눌렀던 값이 엉뚱한 시점에 반영되어 켜짐과 꺼짐의 기본값이 시행마다 달라 보이기도 했습니다.

### 고칠 것

버퍼에 들어 있는 **모든 완결된 줄**을 한 바퀴에 처리하십시오.

```python
while b"\n" in buf:
    raw, buf = buf.split(b"\n", 1)
    if not raw:
        continue
    handle(raw)
```

`examples/status_push.py`는 이미 이 형태(`while b"\n" in buf`)로 되어 있습니다. `ticker.py`만 어긋나 있습니다.

같은 실수가 다시 나오지 않도록 `docs/STATUS-PROTOCOL.md`에 클라이언트 의무를 한 줄 추가하십시오. **수신한 바이트에서 완결된 줄을 모두 처리해야 하며, 연결 직후 `hello` 한 줄이 먼저 도착한다는 사실**을 명시합니다. 지금 명세는 `hello`를 보낸다고만 적혀 있고, 그것이 클라이언트 수신 버퍼의 첫 줄이 된다는 점은 드러나 있지 않습니다.

### 검증

- [ ] `ticker.py`를 띄우고 토글을 누르면 상태가 유지됩니다.
- [ ] 한 번 더 누르면 반대로 바뀌고 그 상태도 유지됩니다.
- [ ] 패널을 닫았다 다시 열어도 마지막 상태가 그대로입니다.
- [ ] `ticker.py`를 죽인 뒤 누르면 2초 뒤에 되돌아가고 항목이 오류 상태가 됩니다. **2초보다 빨리 되돌아가면 별도 결함이므로 보고하십시오.**
- [ ] `[toggle]` 로그에 `acked`가 찍힙니다. 되돌린 경우에만 `revert`가 찍혀야 합니다.

---

## 2. 결함 B: 재현 조건을 좁혔습니다

지난번에 재현하지 못했던 연결 거부를 다시 만났습니다. 새로 얻은 사실은 다음과 같습니다.

- 실패한 시점에 **다른 클라이언트가 이미 연결되어 있었습니다.** `ticker.py`가 몇 분째 붙어 있는 상태에서 두 번째 클라이언트가 8초 동안 `ERROR_FILE_NOT_FOUND`로 실패했습니다.
- 잠시 뒤 같은 조건에서 두 번째 연결이 성공했습니다. 항상 실패하는 것이 아니라 간헐적입니다.
- 지난번 보고에 적으신 "파이썬 `open()`으로 두 번째 동시 연결은 errno 22"도 같은 현상의 다른 얼굴로 보입니다. **그때 이미 단서를 관찰하셨는데 B와 연결짓지 않으셨습니다.**

지난번 30회 시험은 **붙였다 끊기를 순차로** 했기 때문에 이 조건을 만들지 못했습니다.

### 해야 할 일

재현 시험을 동시 연결로 바꾸십시오.

1. 클라이언트 A를 붙여 두고 유지합니다.
2. 그 상태에서 클라이언트 B를 붙였다 끊기를 50회 반복합니다.
3. 다시 A와 B를 붙여 둔 채 C를 50회 반복합니다.
4. 실패가 나오면 그 시각의 `[pipe] listen armed` 로그와 대조합니다. 마지막 `listen armed` 이후 새 수신 인스턴스가 없는 구간에서 실패가 나는지 봅니다.

`ListenLoop`는 accept 직후에 다음 인스턴스를 만듭니다. 그 사이 구간이 얼마나 긴지, 그리고 `CreateNamedPipeW`가 조용히 실패하는지를 로그로 확인하십시오. `[pipe] listen create failed err=` 줄이 한 번이라도 찍히면 그것이 답입니다.

원인을 확정하기 전에 고치지 마십시오. 확정하지 못하면 시험 방법과 관측값만 기록하고 넘기십시오.

---

## 3. 하지 말아야 할 것

1. **bamti의 파서, `patch` 병합, 토글 렌더러를 고치지 마십시오.** 1절의 측정으로 정상임이 확인됐습니다.
2. **`OnToggleTimeout`의 2초를 늘려서 C를 덮지 마십시오.** 예제가 이벤트를 못 읽은 것이 원인입니다.
3. **B를 원인 확정 없이 고치지 마십시오.**

---

## 4. 커밋

| 순서 | 커밋 메시지 |
|---|---|
| 1 | `fix: 예제 티커가 수신 버퍼의 모든 줄을 처리하게 한다` |
| 2 | `docs: 클라이언트 수신 규약과 hello 도착 순서를 명세에 적는다` |
| 3 이후 | 결함 B를 확정했다면 그 원인에 맞는 커밋. 확정하지 못했다면 관측 결과만 문서로 남깁니다 |

---

## 5. 결함 B 재시험 관측 (원인 미확정, 코드 변경 없음)

시험 시각: 2026-08-29 01:27–01:31. bamti pid 13220 (`build\Release\bamti.exe`, 12:55:58 기동). `examples/ticker.py`를 붙여 둔 채 진행.

### 방법

1. 클라이언트 A를 유지한 채 클라이언트 B를 붙였다 끊기 50회. 제한 8초, 재시도 간격 0.15초.
2. A와 B를 유지한 채 클라이언트 C를 50회.
3. 실패 시 `[pipe] listen armed` / `listen create failed err=` 와 시각을 대조.
4. 추가로 `CreateFileW`와 파이썬 `open()`의 첫 오류, 동시 두 연결 경합, `open()` 직후 재연결을 기록.

### 관측

- `[pipe] listen create failed err=` 는 로그 전체에서 0회.
- accept 직후 `listen armed` 간격은 같은 밀리초(0–1ms). 8초 공백 없음.
- `CreateFileW`로 A 유지 + B 50회, A+B 유지 + C 50회: **실패 0, 재시도 0, 전부 첫 시도 즉시 성공.** 첫 시도 `WaitNamedPipe(0)` 도 성공.
- 두 스레드가 동시에 `CreateFileW`: 20/20회 한쪽만 성공, 다른 쪽은 `ERROR_PIPE_BUSY`(231). `ERROR_FILE_NOT_FOUND`(2) 는 한 번도 없음.
- 파이썬 `open(r"\\.\pipe\bamti-status", "r+b", buffering=0)` 으로 ticker가 붙은 채 50회, 끊은 뒤 즉시 재시도: 48/50이 첫 시도에서 `errno=22 Invalid argument`, `winerror=None`. 0.15초 뒤 두 번째 시도는 모두 성공. 8초 동안 실패한 회차는 0.
- 같은 파이썬 `open()` 이어도 끊은 뒤 20ms를 쉬면 49/50이 첫 시도 성공.
- 따라서 파이썬 `open()` 의 errno 22는 수신 인스턴스가 없는 8초 `FILE_NOT_FOUND` 가 아니라, 직전 연결을 닫은 직후 짧은 구간의 `ERROR_PIPE_BUSY` 가 CRT에 `EINVAL`(winerror 없음)으로 보이는 쪽에 가깝습니다.
- 보고하신 8초 `ERROR_FILE_NOT_FOUND` 는 이 동시 연결 조건에서도 재현되지 않았습니다. 원인을 확정하지 못해 bamti 파이프 서버는 고치지 않았습니다.
