# 수정 지시서: 알파 되돌리기 오버플로와 좌클릭을 고친다

작성일: 2026-08-31
선행 문서: `FIX-TRAY-ICON-CLICK.md`
대상: `src/icon_cache.cpp`, `src/tray_intercept.cpp`, 상단바의 아이콘 그리기

---

## 0. 상황

직전 작업으로 클릭 규약의 버전 분기와 아이콘 파이프라인 교체가 들어갔습니다. 서드파티 아이콘 셋(Tailscale, 오피스키퍼, Everything)은 **덤프를 열어 확인한 결과 깨끗합니다.** 그런데 사용자 검증에서 다음이 남았습니다.

| 증상 | 범위 |
|---|---|
| 카카오톡 아이콘의 네 모서리가 라이트 테마에서 어색하게 보인다 | 반투명 가장자리가 있는 모든 아이콘 |
| 볼륨·배터리 아이콘이 흰 파편으로 흩어져 보인다 | explorer의 모노크롬 아이콘 |
| 볼륨·배터리가 라이트 테마에서 흰색이라 보이지 않는다 | 위와 같음 |
| **좌클릭이 여섯 개 전부 동작하지 않는다.** 더블클릭도 안 된다 | 버전 0과 4 모두 |
| 우클릭은 여섯 개 전부 동작한다 | |
| 볼륨은 좌우 클릭 모두 `reason=no_callback`으로 버려진다 | 볼륨만 |

1절과 2절은 **원인을 코드 수준에서 특정했습니다.** 3절은 특정하지 못했으므로 계측을 먼저 합니다.

---

## 1. `PremulToStraight`의 오버플로가 아이콘 둘을 망가뜨린다

### 1-1. 원인

직전 작업에서 더한 `src/icon_cache.cpp`의 `PremulToStraight`입니다.

```cpp
image.pixels[i * 4 + 0] = static_cast<std::uint8_t>((image.pixels[i * 4 + 0] * 255 + a / 2) / a);
```

**255로 자르지 않습니다.** 결과가 255를 넘으면 `uint8_t` 캐스트가 값을 감아 버립니다. 256은 0이 되고 300은 44가 됩니다. **가장 밝아야 할 픽셀이 가장 어두워집니다.**

RGB가 알파보다 커지는 일이 실제로 일어납니다. `BitmapFromIcon`이 내부에서 `BgraToBitmap(image, px)`으로 WIC 축소를 하는데, 프리멀티플라이드 데이터를 보간하면서 반올림 때문에 RGB가 알파보다 1~2 커지는 픽셀이 생깁니다. `DefringePremul` 자체는 `(straight * a + 127) / 255`로 써서 불변식을 지키므로 범인이 아닙니다. 축소 단계가 범인입니다.

이것이 두 증상을 **한꺼번에** 설명합니다.

- **카카오톡의 네 모서리.** 둥근 모서리는 알파가 부분값인 안티에일리어싱 픽셀입니다. 거기서만 값이 감겨 어두운 점이 박힙니다. 다크 테마에서는 어두운 배경에 묻혀 안 보이고 라이트 테마에서만 드러납니다. 사용자가 관찰한 그대로입니다.
- **볼륨·배터리의 흰 파편.** 이 둘은 순백색 글리프에 알파로만 형태를 만드는 모노크롬 아이콘입니다. 거의 모든 픽셀에서 RGB가 알파와 같으므로, 반올림 오차가 생긴 픽셀마다 검은색으로 뒤집힙니다. 흰 글씨가 듬성듬성 지워진 것처럼 보이는 이유입니다.

### 1-2. 어떻게 고치는가

`PremulToStraight`에서 나눗셈 결과를 **255로 자르십시오.**

```cpp
auto unpremul = [](int c, int a) -> std::uint8_t {
  const int v = (c * 255 + a / 2) / a;
  return static_cast<std::uint8_t>(v > 255 ? 255 : v);
};
```

세 채널 모두에 적용합니다. 알파가 0인 픽셀에서 RGB를 0으로 두는 기존 처리는 그대로 유지하십시오.

### 1-3. 검증

`FIX-TRAY-ICON-CLICK.md` 1-5절과 같은 방식으로 PNG를 덤프해서 **직접 눈으로 확인하십시오.** 확인이 끝나면 덤프 코드를 지우고 커밋 대상에서 빼십시오.

볼륨과 배터리는 흰 바탕에서는 안 보이므로, 덤프를 볼 때 회색이나 검은 배경 위에 올려서 보십시오. 확인할 것은 **글리프가 끊기지 않고 이어져 있는가**입니다. 직전 결과는 파편이었습니다.

카카오톡은 둥근 모서리에 검은 점이 없어야 합니다.

---

## 2. 볼륨 아이콘의 콜백 번호가 지워진다

### 2-1. 원인

`src/tray_intercept.cpp`의 `ParseCopyData`에서 항목을 갱신하는 부분입니다.

```cpp
slot->info.uid = data.uid;
slot->info.callback_message = data.callback_message;   // ← 플래그를 보지 않는다
...
if ((data.flags & NIF_TIP) != 0) {          // 툴팁은 본다
  slot->info.tip = TipFrom(data.tooltip, 128);
}
if ((data.flags & NIF_STATE) != 0) {        // 상태도 본다
  ...
}
```

`NOTIFYICONDATA`의 각 필드는 해당 플래그가 켜졌을 때만 의미가 있습니다. 툴팁과 상태는 플래그를 확인하는데 **콜백 메시지만 무조건 덮어씁니다.** 앱이 `NIM_MODIFY`를 `NIF_ICON`만 켜서 보내면 그 자리에는 아무 값이나 들어 있는데, 우리가 그것을 받아 적으면서 `NIM_ADD` 때 제대로 얻어 둔 번호를 잃습니다.

로그가 이것을 보여 줍니다. 볼륨 아이콘(`uid=100`)만 열두 번 전부 `reason=no_callback`으로 버려졌습니다. 볼륨은 음량이 바뀔 때마다 `NIM_MODIFY`를 보내므로 가장 먼저 잃습니다.

### 2-2. 어떻게 고치는가

**`NIF_MESSAGE`가 켜졌을 때만 대입하십시오.**

```cpp
if ((data.flags & NIF_MESSAGE) != 0) {
  slot->info.callback_message = data.callback_message;
}
```

`uid`는 항목을 식별하는 값이라 항상 유효하므로 그대로 두십시오. `owner`도 그대로 둡니다.

주변 코드가 이미 `NIF_TIP`과 `NIF_STATE`로 같은 판단을 하고 있으므로, 이 수정은 관례를 따르는 것이기도 합니다.

### 2-3. 진단 줄을 읽기 쉽게 만드십시오

지금 `invoke skipped` 줄에 키만 있어서 어느 아이콘인지 알 수 없습니다. `uid`와 툴팁을 더하십시오.

```
intercept invoke skipped key=0x... uid=100 tip="DELL U4025QW: 100%" reason=no_callback
```

---

## 3. 좌클릭이 전혀 동작하지 않는다

### 3-1. 지금까지 확인된 것

**추측하지 말고 아래 사실에서 출발하십시오.**

- 로그에 `right=0` 호출이 57회 있고 전부 `posted=1`입니다. 메시지는 실제로 나갑니다.
- 같은 앱에 대해 `right=1`은 전부 동작합니다. 창 핸들도 콜백 번호도 옳다는 뜻입니다.
- 버전 0(카카오톡·Everything·오피스키퍼)과 버전 4(Tailscale·Bluetooth)가 **똑같이** 실패합니다. 버전 분기 문제가 아닙니다.
- 더블클릭도 실패합니다.

즉 **보내는 것은 맞는데 앱이 반응하지 않거나, 반응한 결과가 화면에 나타나지 않습니다.**

### 3-2. 가설 두 개

**가설 A: 포그라운드 권한이 없다.**

좌클릭의 일반적인 결과는 앱이 자기 주 창을 띄우는 것입니다. 앱은 `SetForegroundWindow`를 부르는데, Windows는 권한이 없으면 조용히 거부하고 작업 표시줄만 깜빡입니다. 우클릭의 팝업 메뉴는 이 제약을 덜 받으므로 동작합니다. **좌우 결과가 갈리는 것과 정확히 맞아떨어집니다.**

`Invoke`는 이미 `AllowSetForegroundWindow(pid)`를 부르지만 **반환값을 확인하지 않습니다.** 그리고 상단바 창은 `menu_bar.cpp` 206행에서 `WS_EX_NOACTIVATE`로 만들어집니다. 활성화되지 않는 창이므로 bamti가 포그라운드 프로세스가 아닐 수 있고, 그러면 `AllowSetForegroundWindow`가 실패합니다.

**가설 B: 더블클릭 메시지를 보내지 않는다.**

트레이 아이콘을 좌클릭 한 번으로는 아무 일도 하지 않고 **더블클릭에만** 주 창을 여는 앱이 흔합니다. 우리는 `WM_LBUTTONDBLCLK`을 한 번도 보내지 않습니다. 사용자가 상단바에서 더블클릭하면 우리는 다운·업 쌍을 두 번 보낼 뿐이고, 앱은 그것을 단일 클릭 두 번으로 봅니다.

### 3-3. 먼저 계측하십시오

**고치기 전에 가설 A를 사실로 확정하십시오.** 한 줄이면 됩니다.

```cpp
const BOOL allowed = AllowSetForegroundWindow(pid);
```

`intercept invoke` 줄에 `asfw=%d`를 더해 결과를 남기십시오. 실패했으면 `GetLastError`도 함께 남깁니다.

bamti를 띄우고 아이콘을 좌클릭한 뒤, 이 값이 0인지 1인지 보고하십시오. **이 값에 따라 3-4절에서 할 일이 갈립니다.**

### 3-4. 가설에 따른 처리

**`asfw=0`이면(가설 A가 맞으면)** 포그라운드 권한을 얻는 방법을 찾아야 합니다. 다음 순서로 시도하고, 어디까지 갔는지 보고하십시오.

1. `AllowSetForegroundWindow(ASFW_ANY)`로 바꿔 보십시오. 가장 간단합니다.
2. 그래도 실패하면, 클릭을 처리하는 순간 상단바 창이 입력을 받은 상태이므로 `SetForegroundWindow(owner)`를 우리가 직접 불러 보십시오. 성공 여부를 로그에 남깁니다.
3. 둘 다 실패하면 **거기서 멈추고 보고하십시오.** `AttachThreadInput`으로 입력 큐를 붙이는 우회는 부작용이 크므로 사용자 판단을 받아야 합니다. 임의로 쓰지 마십시오.

`WS_EX_NOACTIVATE`를 상단바 창에서 떼지 마십시오. 상단바가 활성화되면 다른 창의 포커스를 빼앗아 사용성이 크게 나빠집니다.

**`asfw=1`이면(가설 A가 아니면)** 가설 B로 갑니다.

**어느 쪽이든 가설 B는 처리하십시오.** 더블클릭 지원은 그 자체로 빠진 기능입니다.

- 상단바가 미러 아이콘의 더블클릭을 구분해서 백엔드에 알릴 수 있어야 합니다. `TrayBackend::Invoke(icon, right)`에 더블클릭을 표현할 방법이 없으므로 인터페이스를 넓혀야 합니다. **기존 두 인자 형태를 깨지 말고** 기본 구현이 단일 클릭으로 되도는 새 가상 함수를 더하는 편이 안전합니다. UIA 백엔드는 재정의하지 않습니다.
- 버전 0과 3은 `WM_LBUTTONDBLCLK`을 보냅니다. 버전 4는 `NIN_SELECT` 대신 **`NIN_KEYSELECT`가 아니라** `WM_LBUTTONDBLCLK`에 해당하는 규약이 없으므로, 버전 4에서는 더블클릭도 `NIN_SELECT` 한 번으로 두십시오. 버전 4 규약에 더블클릭 이벤트가 없다는 사실을 주석으로 남기십시오.
- 상단바 쪽에서 더블클릭을 판정하는 방법은 기존 코드의 관례를 따르십시오. 새 타이머를 만들지 말고 `WM_LBUTTONDBLCLK`이 이미 창에 오는지부터 확인하십시오. 창 클래스에 `CS_DBLCLKS`가 없으면 오지 않습니다.

---

## 4. 모노크롬 아이콘이 라이트 테마에서 보이지 않는다

### 4-1. 문제

볼륨과 배터리는 explorer가 어두운 작업 표시줄을 전제로 **순백색**으로 그린 아이콘입니다. bamti의 상단바가 밝을 때는 흰 글리프가 흰 배경에 묻혀 아무것도 보이지 않습니다. 1절을 고쳐 파편이 사라져도 이 문제는 남습니다.

### 4-2. 어떻게 고치는가

**거의 단색인 아이콘을 상단바의 글자색으로 다시 칠하십시오.**

- 판정: 알파가 0이 아닌 픽셀들의 RGB가 사실상 하나의 밝은 무채색인지 봅니다. 채도가 낮고 밝기가 높은 픽셀이 전체의 대부분이면 모노크롬으로 봅니다. 임계값은 직접 정하고 근거를 보고하십시오. **컬러 아이콘을 잘못 칠하면 안 되므로 판정을 보수적으로 잡으십시오.** 애매하면 원본을 그대로 두는 쪽이 낫습니다.
- 다시 칠하기: 알파는 그대로 두고 RGB만 목표 색으로 바꿉니다. 형태 정보는 알파에 있으므로 이것으로 충분합니다.
- 목표 색: `src/theme.hpp`의 `ClockTextColor(dark)`가 상단바 글자색입니다. 같은 색을 쓰십시오. 새 색을 정의하지 마십시오.
- 테마가 바뀌면 다시 칠해야 합니다. `ShellUsesDarkMode()`가 바뀌었을 때 미러 아이콘을 다시 만들도록 하십시오.

### 4-3. 어디서 칠할 것인가

**`IconToPng` 안에서 칠하지 마십시오.** 거기서 칠하면 테마가 바뀔 때마다 원본 `HICON`을 다시 받아야 하는데 그럴 방법이 없습니다.

PNG는 원본 그대로 보관하고, **상단바가 그릴 때** 색을 입히는 편이 낫습니다. 다만 기존 그리기 경로와 `IconCache`의 구조를 크게 바꾸지 않는 선에서 방법을 정하고, 어떤 방식을 골랐는지 근거와 함께 보고하십시오.

구조를 크게 손대야만 가능하다고 판단되면 **4절을 건너뛰고 그 이유를 보고하십시오.** 1절과 2절과 3절이 더 급합니다.

---

## 5. 순서

1. **1절**을 먼저 하십시오. 한 줄짜리 수정으로 아이콘 증상 둘이 함께 해결될 가능성이 높습니다.
2. **2절**은 독립적이고 간단합니다.
3. **3-3절 계측**을 넣고 실제 값을 확인한 뒤 3-4절로 갑니다.
4. **4절**은 마지막입니다.

---

## 6. 하지 않을 것

- 상단바 창에서 `WS_EX_NOACTIVATE`를 떼지 마십시오.
- `AttachThreadInput`을 쓰지 마십시오. 3-4절 3번에서 멈추고 보고하십시오.
- `icon_cache.cpp`의 기존 함수 동작을 바꾸지 마십시오. `PremulToStraight`는 직전 작업에서 새로 더한 것이므로 고쳐도 됩니다.
- UIA 백엔드의 클릭 경로를 건드리지 마십시오.
- 선행 지시서들의 금지 사항은 그대로 유효합니다. explorer 메모리 접근, 권한 승격, 셸 대체는 금지입니다.
- **사용자 화면에 마우스나 키보드 입력을 합성하지 마십시오.** 클릭 검증은 사용자에게 부탁합니다.

---

## 7. 검증

### 7-1. Cursor가 직접 할 것

| 확인 항목 | 방법 |
|---|---|
| 빌드 | Release. 경고가 늘지 않아야 합니다 |
| 아이콘 픽셀 | 1-3절. 덤프를 회색 배경에 올려 눈으로 보십시오 |
| 콜백 보존 | 볼륨을 여러 번 갱신시킨 뒤(음량 조절) `invoke skipped`가 사라지는지 봅니다 |
| `asfw` 값 | 3-3절. 0인지 1인지 반드시 보고하십시오 |

### 7-2. 사용자가 채울 표

| 아이콘 | 버전 | 좌클릭 | 더블클릭 | 우클릭 | 라이트 테마 가시성 |
|---|---|---|---|---|---|
| KakaoTalk | 0 | | | | |
| Tailscale | 4 | | | | |
| Everything | 0 | | | | |
| 오피스키퍼 | 0 | | | | |
| Bluetooth | 4 | | | | |
| 볼륨 | 0 | | | | |
| 배터리 | 0 | | | | |

---

## 8. 보고

- 절별로 고친 파일과 함수
- **`asfw` 값과 3-4절에서 어디까지 갔는지.** 이것이 가장 중요합니다
- 1절 수정 뒤 덤프를 보고 판단한 결과
- 4절의 모노크롬 판정 임계값과 그 근거. 건너뛰었으면 그 이유
- 이 지시서와 다르게 판단한 부분과 그 이유

작업 트리는 커밋하지 말고 그대로 두십시오. 검수 후에 정리합니다.
