# 수정 지시서: 트레이 아이콘 메뉴의 이름을 고정한다

작성일: 2026-08-31
선행 문서: `FIX-TRAY-HIDE-ALPHA.md`
대상: `src/tray_backend.hpp`, `src/tray_intercept.cpp`, `src/tray_mirror.cpp`

---

## 0. 무엇을 고치는가

`트레이 아이콘` 하위 메뉴의 항목 이름이 툴팁 그대로여서 이렇게 나옵니다.

```
DELL U4025QW: 100%
배터리 상태: 90%를 사용 가능(전원 연결됨)
```

두 이름 모두 **상태가 바뀔 때마다 달라집니다.** 음량을 조절하거나 전원을 꽂으면 메뉴의 같은 줄이 다른 글자로 보입니다. 목록에서 항목을 알아보기 어렵고, 스피커를 바꾸면 볼륨 항목의 이름이 통째로 바뀝니다.

**이 둘은 `볼륨`과 `배터리`로 고정해서 보여 주십시오.**

바꾸는 것은 **하위 메뉴의 이름뿐입니다.** 상단바에서 아이콘에 마우스를 올렸을 때 보이는 툴팁은 지금처럼 살아 있는 값을 그대로 보여 주어야 합니다. 거기서는 `100%`와 `전원 연결됨`이 쓸모 있는 정보입니다.

---

## 1. 무엇을 기준으로 알아볼 것인가

**툴팁 문자열로 판정하지 마십시오.** 언어와 장치 이름에 따라 달라지므로 깨집니다.

explorer의 시스템 아이콘은 `Shell_NotifyIcon`에 **GUID로 등록됩니다.** 로그의 `guid=1`이 그것입니다. GUID는 Windows가 정한 고정값이므로 어느 컴퓨터에서나 같습니다. 이것을 기준으로 삼으십시오.

지금은 GUID를 보관하지 않습니다. `ItemKey`(`tray_intercept.cpp` 178행)가 GUID를 해시해서 키만 만들고 원본은 버립니다.

### 1-1. GUID를 보관하십시오

`TrayIconInfo`(`tray_backend.hpp`)에 필드를 하나 더합니다.

```cpp
GUID guid_item{};   // 등록에 GUID가 쓰였을 때만 채운다. UIA 백엔드는 채우지 않는다.
```

가로채기 백엔드가 항목을 만들거나 갱신할 때 `data.guid_item`을 그대로 넣으십시오. 비어 있으면 그대로 둡니다. UIA 백엔드는 손대지 마십시오.

### 1-2. 실제 GUID를 먼저 관측하십시오

**GUID 값을 추측해서 적지 마십시오.** 기억이나 짐작으로 쓴 값은 틀립니다.

진단 로그를 임시로 넣어 이 컴퓨터에서 오는 값을 그대로 찍으십시오.

```
intercept guid tip="DELL U4025QW: 100%" guid={XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX}
```

`intercept item` 줄과 같은 조건으로, 키마다 한 번만 남기면 됩니다. 관측이 끝나면 이 로그는 지우십시오.

관측한 값을 보고에 그대로 적으십시오. 표를 채워 주시면 됩니다.

| 아이콘 | 관측한 GUID | 정할 이름 |
|---|---|---|
| 볼륨 | | 볼륨 |
| 배터리 | | 배터리 |
| Bluetooth | | Bluetooth |
| 그 밖에 `guid=1`인 explorer 아이콘 | | 관측한 툴팁으로 판단 |

---

## 2. 이름표를 만드십시오

`tray_mirror.cpp`의 익명 이름 공간에 표를 두십시오.

```cpp
struct KnownIcon {
  GUID guid;
  const wchar_t* label;
};
```

- **1-2절에서 실제로 관측한 GUID만 넣으십시오.** 관측하지 않은 것을 넣지 마십시오.
- 각 항목 옆에 그 GUID가 무엇인지 짧은 주석을 다십시오.
- 표에 없으면 지금처럼 툴팁을 쓰고, 툴팁이 비어 있으면 `(이름 없음)`을 씁니다. 기존 `ClipMenuLabel` 처리도 그대로 유지하십시오.

`MenuItems()`가 이름을 정할 때 이 표를 먼저 찾아보게 하십시오. **숨긴 항목에도 똑같이 적용해야 합니다.** 사용자가 이미 볼륨과 배터리를 숨겨 두었으므로, 숨긴 줄에서도 `볼륨`과 `배터리`로 보여야 합니다.

숨긴 항목은 `last_tips_`에 기억한 툴팁으로 이름을 만들고 있습니다. GUID도 함께 기억해야 표를 찾을 수 있습니다. `last_tips_`를 툴팁과 GUID를 함께 담는 형태로 넓히거나 같은 목적의 자료 구조를 하나 더 두십시오. 어느 쪽을 골랐는지 보고하십시오.

---

## 3. 하지 않을 것

- 상단바 툴팁을 바꾸지 마십시오. 살아 있는 값을 그대로 보여야 합니다.
- 툴팁 문자열로 아이콘을 판정하지 마십시오.
- 관측하지 않은 GUID를 표에 넣지 마십시오.
- `ItemKey`의 키 생성 방식을 바꾸지 마십시오. 숨김 설정이 전부 풀립니다.
- UIA 백엔드를 건드리지 마십시오.
- 볼륨 위젯을 만들지 마십시오. 다음에 따로 합니다.
- 선행 지시서들의 금지 사항은 그대로 유효합니다.
- **사용자 화면에 마우스나 키보드 입력을 합성하지 마십시오.**

---

## 4. 검증

| 확인 항목 | 방법 |
|---|---|
| 빌드 | Release. 경고가 늘지 않아야 합니다 |
| GUID 관측 | 1-2절의 표를 채우십시오 |
| 이름 적용 | 메뉴를 만드는 경로에서 항목 이름을 한 줄 로그로 남겨 `볼륨`과 `배터리`가 나오는지 확인한 뒤 그 로그를 지우십시오 |
| 숨긴 항목 | 지금 두 아이콘이 숨김 상태이므로, 위 로그에서 숨긴 줄의 이름도 함께 확인하십시오 |

메뉴를 실제로 열어 보는 것은 사용자 몫입니다.

---

## 5. 보고

- 고친 파일과 함수
- 1-2절의 GUID 관측 표
- `last_tips_`를 어떻게 넓혔는지와 그 이유
- 이 지시서와 다르게 판단한 부분과 그 이유

작업 트리는 커밋하지 말고 그대로 두십시오. 검수 후에 정리합니다.
