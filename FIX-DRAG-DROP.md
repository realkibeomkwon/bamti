# 작업 지시서 12: 드롭 직후의 되돌아감 제거와 반투명 효과 제거

기능은 동작합니다. 순서도 저장되고 재시작 후에도 유지됩니다. 이번에는 두 가지를 처리합니다. 1번은 결함 수정이고, 2번은 사용자가 요청한 제거입니다.

---

## 1. 드롭 직후 아이콘이 원래 자리로 되돌아갔다가 다시 이동한다

### 1.1 보고된 증상

> 드래그했다가 놓으면 원래 위치로 다시 한번 되돌아간 다음 바꾼 위치로 다시 또 이동하는 게 보입니다.

한 번의 드롭에 아이콘이 **두 단계로** 움직입니다. 되돌아가는 단계가 보이면 안 됩니다.

### 1.2 구조상의 원인

`EndDrag`(`src/dock.cpp`)는 드롭을 확정할 때 `pins_`만 재정렬합니다.

```cpp
const std::wstring moved = pins_[static_cast<size_t>(from)];
pins_.erase(pins_.begin() + from);
pins_.insert(pins_.begin() + to, moved);
SaveDockPins(pins_);
pending_rebuild_ = true;
force_collect_ = true;
```

`items_`와 `icons_`는 건드리지 않습니다. 그리고 곧바로 `dragging_ = false`가 되므로, `DisplayOrder()`(dock.cpp:1960)는 미리보기 회전을 멈추고 **항등 순서**를 돌려줍니다.

```
1. 버튼을 뗀다.
2. dragging_ = false → DisplayOrder() 가 회전을 멈춘다.
3. items_ 는 아직 드래그 이전 순서다.
   → 이 시점의 모든 그리기가 아이콘을 "원래 자리"에 그린다.
4. ScheduleRebuild() 가 kRebuildDelayMs = 300ms 타이머를 건다.
5. 300ms 뒤 Rebuild() 가 pins_ 순서로 items_ 를 다시 만든다.
   → 그제서야 아이콘이 새 자리로 간다.
```

**3번과 5번 사이의 300ms 동안 화면이 드래그 이전 상태를 보여 주는 것이 되돌아가는 단계입니다.** 사용자가 본 두 번의 이동은 이 구간의 시작과 끝입니다.

`ScheduleRebuild`의 300ms 지연 자체는 결함이 아닙니다. 되돌아가는 화면을 만드는 것은 **확정된 순서가 `items_`에 반영되지 않은 것**입니다.

### 1.3 확정된 순서를 그 자리에서 `items_`에 반영한다 (필수)

`EndDrag`에서 `pins_`를 재정렬할 때, **같은 회전을 `items_`와 `icons_`에도 적용**하십시오.

지킬 것이 있습니다.

- `icons_[i]`는 `items_[i]`에 대응하는 병렬 배열입니다. **두 배열을 반드시 같은 방식으로 회전**시키십시오. 한쪽만 옮기면 아이콘이 뒤바뀝니다.
- 회전 규칙은 `DisplayOrder()`가 이미 구현하고 있는 것과 **같아야 합니다.** `to < from`이면 `rotate(begin+to, begin+from, begin+from+1)`, 아니면 `rotate(begin+from, begin+from+1, begin+to+1)`입니다. 미리보기와 확정 결과가 어긋나면 여전히 튑니다.
- `icons_.size()`가 `items_.size()`와 다를 수 있는지 확인하고, 다르면 범위를 벗어나지 않게 하십시오.
- 회전은 **`commit`이 참이고 `from != to`일 때만** 합니다. `EndDrag(false)`(캡처 손실로 인한 취소)에서는 아무것도 옮기면 안 됩니다.

### 1.4 반영한 순서를 즉시 그린다 (필수)

현재는 `pending_rebuild_`가 참이면 `ScheduleRebuild()`만 하고 그리지 않습니다.

```cpp
if (pending_rebuild_) {
  ScheduleRebuild();
} else if (shown_) {
  RenderLayered();
}
```

1-3으로 `items_`가 이미 맞는 순서가 되었으므로, **재구성을 예약하더라도 한 번 그려야 합니다.** 그래야 버튼을 뗀 순간의 화면이 최종 위치와 같아집니다.

`shown_`일 때 `RenderLayered()`를 부르도록 고치십시오. `ScheduleRebuild()`를 없애지는 마십시오. 재구성은 실제 창 상태와 맞추는 별개의 일입니다.

### 1.5 재구성이 같은 순서를 만드는지 확인한다 (필수, 계측)

1-3과 1-4를 해도, 300ms 뒤의 `Rebuild()`가 **다른 순서**를 만들면 두 번째 이동이 그대로 남습니다. 이것은 확인해야 알 수 있습니다.

`EndDrag`의 회전 직후와 `Rebuild()` 직후에 각각 한 줄씩 남기십시오.

```cpp
Log(L"dock", L"order after-drop %s", joined);
Log(L"dock", L"order after-rebuild %s", joined);
```

`joined`는 `items_`의 `display_name`을 `>`로 이어 붙인 문자열입니다. 길면 앞 8개까지만 적으십시오.

**두 줄이 같아야 합니다.** 다르면 그 차이를 보고하십시오. 다른 경우에는 `CollectDockApps`의 핀 순서 반영이 `pins_`와 어긋난다는 뜻이며, 그때는 추측하지 말고 보고만 하십시오.

로그는 드롭당 두 줄이므로 제한이 필요 없습니다.

---

## 2. 반투명 효과를 제거한다

### 2.1 사용자의 요청

> 반투명 이펙트는 그냥 제거해 주세요.

지난 작업에서 커밋 `ee47638`로 넣은 반투명 표현 **전부**를 없앱니다. 사용자가 직접 보고 내린 판단이므로 그대로 따릅니다.

### 2.2 제거 대상 (필수)

`RenderLayered()`에서 다음을 없애십시오.

- 커서를 따라다니는 **알파 0.7의 고스트 아이콘.** 그리기 루프 뒤에 있는 `if (dragging_ && drag_index_ >= 0 ...)` 블록 전체입니다.
- 끌고 있는 항목을 원래 자리에 **알파 0.25로 흐리게** 그리는 처리입니다. `is_dragged` 판정을 없애고 항상 알파 `1.0f`로 그리십시오.

`draw_icon` 람다 자체는 **남기십시오.** 중복을 줄인 정리이고, 알파 인자는 `1.0f`로 고정해 부르면 됩니다.

### 2.3 함께 정리할 것 (필수)

고스트가 없어지면 매 이동마다 다시 그릴 이유가 사라집니다.

- `UpdateDrag`를 **드롭 슬롯이 바뀔 때만 그리던 원래 형태로 되돌리십시오.** 16ms 제한은 필요 없어지므로 제거합니다.
- `drag_cursor_` 멤버를 제거하십시오. 다른 곳에서 쓰지 않는지 확인한 뒤에 지우십시오.
- 드래그 렌더 계측(`drag_render_n_`, `drag_render_total_us_`, `drag_render_max_us_`, `last_drag_render_`)과 `[perf] drag render` 로그를 제거하십시오. 매 이동 렌더링이 없어지면 잴 대상이 없습니다. 지난번의 `avg=11.7ms`도 함께 해소됩니다.
- `QueryPerformanceCounter` 관련 코드가 `UpdateDrag`에만 있었다면 같이 지우십시오. 다른 곳에서 쓰고 있으면 두십시오.

### 2.4 남겨야 할 것

- **드롭 미리보기는 유지하십시오.** `DisplayOrder()`의 회전은 그대로입니다. 슬롯 경계를 넘으면 다른 아이콘들이 자리를 비켜 주는 표현이 유일한 시각적 피드백이 됩니다.
- 호버 강조(`hover_`)는 이번 제거 대상이 **아닙니다.** 그대로 두십시오. 드래그 중에 그리지 않는 조건도 유지합니다.
- 눌림 표시(`pressed_`일 때 아이콘을 1dip 내리는 것)도 그대로 둡니다.

### 2.5 커밋을 분리하십시오

되돌리기 쉬워야 하므로 **1번과 2번을 절대 한 커밋에 섞지 마십시오.**

---

## 3. 확인할 동작

- 아이콘을 두 칸 이상 옮기고 버튼을 뗀다. **아이콘이 한 번만 움직인다.** 원래 자리를 거치지 않는다.
- 인접한 칸으로 한 칸만 옮겨도 같다.
- 맨 왼쪽 아이콘을 맨 오른쪽 핀 자리로 옮겨도 같다.
- 드래그 도중 커서를 독 밖으로 빼서 취소되면, 아이콘이 **원래 자리로 돌아온다.** 이 동작은 그대로여야 합니다.
- 드래그 중에 반투명하게 보이는 것이 하나도 없다.

---

## 4. 하지 말아야 할 것

- `ScheduleRebuild()`를 없애거나 `kRebuildDelayMs`를 줄여서 되돌아감을 가리지 마십시오. 되돌아가는 화면 자체를 없애야 합니다.
- `DisplayOrder()`의 회전 규칙을 바꾸지 마십시오. `EndDrag` 쪽을 거기에 맞춥니다.
- `items_`만 옮기고 `icons_`를 두지 마십시오.
- 1-5에서 순서가 다르게 나오더라도 핀 로직을 추측해서 고치지 마십시오. 보고만 하십시오.
- 2번에서 호버 강조나 드롭 미리보기까지 함께 지우지 마십시오. 제거 대상은 반투명 표현뿐입니다.
- 반투명 대신 다른 효과를 새로 넣지 마십시오. 제거 요청입니다.
- 지금까지의 안정성 수정과 계측을 되돌리지 마십시오. 감시 스레드, `msg flood`, 트레이 억제, 굶주림 방지를 모두 유지합니다.

---

## 5. 검증

**되돌아감 (핵심)**
- [ ] 드롭한 아이콘이 한 번만 움직인다.
- [ ] `[dock] order after-drop`과 `order after-rebuild`가 같다. 두 줄을 그대로 보고한다.
- [ ] 드래그를 취소하면 아이콘이 원래 자리로 돌아온다.
- [ ] 재시작 뒤에도 바뀐 순서가 유지된다.

**반투명 제거**
- [ ] 드래그 중에 반투명한 아이콘이 보이지 않는다.
- [ ] 슬롯을 넘으면 다른 아이콘들이 자리를 비켜 주는 미리보기는 그대로 동작한다.
- [ ] `[perf] drag render` 줄이 더 이상 남지 않는다.

**회귀**
- [ ] 드래그하지 않고 클릭하면 앱이 활성화된다.
- [ ] 호버 강조가 여전히 동작하고, 드래그 중에는 그려지지 않는다.
- [ ] 우클릭 메뉴가 열리고 바깥 클릭으로 닫힌다.
- [ ] `새 창` 항목이 그대로 있다.
- [ ] 유휴 60초 누적 CPU가 1초 미만이다.
- [ ] `[perf] msg flood` 줄이 남지 않는다.
- [ ] Release 빌드가 `/W4` 경고 없이 통과한다.

---

## 6. 커밋

둘로 나누십시오. 섞지 마십시오.

```
fix: 드롭한 순서를 즉시 반영해 아이콘이 원래 자리로 되돌아가지 않게 한다
```
1번입니다. 본문에 `order after-drop`과 `order after-rebuild` 두 줄을 적으십시오.

```
revert: 드래그 중 반투명 아이콘 표현을 제거한다
```
2번입니다. 본문에 사용자 요청으로 제거했다는 사실과, 함께 제거한 렌더 계측을 적으십시오.
