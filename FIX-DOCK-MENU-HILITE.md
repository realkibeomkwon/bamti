# 작업 지시서 34: 독 우클릭 메뉴의 강조 박스와 서브메뉴 정렬

사용자가 bamti 독 우클릭 메뉴와 맥 독 우클릭 메뉴를 나란히 놓고 비교했습니다. 두 스크린샷을 픽셀 단위로 실측한 결과, 문제는 두 가지로 좁혀졌습니다.

**모서리 반지름은 문제가 아닙니다. 손대지 마십시오.** 아래 0절에 근거가 있습니다.

---

## 0. 측정 결과

### 0.1 두 이미지의 배율

bamti 스크린샷은 150% 배율입니다. 강조 박스 높이가 45픽셀로 측정되었고, 이는 `kMenuRowDip` 30dip에 1.5를 곱한 값과 정확히 일치합니다. 맥 스크린샷은 2배 배율입니다.

### 0.2 좌우 방향: 이미 맥과 같습니다

| 항목 | bamti (150%) | 환산 | 맥 (2x) | 환산 |
|---|---|---|---|---|
| 카드 안쪽 좌측 | x=39 | | x=34 | |
| 강조 박스 좌측 | x=47 | | x=44 | |
| 좌측 여백 | 8px | 5.3dip | 10px | 5.0pt |
| 강조 폭 ÷ 카드 폭 | 222 ÷ 238 | 93.3% | 328 ÷ 347 | 94.5% |

**좌우 여백은 이미 맥과 사실상 같습니다.** 이전 지시서에서 "좌우 인셋이 없다"고 적었던 것은 잘못된 판단이었습니다.

### 0.3 세로 방향: 여기가 문제입니다

맥 메뉴에서 구분선을 기준으로 행 영역을 재고, 그 안에서 강조 박스가 차지하는 범위를 measured했습니다.

```
맥 (x=300 열, 2x 픽셀)
  y=144..145  구분선
  y=146..155  카드 배경        ← 위 여백 10px (5pt)
  y=156..203  강조 박스        ← 높이 48px (24pt)
  y=204..213  카드 배경        ← 아래 여백 10px (5pt)
  y=214..215  구분선
```

| 항목 | bamti | 맥 |
|---|---|---|
| 강조 박스 높이 | 45px (30dip) | 48px (24pt) |
| 행 영역 높이 | 45px (30dip) | 68px (34pt) |
| 강조 ÷ 행 | **100%** | **70.6%** |
| 위아래 여백 | **0** | **각 5pt** |

**bamti의 강조 박스는 행 높이를 남김없이 채웁니다.** 사용자가 "너무 꽉차게 표시된다"고 한 것이 바로 이것이고, 사용자의 진단이 정확했습니다.

### 0.4 모서리 반지름: 오히려 bamti가 더 둥급니다

강조 박스의 좌상단 모서리 프로파일을 재서 반지름을 역산했습니다.

| | 측정 | 환산 | 강조 높이 대비 |
|---|---|---|---|
| bamti | 8px (150%) | 5.3dip | 17.8% |
| 맥 | 7px (2x) | 3.5pt | 14.6% |

`kMenuHoverRadiusDip`은 현재 6dip이고, 이는 맥보다 이미 둥급니다. 각져 보였던 것은 반지름이 작아서가 아니라 **박스가 행을 꽉 채워서 모서리가 이웃 행과 카드 테두리에 부딪혔기 때문**입니다. 위아래 여백이 생기면 모서리가 드러납니다.

**그러므로 `kMenuHoverRadiusDip`을 바꾸지 마십시오.**

### 0.5 서브메뉴 정렬

| 항목 | 측정 |
|---|---|
| 상위 메뉴 "옵션" 강조 상단 | y=156 |
| 서브메뉴 "독에 유지" 강조 상단 | y=165 |
| 어긋난 양 | **9px = 6dip** |

6dip은 `kMenuPadDip`과 정확히 같은 값입니다. 원인이 여기 있습니다.

---

## 1. 강조 박스에 위아래 여백을 준다

### 1.1 위치

`src/dock.cpp`의 메뉴 행 그리기에서 강조 박스를 채우는 부분입니다.

```cpp
const bool selected = i == hot;
if (selected && hover) {
  const float rr = corner::ClampPx(hover_r, box.right - box.left, box.bottom - box.top);
  target->FillRoundedRectangle(D2D1_ROUNDED_RECT{box, rr, rr}, hover.Get());
}
```

`box`는 `DockMenuRowRectOf`가 돌려준 행 사각형 그대로입니다.

### 1.2 바꿀 것

상수를 하나 추가하십시오. 기존 메뉴 상수들 옆에 두십시오.

```cpp
// 강조 칠이 행을 꽉 채우지 않게 위아래로 물러나는 양.
// 맥 메뉴는 강조가 행의 70% 정도만 차지한다.
constexpr int kMenuHoverInsetDip = 4;
```

강조를 채울 때만 위아래를 이 값만큼 줄이십시오. **`box` 자체를 바꾸지 마십시오.** 글자, 체크 표시, 화살표는 모두 `box`를 기준으로 배치되므로 그것들은 지금 위치를 유지해야 합니다.

```cpp
if (selected && hover) {
  const float inset = corner::ToPx(kMenuHoverInsetDip, dpi);
  const D2D1_RECT_F fill_box{box.left, box.top + inset, box.right, box.bottom - inset};
  const float rr = corner::ClampPx(hover_r, fill_box.right - fill_box.left, fill_box.bottom - fill_box.top);
  target->FillRoundedRectangle(D2D1_ROUNDED_RECT{fill_box, rr, rr}, hover.Get());
}
```

행 높이 30dip에서 위아래 4dip씩 빼면 강조 높이가 22dip이 되어 행의 73%를 차지합니다. 맥의 70.6%에 가깝습니다.

히트 테스트는 건드리지 마십시오. 마우스는 행 전체에서 반응해야 합니다.

이 그리기 코드는 메인 메뉴와 서브메뉴가 함께 쓰는 것으로 보입니다. 확인하고, 한쪽에만 적용되지 않도록 하십시오.

---

## 2. 서브메뉴 첫 항목을 상위 행과 같은 높이에 놓는다

### 2.1 원인

`Dock::OpenOptionsSubmenu()`가 서브메뉴 창의 기준점을 이렇게 잡습니다.

```cpp
const POINT anchor{row.right, row.top};
```

`PopupSurface::Place`의 `Anchor::RightOf` 처리는 이 값을 창의 좌상단에 그대로 씁니다. 그래서 서브메뉴 **카드의 위 테두리**가 상위 **행의 위쪽**에 맞춰집니다. 카드 안쪽 여백 6dip만큼 첫 항목이 아래로 밀립니다.

### 2.2 바꿀 것

`OpenOptionsSubmenu()`에서 기준점의 세로 좌표를 카드 여백만큼 올리십시오.

```cpp
const POINT anchor{row.right, row.top - Dip(kMenuPadDip)};
```

`PopupSurface`는 건드리지 마십시오. `Anchor::RightOf`는 다른 곳에서도 쓰이고, 그쪽은 지금 동작이 맞습니다.

1번의 강조 여백은 상위와 하위에 똑같이 적용되므로 정렬에 영향을 주지 않습니다. 두 강조 박스의 위 끝이 그대로 맞습니다.

### 2.3 확인할 것

- 화면 위쪽 끝에서 서브메뉴가 열릴 때 6dip 올린 값 때문에 창이 화면 밖으로 나가지 않는지 확인하십시오. `Place`에 좌우 보정은 있지만 세로 보정이 있는지 확인이 필요합니다.
- 서브메뉴가 왼쪽으로 뒤집혀 열리는 경우에도 세로 위치는 같아야 합니다.

---

## 하지 말 것

- `kMenuHoverRadiusDip`(6dip)을 바꾸지 마십시오. 실측 결과 맥보다 이미 둥급니다.
- `kMenuCornerDip`(10dip)을 바꾸지 마십시오. 이미 조정된 값입니다.
- 좌우 여백을 바꾸지 마십시오. 이미 맥과 같습니다.
- `kMenuRowDip`(30dip), 글꼴, 색을 바꾸지 마십시오.
- `PopupSurface`의 앵커 계산을 바꾸지 마십시오.
- 상단바 메뉴, 제어 센터, 시계 플라이아웃을 건드리지 마십시오.
- 지시서 33에서 적용한 독 외형 변경을 되돌리지 마십시오.

---

## 검수 항목

- [ ] Release 빌드가 `/W4` 경고 없이 통과한다. CMake 빌드도 통과한다.
- [ ] 강조 박스 위아래에 여백이 생겨서 행을 꽉 채우지 않는다.
- [ ] 강조 박스의 둥근 모서리가 눈에 보인다.
- [ ] 글자, 체크 표시, 화살표의 위치가 이전과 같다.
- [ ] 마우스가 행 어디에 있어도 강조가 반응한다. 여백 부분에서도 반응해야 한다.
- [ ] 서브메뉴 첫 항목의 강조와 상위 "옵션" 행의 강조가 같은 높이에서 시작한다.
- [ ] 메인 메뉴와 서브메뉴 양쪽에 여백이 적용되었다.
- [ ] 100%와 150% 배율에서 모두 확인했다.

## 검증 방법

화면 확인이 필요한 항목은 **사용자에게 스크린샷을 요청하십시오.** 사용자 화면에 마우스 이동이나 키 입력을 합성하지 마십시오. 레지스트리를 쓰는 검증 절차를 넣지 마십시오.

빌드한 뒤에는 실행 중인 bamti를 새 빌드로 다시 띄우십시오.

## 보고에 포함할 것

- 추가한 상수와 그 값
- 강조 박스의 실제 높이가 몇 dip이 되었는지
- 서브메뉴 세로 보정 후 화면 위쪽 끝에서의 동작
- 확인하지 못한 항목이 있다면 무엇을 왜 확인하지 못했는지
