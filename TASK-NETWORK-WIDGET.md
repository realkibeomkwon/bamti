# 작업 지시서: 네트워크 위젯을 맥 네트워크 메뉴 모양으로 다시 만든다

건드리는 파일은 `src/widgets/builtin.cpp`, `src/widgets/builtin.hpp`, `src/control_center.cpp`,
`src/control_center.hpp`, `src/clock_renderer.cpp`, `src/clock_renderer.hpp`, `src/bar_layout.cpp`,
`src/menu_bar.cpp`, `src/settings.cpp`, `src/settings.hpp`, `src/status_item.hpp`입니다.

참조 이미지 두 장이 있습니다.

- 현재 화면: `C:\Users\KIBEOMKWON\Pictures\Screenshots\Screenshot-2026-09-05_07-45-09.png`
- 목표 화면(맥 네트워크 메뉴): `C:\Users\KIBEOMKWON\Pictures\Screenshots\Screenshot-2026-09-05_07-53-39.png`

---

## 1. 현재 상태

상단바에 네트워크와 관련된 항목이 두 개 있습니다.

| 항목 | 아이디 | 화면에 보이는 것 | 패널 |
| --- | --- | --- | --- |
| 트래픽 | `bamti.widget/net` | `⇅ 7.1KB/s · 2.5KB/s` | 받기, 보내기, 인터페이스, 누적, 설정 열기 |
| Wi-Fi | `bamti.widget/wifi` | Wi-Fi 글리프만 | 누르면 제어 센터의 Wi-Fi 페이지가 열립니다 |

Wi-Fi 항목을 누를 때 제어 센터의 Wi-Fi 페이지가 열리는 경로는 `MenuBar::OpenStatusPanel`의 첫 줄에
이미 들어가 있습니다. `SampleWlan`이 만드는 `StatusPanel`(상태, 네트워크, 설정 열기 세 줄)은 그래서
화면에 뜨지 않습니다. 죽은 코드입니다.

Wi-Fi 목록 조회와 연결과 라디오 토글은 `src/control_center.cpp`에 전부 구현되어 있습니다.
`RefreshPageLists`가 `WlanGetAvailableNetworkList`로 목록을 만들고, `Invoke`의 `kPageList` 분기가
`WlanConnect`를 부르며, `kPageToggle` 분기가 `WlanSetInterface`로 라디오를 켜고 끕니다.
**이 로직은 그대로 살려서 재사용하십시오. 다시 쓰지 마십시오.**

## 2. 목표

1. 트래픽 위젯을 없앱니다.
2. Wi-Fi 위젯의 이름을 네트워크로 바꿉니다. 메뉴 항목과 툴팁과 위젯 아이디가 모두 바뀝니다.
3. 패널을 목표 이미지의 모양으로 다시 그립니다. 곡률과 여백과 글자 크기까지 6절의 수치표대로 맞춥니다.
4. 상단바 아이콘이 활성 연결의 종류를 따라갑니다. Wi-Fi면 Wi-Fi 신호이고 이더넷이면 이더넷 아이콘입니다.

목표 이미지에서 **개인용 핫스팟 구역(`Personal Hotspot` 머리글과 `iPhone` 줄)은 만들지 마십시오.**
애플 기기끼리만 되는 기능이라 Windows에 대응물이 없습니다.

패널 안의 머리글은 목표 이미지 그대로 `Wi-Fi`입니다. 위젯의 이름만 네트워크로 바뀝니다.
맥도 메뉴 막대 항목은 네트워크이고 패널의 첫 줄은 Wi-Fi 토글입니다.

---

## 3. 1단계: 트래픽 위젯을 없앤다

### 3-1. `src/widgets/builtin.cpp`

지울 것은 다음과 같습니다.

- 상수 `kNetId`, `kNetPriority`, `kNetPeriodMs`, `kNetGlyph`
- 구조체 `NetSnap`과 함수 `ReadNet` 전체
- 멤버 함수 `SampleNet` 전체
- `Publish`와 `DropItem`의 `kNetId` 분기
- `SampleDue`, `NextDeadlineLocked`, `HasSampleDeadlineLocked`, `SetSettings`의 트래픽 관련 줄
- 멤버 `net_due_`, `net_has_baseline_`, `net_in_`, `net_out_`, `net_tick_`, `session_in_`,
  `session_out_`, `fp_net_`, `logged_slow_if_`

`GetIfTable2`를 부르는 곳이 여기밖에 없으면 그 헤더 포함도 정리하십시오. 다만 5절에서 활성 연결의
종류를 판정할 때 `iphlpapi`를 다시 쓰므로 링크 설정은 그대로 둡니다.

`PendingAction::kNetworkSettings`는 **남깁니다.** 새 패널의 `네트워크 설정…` 줄이 씁니다.

### 3-2. 설정

`WidgetSettings`에서 `wifi`를 지우고 `network`를 남깁니다. 남은 `network`의 뜻이 트래픽에서
네트워크 위젯으로 바뀝니다.

`LoadWidgetSettings`는 이렇게 읽으십시오. 옛 설정 파일에서 둘 중 하나라도 켜져 있었으면 새 위젯을 켭니다.

```cpp
s.network = json::GetBool(*widgets, "network").value_or(false) ||
            json::GetBool(*widgets, "wifi").value_or(false);
```

`SaveWidgetSettings`는 `network`만 씁니다. `wifi` 키는 더 쓰지 않습니다.

`bar_order`에는 옛 아이디가 남아 있습니다. 로드 직후에 한 번 이관하십시오.

- `"bamti.widget/wifi"`가 있으면 `"bamti.widget/network"`로 바꿉니다.
- `"bamti.widget/net"`은 지웁니다.
- 이관한 뒤에 `"bamti.widget/network"`가 둘이 되면 앞의 것만 남깁니다.

이관을 빼먹어도 막대가 깨지지는 않습니다. `MenuBar::OrderedItems`가 짝이 없는 아이디를 그냥 건너뛰고
새 항목을 뒤에 붙이기 때문입니다. 다만 사용자가 정해 둔 자리가 초기화되므로 이관하십시오.

### 3-3. 메뉴

`src/menu_bar.cpp`에서 `kWidgetWifiCmd`와 그 처리(`next.wifi`를 뒤집는 줄)를 지웁니다.
`kWidgetNetworkCmd`는 그대로 두고 `next.network`를 뒤집게 합니다. 메뉴 라벨은 `네트워크` 하나만
남고 `Wi-Fi` 줄은 없어집니다.

---

## 4. 2단계: 위젯의 정체성

| 자리 | 옛 값 | 새 값 |
| --- | --- | --- |
| 아이템 아이디 | `bamti.widget/wifi` | `bamti.widget/network` |
| 상수 이름 | `kWifiId` | `kNetworkId` |
| 우선순위 상수 | `kWifiPriority` = 36 | `kNetworkPriority` = 36 |
| 지문 멤버 | `fp_wifi_` | `fp_network_` |
| 툴팁 | `Wi-Fi · <SSID>` | 5-3절 참조 |
| `menu_bar.cpp` 상수 | `kWifiItemId` | `kNetworkItemId` |
| `menu_bar.cpp` 함수 | `ToggleWifiPanel` | `ToggleNetworkPanel` |

`ControlCenterContent::ShowsWifi()`는 이름을 `ShowsNetwork()`로 바꾸십시오. 부르는 쪽은
`MenuBar::ToggleNetworkPanel` 한 곳입니다.

우선순위 36은 그대로 둡니다. 볼륨(35)과 배터리(40) 사이라는 관계가 바뀌면 막대 순서가 달라집니다.

---

## 5. 3단계: 활성 연결의 종류와 상단바 아이콘

### 5-1. 무엇을 판정하는가

지금 인터넷이 어느 인터페이스로 나가는지를 판정합니다. 트래픽이 제일 많은 인터페이스가 아닙니다.
옛 `ReadNet`이 그렇게 골랐는데, 그 방식은 큰 파일을 받는 동안 가상 어댑터가 1등이 되면 틀립니다.

`GetBestRoute2`에 목적지 `0.0.0.0`(`AF_INET`)을 넣어 기본 경로의 인터페이스 인덱스를 얻고,
그 인덱스로 `GetIfEntry2`를 불러 `Type`과 `Alias`를 읽으십시오.

```
Type == IF_TYPE_IEEE80211  또는  PhysicalMediumType == NdisPhysicalMediumNative802_11  → Wi-Fi
Type == IF_TYPE_ETHERNET_CSMACD                                                        → 이더넷
그 밖 또는 경로 없음                                                                    → 연결 없음
```

`GetBestRoute2`가 실패하면 연결 없음으로 봅니다. 실패를 한 번은 로그로 남기되 매 주기마다 남기지
마십시오. 옛 `logged_slow_if_`가 쓰던 한 번만 찍는 방식을 그대로 따르십시오.

### 5-2. 어디서 재는가

`BuiltinWidgets::SampleWlan`을 `SampleNetwork`로 바꾸고 그 안에서 함께 재십시오. 주기는 지금 쓰는
2초(`kBrightnessPeriodMs`)를 그대로 씁니다. 워커 스레드이므로 UI를 막지 않습니다.

**첫 회차의 소요 시간을 로그로 남기십시오.** `GetIfTable2`의 첫 회차가 9ms까지 걸린 전례가 있어
새로 부르는 API도 같은 방식으로 확인해야 합니다. 5ms를 넘으면 그 사실도 함께 남기십시오.

### 5-3. 아이템에 싣는 값

```
연결 종류 = Wi-Fi   → 아이콘 VectorIcon::kWifi,      value = 1.0f
연결 종류 = 이더넷  → 아이콘 VectorIcon::kEthernet,  value = 1.0f
연결 없음           → 아이콘 VectorIcon::kWifi,      value = 0.0f
```

`VectorIcon::kNetwork`는 트래픽 위젯 전용이었으므로 열거에서 지우고 그 자리에 `kEthernet`을
더하십시오. **열거 값의 수를 바꾸면 프로토콜 호환이 깨집니다.** `docs/STATUS-PROTOCOL.md`에 벡터
아이콘 이름이 실려 있는지 확인하고, 실려 있으면 문서도 같이 고치십시오.

툴팁은 이렇게 만듭니다.

```
Wi-Fi 연결   →  "Wi-Fi · <SSID>"
이더넷 연결  →  "이더넷 · <어댑터 별칭>"
연결 없음    →  "연결 안 됨"
```

막대에 글자는 붙이지 않습니다. 지금처럼 아이콘만 둡니다.

### 5-4. 아이콘을 그린다

`ClockRenderer::DrawVectorIcon`의 `kNetwork` 분기를 `kEthernet`으로 바꾸고 폰트 글리프 대신
**직접 그리십시오.** 지금 쓰는 `U+E839`는 모니터와 받침대 모양이라 사용자가 요청한 모양과 다릅니다.
`kNetworkFluent`와 `kNetFallback` 상수는 다른 곳에서 안 쓰이면 지웁니다.

목표 이미지의 상단바 맨 오른쪽 아이콘을 실측한 값입니다. 원본에서 40 × 22 픽셀이고 2배 화면이므로
**20 × 11 DIP 상자**입니다. 상자의 좌상단이 원점입니다.

```
좌 꺾쇠   (4.40, 0.40) → (0.40, 5.50) → (4.40, 10.60)
우 꺾쇠   (15.60, 0.40) → (19.60, 5.50) → (15.60, 10.60)
점 세 개  중심 y = 5.50,  중심 x = 6.50 / 10.00 / 13.50,  지름 2.00

선 굵기 1.80   끝 모양 둥글게   이음 둥글게
```

- 세 점의 간격은 3.50이고 가운데 점이 상자의 중앙입니다. 좌우 꺾쇠도 중앙을 기준으로 대칭입니다.
  한쪽으로 쏠려 보이면 대칭이 깨진 것입니다.
- 다른 크기로 그릴 때는 모든 수치에 `높이 / 11`을 곱하십시오. 선 굵기와 점 지름도 함께 곱합니다.
- 색은 다른 벡터 아이콘과 같은 `ClockTextColor(dark)`입니다.
- 꺾쇠는 채우지 말고 선으로만 그리십시오. `EnsureStroke()`가 만드는 `round_stroke_`를 재사용하고,
  끝 모양이 둥글지 않으면 이 아이콘 전용 스타일을 하나 더 만드십시오.

`kWifi` 분기는 지금처럼 `U+E701` 글리프를 씁니다. 다만 `value`를 지금은 무시하고 있으므로 고치십시오.
`value < 0.5f`면 `ScaleAlpha(ClockTextColor(dark), 0.45f)`로 흐리게 그려 연결이 없음을 나타냅니다.

막대의 폭도 함께 고쳐야 합니다. 폭을 계산하는 자리가 두 군데입니다.

- `src/bar_layout.cpp`의 `StatusIconWidth`: 배터리처럼 `kEthernet`도 폭을 따로 돌려주게 합니다.
  `kEthernetIconDip = 20.0f`를 더하십시오.
- `src/clock_renderer.cpp`에서 그리는 자리의 `icon_w` 계산: 같은 조건을 더합니다.

두 곳의 값이 어긋나면 아이콘이 이웃 항목을 침범하거나 빈칸이 생깁니다. 반드시 같은 상수를 보게 하십시오.

---

## 6. 4단계: 패널 레이아웃

### 6-1. 어디에 그리는가

제어 센터의 Wi-Fi 페이지를 새 모양으로 바꾸고 위젯도 같은 화면을 씁니다. 화면을 둘로 나누지 마십시오.
`MenuBar::OpenStatusPanel`이 이미 `ControlCenterPage::kWifi`로 제어 센터를 여는 경로를 갖고 있습니다.

- `Page::kWifi`의 렌더링은 새로 만드는 `RenderNetworkPage`가 맡습니다.
- 지금의 `RenderListPage`는 **Bluetooth 전용으로 남깁니다.** 두 화면의 구조가 달라졌으니 한 함수에
  분기를 더 넣지 마십시오.
- `BuildHits`의 `page_ != Page::kHome` 분기도 Wi-Fi와 Bluetooth로 나누십시오.
- 제어 센터 홈에서 들어오면 `show_back_`이 참이라 뒤로 단추가 붙고, 위젯에서 열면 거짓이라 붙지
  않습니다. 이 동작은 지금 그대로입니다.

`ControlCenterContent::HeightDip()`은 지금 홈 화면의 높이만 계산합니다. 목록 화면도 그 높이를 쓰고
있어서 항목이 몇 개든 높이가 같습니다. **페이지별로 높이를 나누십시오.** Wi-Fi 페이지의 높이는
6-3절의 합계이고 목록 개수에 따라 달라집니다.

### 6-2. 실측 수치의 출처

목표 이미지에서 잰 값입니다. 원본이 2배 화면이라 픽셀을 2로 나눈 값이 DIP입니다.
**아래 표의 DIP 열을 그대로 상수로 쓰십시오.** 눈대중으로 바꾸지 마십시오.

패널 전체입니다.

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 패널 폭 | 616 | **308** |
| 모서리 반지름 | 33.4 | **16** (아래 설명 참조) |
| 좌우 콘텐츠 인셋 | 28 | **14** |
| 패널 위 → 머리글 행 위 | 18 | **9** |
| 마지막 줄 아래 → 패널 아래 | 21 | **10** |

모서리 반지름의 실측값 16.7 DIP는 `corner::kHeroDip`(16)과 사실상 같습니다. **새 상수를 만들지 말고
`corner::kHeroDip`을 쓰십시오.** `PopupSurface`가 이미 계층으로 곡률을 정하고 `IsHero`가 참이면
스쿼클로 그립니다. 팝업이 지금 어느 계층으로 열리는지 확인하고, hero가 아니면 이 패널만 hero로
올리십시오. 원호로 피팅했을 때 잔차가 0.45픽셀이라 실측 곡선 자체는 원호에 가깝고,
`kSquircleExponent`(2.4)가 그 곡선과 맞습니다.

폭 308 DIP는 지금 제어 센터의 `kCcWidthDip`(340)보다 좁습니다. 제어 센터 홈은 340을 유지하고
**Wi-Fi 페이지만 308을 쓰십시오.** `Measure`가 페이지에 따라 다른 폭을 돌려주면 됩니다.

### 6-3. 세로 구성

위에서 아래로 이렇게 쌓습니다. 오른쪽 숫자가 그 구역의 높이이고 단위는 DIP입니다.

```
패널 위 여백                      9
머리글 행  "Wi-Fi" + 토글        24
간격                              9
이더넷 줄                        16      (이더넷이 연결됐을 때만)
간격                              8
구분선                            1
간격                              8
구역 머리글  "알려진 네트워크"   17      (해당 항목이 하나라도 있을 때만)
목록 행                       32 × n
구역 머리글  "다른 네트워크"     17      (해당 항목이 하나라도 있을 때만)
목록 행                       32 × m
간격                              8
구분선                            1
간격                              8
"네트워크 설정…"                 22
"Wi-Fi 설정…"                    22
패널 아래 여백                   10
```

- 구역 머리글의 17은 글자를 담는 높이입니다. 실측에서 구분선과 첫 머리글 글자의 중심 거리가 33.5픽셀,
  머리글 중심과 첫 목록 행 중심의 거리가 54픽셀이었고, 위의 쌓기가 그 값을 재현합니다. 결과가
  어긋나면 간격 8을 조정하지 말고 머리글 높이를 조정하십시오.
- 이더넷이 연결되지 않았으면 이더넷 줄과 그 위의 간격을 통째로 뺍니다. 빈 줄을 남기지 마십시오.
- 목록이 하나도 없으면 목록 자리에 `사용 가능한 네트워크가 없습니다`를 한 줄(32) 넣습니다.

### 6-4. 머리글 행

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 행 높이 | 48 | **24** |
| 제목 글자 크기 | cap 20 | **14, semibold** |
| 토글 트랙 | 108 × 48 | **54 × 24** |
| 토글 노브 지름 | 38 | **19** |
| 노브와 트랙 사이 여백 | 5 | **2.5** |

- 제목 글자와 토글은 행 안에서 세로 가운데 정렬입니다. 실측에서 둘의 중심이 같은 y였습니다.
- 토글의 오른쪽 끝이 콘텐츠 인셋(14)과 맞습니다.
- 트랙의 곡률은 `corner::PillPx(높이)`입니다. 이미 쓰고 있는 방식 그대로입니다.
- 켜짐은 `AccentFillColor(dark)`, 꺼짐은 `BadgeOffFill(dark)`, 노브는 `AccentOnColor(dark)`입니다.
- 지금 제어 센터의 토글은 44 × 24입니다. Wi-Fi 페이지의 토글만 54 × 24로 넓히십시오.

### 6-5. 이더넷 줄

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 글자 크기 | cap 17 | **12, regular** |
| 왼쪽 인셋 | 28 | **14** |

색은 `ScaleAlpha(fg, 0.55f)`입니다. 문구는 `이더넷: <어댑터 별칭>`이고 별칭은 5-1절에서 얻은
`GetIfEntry2`의 `Alias`입니다. 별칭이 비면 `이더넷: 연결됨`으로 씁니다.

이더넷의 연결 여부와 별칭은 `ControlCenterLive`에 실어 나르십시오. 필드 두 개를 더합니다.

```cpp
bool eth_on = false;
std::wstring eth_name;
```

`BuiltinWidgets::LiveForControlCenter`가 5-1절의 판정 결과를 채우고
`ControlCenterContent::ApplyLive`가 받습니다. 패널이 제 스레드에서 `GetBestRoute2`를 다시 부르지
않게 하십시오. UI 스레드에서 부르면 팝업이 열릴 때 멈출 수 있습니다.

### 6-6. 구분선

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 좌우 인셋 | 28 | **14** |
| 두께 | 1~2 | **1** |

색은 전경색의 알파 0.10 정도입니다. `ScaleAlpha(fg, 0.10f)`을 쓰고, 라이트와 다크에서 각각 보고
너무 진하거나 흐리면 조정한 값을 보고하십시오. 좌우 인셋이 콘텐츠 인셋과 같아서 구분선이 목록과
같은 폭으로 끝납니다.

### 6-7. 구역 머리글

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 글자 크기 | cap 17 | **12, regular** |
| 왼쪽 인셋 | 28 | **14** |

색은 `ScaleAlpha(fg, 0.55f)`입니다. 문구는 `알려진 네트워크`와 `다른 네트워크`입니다. 가르는 기준은
`WLAN_AVAILABLE_NETWORK_HAS_PROFILE` 플래그이고 `RefreshPageLists`가 이미 `has_profile`로
읽고 있습니다.

목표 이미지의 `Other Networks`에는 오른쪽에 펼침 화살표가 있어 하위 메뉴로 들어갑니다.
**하위 메뉴는 만들지 마십시오.** 팝업을 겹쳐 여는 비용이 크고 지금 구조에 그 경로가 없습니다.
머리글 아래에 그대로 펼쳐 두고 개수를 6-8절의 상한으로 자르십시오.

### 6-8. 목록 행

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 행 높이 | 64 | **32** |
| 아이콘 원 지름 | 52 | **26** |
| 원의 왼쪽 = 콘텐츠 인셋 | 28 | **14** |
| 원 오른쪽 → 글자 왼쪽 | 18 | **9** |
| 글자 왼쪽(패널 기준) | 98 | **49** |
| SSID 글자 크기 | cap 20 | **14, regular** |
| 자물쇠 | 18 × 25 | **9 × 12.5** |
| 자물쇠 오른쪽 → 콘텐츠 인셋 안쪽 | 4 | **2** |

- 아이콘 원과 글자는 행 안에서 세로 가운데 정렬입니다.
- 연결된 항목의 원은 `AccentFillColor(dark)`로 채우고 안의 Wi-Fi 글리프를 `AccentOnColor(dark)`로
  그립니다. 연결되지 않은 항목의 원은 `BadgeOffFill(dark)`로 채우고 글리프는 전경색입니다.
  실측에서 채워진 원이 `#0088FE`, 빈 원이 `#CED4E0`이었고 둘 다 반투명 패널 위의 값입니다.
  우리는 색을 직접 박지 말고 위의 테마 헬퍼를 쓰십시오.
- 원 안의 글리프는 `kWifiGlyph`(`U+E701`)이고 원 지름의 55% 정도로 그립니다. 실측에서 원 52픽셀에
  글리프가 28픽셀 남짓이었습니다.
- 자물쇠는 `kLockGlyph`(`U+E72E`)를 `ScaleAlpha(fg, 0.55f)`로 그립니다. 보안이 걸린 망에만 붙입니다.
- 지금 목록에 있는 세로 강조 막대와 `연결됨, 보안` 부제와 `연결 끊기` 배지는 **전부 지우십시오.**
  목표 화면에 없습니다. 연결 상태는 원의 색으로만 나타냅니다.
- 호버 강조는 행 전체에 `MenuItemHoverFill(dark, false)`를 깔고 곡률은 `corner::HoverPx(행 높이, dpi)`
  입니다. 지금 방식 그대로입니다.
- 상한은 알려진 망 6개, 다른 망 6개, 합계 10개입니다. `kPageListMax`(6)를 이 규칙에 맞게 고치십시오.
  Bluetooth 페이지가 같은 상수를 쓰고 있으니 상수를 나누십시오.

### 6-9. 아래쪽 설정 줄

| 항목 | 실측 px | DIP |
| --- | ---: | ---: |
| 줄 높이 | 44 | **22** |
| 글자 크기 | cap 19 | **13, regular** |
| 왼쪽 인셋 | 28 | **14** |

두 줄입니다.

```
네트워크 설정…   →  ms-settings:network
Wi-Fi 설정…      →  ms-settings:network-wifi
```

말줄임표는 한 글자 `…`(U+2026)를 쓰십시오. 마침표 세 개가 아닙니다.
지금의 `추가 Wi-Fi 설정` 한 줄은 이 두 줄로 대체합니다.
호버 강조는 목록 행과 같은 방식입니다.

---

## 7. 5단계: 동작

| 누른 곳 | 하는 일 |
| --- | --- |
| 머리글의 토글 | `WlanSetInterface`로 라디오를 뒤집고 목록을 다시 읽습니다 |
| 목록 행 (연결 안 된 망) | `WlanConnect`로 바로 연결하고 목록을 다시 읽습니다 |
| 목록 행 (연결된 망) | 아무 것도 하지 않습니다 |
| `네트워크 설정…` | `ms-settings:network` |
| `Wi-Fi 설정…` | `ms-settings:network-wifi` |

- 연결과 토글의 구현은 `ControlCenterContent::Invoke`에 이미 있습니다. 히트 아이디만 새 레이아웃에
  맞게 이으십시오.
- 지금 연결된 행을 누르면 `WlanDisconnect`가 불리는 `kPageAction` 경로가 있습니다. 배지를 없애므로
  이 경로도 지웁니다. 연결을 끊는 일은 Windows 설정에서 하게 둡니다.
- 라디오가 꺼져 있으면 목록 자리에 `Wi-Fi가 꺼져 있습니다`를 한 줄 넣고 목록을 그리지 않습니다.
- 무선 어댑터가 아예 없으면 머리글의 토글을 그리지 않고 목록 자리에 `무선 어댑터가 없습니다`를
  넣습니다. 이더넷 줄과 설정 줄은 그대로 둡니다.

`WlanConnect`는 프로필이 없는 보안 망에서 암호를 물어야 하므로 대개 실패합니다. 실패해도 팝업을 닫지
말고 로그만 남기십시오. 지금 `Log(L"cc", L"wifi connect %s err=%lu", ...)`가 하는 그대로입니다.

---

## 8. 검증

빌드는 Release 클린 빌드로 하고 경고가 없어야 합니다.

1. **트래픽 위젯이 사라졌는지.** 상단바에 `⇅ 7.1KB/s · 2.5KB/s`가 없어야 하고, 우클릭 메뉴에
   `네트워크` 한 줄만 있고 `Wi-Fi` 줄이 없어야 합니다.
2. **옛 설정 파일이 이관되는지.** 지금 설정 파일을 복사해 두고 새 빌드를 한 번 띄운 뒤, 저장된
   파일에서 `wifi` 키가 사라지고 `network`가 남았는지 확인하십시오. 막대에서 네트워크 아이콘의
   자리가 옛 Wi-Fi 아이콘의 자리와 같아야 합니다.
3. **아이콘이 연결을 따라가는지.** 랜선을 꽂았다 뽑으면서 2초 안에 아이콘이 이더넷과 Wi-Fi 사이를
   오가는지 보십시오. Wi-Fi를 끄면 흐린 Wi-Fi 아이콘이 되어야 합니다.
4. **레이아웃이 수치표와 맞는지.** 패널을 띄워 화면을 찍고 6절 표의 값을 픽셀로 환산해 재어
   보십시오. 150% 배율에서는 표의 DIP에 1.5를 곱한 값이 나와야 합니다.
   **어긋나는 항목이 있으면 그 항목과 실제 값을 보고하십시오.**
5. **배율 세 가지.** 100%, 150%, 200%에서 각각 띄워 글자가 잘리거나 아이콘이 겹치지 않는지 보십시오.
6. **테마 두 가지.** 라이트와 다크에서 각각 구분선과 흐린 글자가 보이는지 확인하십시오.
   흰 선이 라이트 테마에서 사라진 전례가 있습니다.
7. **연결이 되는지.** 알려진 망을 눌러 실제로 붙는지, 붙은 뒤에 원의 색이 바뀌는지 보십시오.
8. **토글이 되는지.** Wi-Fi를 끄고 켜면서 목록이 사라지고 돌아오는지 보십시오.
9. **제어 센터에서 들어가도 같은지.** 제어 센터 홈에서 Wi-Fi 타일을 눌러 들어간 화면이 위젯에서 연
   화면과 같아야 합니다. 다른 점은 뒤로 단추 하나뿐입니다.
10. **첫 회차 소요 시간.** 5-2절의 로그에서 `GetBestRoute2`와 `GetIfEntry2`의 첫 회차 값을
    확인하고 보고하십시오.

레지스트리를 쓰는 검증은 넣지 마십시오. 되돌리지 못하고 사용자 설정을 날린 전례가 있습니다.

---

## 9. 보고할 것

- 패널을 찍은 화면. 라이트와 다크 각각, 100%와 150% 각각.
- 상단바 아이콘을 찍은 화면. Wi-Fi 연결, 이더넷 연결, 연결 없음 세 가지.
- 6절 수치표에서 값을 바꾼 항목이 있으면 바꾼 값과 이유.
- 8-2의 설정 이관 결과. 이관 전후의 `bar_order` 내용.
- 8-10의 첫 회차 소요 시간.
- 목록이 비었을 때와 Wi-Fi가 꺼졌을 때와 어댑터가 없을 때의 화면. 어댑터가 없는 경우를 만들 수
  없으면 그 사실을 적으십시오.
