# 작업 지시서: 패널 갱신 뒤의 히트 배열 경계

`FIX-PANEL-AND-SAVE.md`의 1번 커밋(`4511b08`)을 검수하다가 찾은 결함입니다. 그 커밋의 판단 자체는 옳았고, 그 판단 때문에 드러난 기존 렌더러의 빈틈을 막는 작업입니다.

고칠 파일은 `src/status_panel.cpp`와 `src/menu_bar.cpp` 두 곳이며, 커밋은 하나입니다.

---

## 1. 결함

`StatusPanelContent::Render`는 상호작용 행을 그릴 때 `hits_`를 **경계 검사 없이** 훑습니다.

```cpp
// src/status_panel.cpp, kButton 분기
const Hit& hit = hits_[static_cast<size_t>(hit_i++)];
const StatusRow& button = rows[static_cast<size_t>(hit.row)];

// kToggle 분기
const Hit& hit = hits_[static_cast<size_t>(hit_i++)];
```

`hits_`는 `Measure`가 채우고, `Measure`는 `PopupSurface::Open`에서 **열 때 한 번만** 불립니다. 그런데 이제 `RefreshOpenPanel`이 열려 있는 패널의 항목을 바꿔 끼웁니다. `Reset`은 `hits_`를 건드리지 않으므로, 새 항목의 행 구성이 열 때와 다르면 `hits_`의 길이와 `Render`가 소비하는 개수가 어긋납니다.

`RefreshOpenPanel`이 막아 주는 것은 **행 개수**뿐입니다. 개수가 같으면서 종류가 다른 경우는 통과합니다.

재현 경로입니다.

1. 파이프 클라이언트가 `[gauge, text]` 두 행짜리 패널을 올립니다. 상호작용 행이 없으므로 `hits_`는 비어 있습니다.
2. 사용자가 그 패널을 엽니다.
3. 클라이언트가 `[gauge, button]`을 `upsert`합니다. 행 개수는 그대로 둘입니다.
4. `RefreshOpenPanel`이 개수만 보고 통과시켜 항목을 바꿔 끼웁니다.
5. `Render`가 `kButton` 분기에 들어가 **비어 있는 `hits_`의 0번을 읽습니다.**

`std::vector`의 범위 밖 읽기이므로 쓰레기 `RECT`와 쓰레기 `hit.row`가 나오고, 이어지는 `rows[hit.row]`에서 대개 죽습니다. Release 빌드에서는 조용히 이상한 화면이 나올 수도 있습니다.

내장 위젯은 행 구성이 고정이라 이 경로를 타지 않습니다. **파이프 클라이언트가 상태에 따라 행 종류를 바꾸면 걸립니다.** 명세가 허용하는 정상적인 사용 방식입니다.

### 1-1. `hits_.clear()`를 되돌리지 마십시오

`4511b08`은 `Reset`에서 `hits_.clear()`를 지웠습니다. **이 판단은 옳습니다.** 되돌리면 갱신 직후의 `Render`가 언제나 빈 배열을 읽게 되어, 지금은 드문 경로인 문제가 상시 문제가 됩니다.

참고로 그 커밋 이전에도 `OnToggleTimeout`이 `Reset` 뒤에 바로 `InvalidateRect`를 불렀기 때문에, 토글 되돌리기가 일어날 때마다 같은 범위 밖 읽기가 발생하고 있었습니다. 이 결함은 새로 생긴 것이 아니라 원래 있던 것이 드러난 것입니다.

---

## 2. 고치는 방법

방어를 두 겹으로 둡니다. 하나는 애초에 어긋난 항목이 들어오지 못하게 막고, 다른 하나는 그래도 어긋났을 때 죽지 않게 합니다. 둘 다 넣어 주십시오.

### 2-1. 겹 하나: 행 구성이 달라지면 갱신하지 않는다

`MenuBar::RefreshOpenPanel`의 행 개수 비교를 **행 종류의 나열 비교**로 바꿉니다. 개수와 순서와 종류가 모두 같을 때만 갱신하고, 하나라도 다르면 지금 개수가 다를 때와 똑같이 팝업을 닫습니다.

`open_panel_rows_`(현재 `size_t`)를 종류 나열을 담는 `std::vector<RowType>`으로 바꾸면 됩니다. `OpenStatusPanel`에서 채우고 `RefreshOpenPanel`에서 비교합니다. 두 개의 멤버로 늘리지 말고 하나로 바꾸십시오. 개수는 나열의 크기로 알 수 있습니다.

`row_id`나 레이블까지 비교하지는 마십시오. 그것들이 바뀌어도 히트 영역의 개수는 그대로이므로 갱신해도 안전하고, 오히려 값이 멈추는 원래 결함이 되살아납니다. **종류의 나열만** 봅니다.

### 2-2. 겹 둘: 렌더러가 범위를 넘지 않는다

`StatusPanelContent::Render`의 두 곳에서 `hits_`를 읽기 전에 `hit_i`가 `hits_.size()`보다 작은지 확인합니다. 아니면 그 행의 상호작용 표시를 그리지 않고 넘어갑니다. `hit.row`로 `rows`를 인덱싱하는 곳도 같이 확인합니다.

넘어갈 때 세로 좌표(`y`) 전진은 그대로 해야 합니다. 그리지 않는다고 좌표까지 건너뛰면 아래 행들이 위로 밀려 올라갑니다.

이 상황이 실제로 일어나면 `Log(L"panel", ...)`로 한 번 남겨 주십시오. 2-1절이 제대로 막고 있으면 이 로그는 나오지 않아야 합니다. 나온다면 막지 못한 경로가 또 있다는 뜻입니다.

---

## 3. 하지 말아야 할 것

1. `Reset`에서 `hits_`를 다시 비우지 마십시오(1-1절).
2. 갱신할 때 `Measure`를 다시 부르지 마십시오. `FIX-PANEL-AND-SAVE.md` 1-2절의 이유가 그대로입니다. 창 크기가 흔들립니다.
3. 행 구성이 달라졌을 때 팝업을 닫는 대신 다시 여는 방식으로 바꾸지 마십시오. 커서 아래에서 창이 깜빡입니다.
4. `Render`에 경계 검사를 넣으면서 그리기 순서나 여백 계산을 함께 손보지 마십시오.
5. 오버플로 팝업은 이번에도 손대지 않습니다.

---

## 4. 검증

**빌드**
- [ ] `/W4` 경고 없이 Debug와 Release가 빌드됩니다.

**결함**
- [ ] 파이프 클라이언트로 `[gauge, text]` 패널을 올리고 연 다음, 같은 `id`로 `[gauge, button]`을 보냅니다. 죽지 않고 팝업이 닫힙니다.
- [ ] 같은 방식으로 `[kv, toggle]`을 연 뒤 `[kv, kv]`를 보내도 죽지 않습니다.
- [ ] 위 두 경우에 `panel` 로그가 남지 않습니다. 2-1절에서 걸러졌다는 뜻입니다.

**회귀**
- [ ] CPU 패널을 열어 두면 값이 계속 갱신됩니다. 행 구성이 같으므로 닫히지 않아야 합니다.
- [ ] 네트워크 패널도 같습니다.
- [ ] 토글 행이 있는 클라이언트 패널에서 스위치를 눌렀을 때, 위젯이 갱신되어도 스위치가 유지되고 응답이 없으면 2초 뒤에 되돌아갑니다.
- [ ] 되돌아간 뒤에도 그 패널의 버튼과 토글을 계속 누를 수 있습니다.
- [ ] 패널 위 호버 강조가 갱신 뒤에도 유지됩니다.

---

## 5. 커밋

| 순서 | 작업 | 커밋 메시지 |
|---|---|---|
| 1 | 행 구성 비교와 렌더러 경계 검사 | `fix: 패널 행 구성이 바뀌면 낡은 히트 영역을 쓰지 않는다` |
