# 수정 지시서: 선점 작업의 진단 코드와 자동 시작을 고친다

작성일: 2026-08-31
선행 문서: `TASK-TRAY-PREEMPT.md`
대상: 그 지시서로 구현한 코드 가운데 다섯 곳

---

## 0. 먼저 알아 둘 것

선행 작업은 잘 되었습니다. 선기동 15ms, 우선순위 확보 16ms, explorer 재시작 때 재브로드캐스트 1회와 `suppressed` 1회가 로그로 확인됐습니다. 가장 걱정했던 무한 루프는 막혔습니다.

고칠 것은 **진단 코드가 만든 부작용 셋과 자동 시작의 결함 하나, 문서 오염 하나**입니다. 3절의 결함은 선행 지시서가 잘못 적은 탓이므로 이번에 규칙을 바꿉니다.

이 다섯 가지 가운데 1절, 2절, 4절은 **재부팅 측정을 하기 전에 반드시 고쳐야 합니다.** 지금 상태로 측정하면 숫자 자체를 믿을 수 없습니다.

---

## 1. 진단용 UIA 열거가 영원히 돈다

### 1-1. 무엇이 문제인가

`src/tray_mirror.cpp` 686행에서 `diag_uia`를 만들고 785행에서 매 라운드마다 `Enumerate`를 부릅니다. 보고서에는 "로그용으로만 한 번씩 돌린다"고 적혀 있지만, 실제로는 **프로세스가 살아 있는 내내 5초마다** 돕니다. `kSafetyIntervalMs = 5000`이 항상 라운드를 깨우기 때문입니다.

같은 로그에 UIA 백엔드의 열거 비용이 `enum_ms=16`으로 찍혀 있습니다. 5초마다 16ms이므로 코어 하나 기준 약 0.3%이고, 유휴 0.2% 예산을 이것 하나로 넘깁니다.

가로채기 백엔드는 푸시 방식이라 폴링이 필요 없다는 점이 존재 이유입니다. 진단 코드가 그 이점을 통째로 되돌려 놓았습니다.

### 1-2. 어떻게 고치는가

진단 열거에 **수명을 줍니다.**

```cpp
constexpr ULONGLONG kDiagWindowMs = 180000;  // 3분
```

- `diag_uia`를 만들 때 시작 시각을 기록합니다.
- 라운드마다 경과를 확인해서 `kDiagWindowMs`를 넘겼으면 3절의 로스터를 남긴 뒤 `diag_uia.reset()`으로 놓아 버립니다.
- 창이 닫힌 뒤에는 다시 만들지 않습니다.

3분은 로그온 직후의 등록이 전부 끝나기에 충분하고, 재부팅 측정에서 사용자가 2분 뒤에 로그를 보는 절차와도 맞습니다.

`diag_uia`가 살아 있는 동안에도 매 5초는 촘촘합니다. **진단 열거는 최소 5초 간격을 지키되 라운드가 그보다 자주 돌면 건너뛰십시오.** 즉 마지막 진단 열거 시각을 따로 들고 있다가 5초가 지났을 때만 부릅니다. 지금은 `DoRound` 바로 뒤에 무조건 붙어 있어서 라운드가 몰리면 함께 몰립니다.

---

## 2. 항목 로그가 중복돼 개수 비교가 어긋난다

### 2-1. 무엇이 문제인가

같은 앱이 150ms 안에 두 번씩 찍혔습니다.

```
10:32:33.384  intercept item tip="KakaoTalk" exe=KakaoTalk.exe hwnd=0x30304 uid=222 guid=0
10:32:33.545  intercept item tip="KakaoTalk" exe=KakaoTalk.exe hwnd=0x30304 uid=222 guid=0
```

`hwnd`와 `uid`가 같으므로 키도 같습니다. 그런데도 두 번 찍힌 이유는 앱이 `TaskbarCreated`를 받을 때 `NIM_DELETE`를 보내고 곧바로 `NIM_ADD`를 보내는 관례를 쓰기 때문입니다. 삭제 분기가 `items_`에서 항목을 지우므로 다음 `NIM_ADD`에서 `created_now`가 다시 참이 됩니다. explorer가 뿌린 브로드캐스트와 우리가 뿌린 브로드캐스트가 각각 한 번씩 이 순환을 일으켜 두 줄이 됩니다.

**원인은 선행 지시서 8-3절입니다.** "사라졌다가 다시 나타나면 다시 1회를 허용하되 최대 5회"라고 적었는데, 그 규칙이 재등록마다 새 줄을 만듭니다. 9-3절이 요구하는 "줄 수를 세어 비교한다"와 정면으로 충돌합니다.

### 2-2. 어떻게 고치는가

**항목 줄은 키마다 프로세스 수명에 딱 한 번만 남깁니다.** 재출현 허용을 없애십시오.

- `src/tray_intercept.cpp`의 `MaybeLogNewItem`이 쓰는 `item_log_n_`을 횟수 맵이 아니라 **이미 남긴 키의 집합**으로 바꿉니다. 집합에 있으면 그냥 돌아옵니다.
- `kItemLogMax` 상수는 지웁니다.
- `src/tray_backend_uia.cpp`의 `NoteNewItems`도 같게 고칩니다. `diag_log_n_`과 `diag_present_` 두 개를 유지할 이유가 없어지므로, 이미 남긴 키 집합 하나만 남기고 나머지는 지웁니다.

이렇게 하면 `created_now` 자체는 손대지 않아도 됩니다. 항목의 생성과 소멸 로직은 그대로 두고 로그 게이트만 바꾸는 것이 안전합니다.

### 2-3. 로스터 줄을 더한다

줄 수를 세는 것보다 **한 줄에 정리해 주는 편**이 사용자에게 훨씬 낫습니다. 진단 창이 끝날 때 양쪽에서 한 줄씩 남깁니다.

```
intercept roster n=8 tips="KakaoTalk|Tailscale|Microsoft Teams|..."
uia roster n=12 tips="숨겨진 아이콘 표시|KakaoTalk|..."
```

- 툴팁은 `|`로 잇습니다. 빈 툴팁은 `(no tip)`으로 적습니다.
- 전체 길이가 1000자를 넘으면 자르고 끝에 `...`을 붙입니다.
- `n`은 자른 것과 무관하게 실제 개수입니다.
- 두 줄은 1-1절의 진단 창이 닫히는 시점에 연달아 남깁니다. 순서는 `intercept` 다음 `uia`입니다.

가로채기 쪽 로스터는 백엔드 안의 현재 목록에서 만들면 됩니다. `Enumerate`로 복사본을 얻어 쓰는 편이 잠금 범위를 넓히지 않으므로 낫습니다.

---

## 3. 툴팁의 줄바꿈이 로그 줄을 깨뜨린다

로그에 이렇게 남았습니다.

```
uia item tip="네트워크 IMTS5G
uia item tip="시계 오전 10:32:34
```

닫는 따옴표가 없습니다. 툴팁에 개행이 들어 있어서 한 줄이 두 줄로 갈라졌습니다. 이 상태로는 줄을 세는 것도 대조하는 것도 불가능합니다.

**로그에 넣기 전에 툴팁을 정제하십시오.**

- `\r`과 `\n`과 `\t`를 공백 하나로 바꿉니다.
- 연속된 공백은 하나로 줄입니다.
- 앞뒤 공백을 없앱니다.
- 128자를 넘으면 자릅니다.

가로채기와 UIA 양쪽, 그리고 2-3절의 로스터 모두에 같은 정제를 적용합니다. 헬퍼 하나를 만들어 쓰되, 두 파일이 서로를 포함하지 않으므로 각자 두거나 이미 있는 공용 위치에 두십시오. 새 공용 헤더를 만들지는 마십시오.

`TipFrom`이 이미 길이를 자르고 있다면 개행 처리만 더하면 됩니다. 기존 함수를 고칠지 로그 직전에 한 번 더 정제할지는 판단해서 정하고, 어느 쪽을 골랐는지 보고하십시오. 다만 **`TipFrom`의 결과가 화면에 그려지는 툴팁에도 쓰인다면 그쪽 동작을 바꾸지 마십시오.** 그때는 로그 직전 정제로 가십시오.

---

## 4. 자동 시작 토글이 먹통이 될 수 있다

### 4-1. 무엇이 문제인가

`src/autostart.cpp`의 `SetAutostart(true)`가 `Run` 값만 씁니다. `StartupApproved\Run`에 비활성 값이 남아 있으면 `AutostartEnabled()`는 계속 `false`를 돌려줍니다.

사용자가 작업 관리자에서 bamti의 시작 항목을 끄면 이 상태가 됩니다. 그다음부터는 메뉴의 체크가 꺼진 채로 아무리 눌러도 켜지지 않습니다. 값은 매번 다시 쓰이는데 판정이 바뀌지 않기 때문입니다. 사용자 입장에서는 메뉴가 고장 난 것으로 보입니다.

**이것도 선행 지시서 3-1절이 잘못 적은 탓입니다.** "작업 관리자에서 끈 것을 우리가 되돌리면 안 된다"고 썼는데, 그 원칙은 **우리가 임의로 되돌리는 경우**에나 맞습니다. 사용자가 메뉴를 직접 누른 것은 명시적인 요청이므로 되돌리는 것이 옳습니다.

같은 저장소의 `dock.cpp`에 `DeleteApprovedValue`가 이미 있고, 독 앱의 로그인 항목 토글이 정확히 그렇게 하고 있습니다. 지금 코드는 저장소 관례에도 어긋납니다.

### 4-2. 어떻게 고치는가

`SetAutostart(true)`가 `Run` 값을 쓴 뒤에 `StartupApproved\Run`의 `bamti` 값을 지우게 하십시오.

- 지우는 대상은 **값 이름이 정확히 `bamti`인 것 하나뿐**입니다. `dock.cpp`의 `IsBamtiDockRunName` 같은 방어를 같은 취지로 두십시오. 다른 값을 건드리면 안 됩니다.
- 키가 없거나 값이 없으면 성공으로 봅니다.
- 지우기에 실패해도 `SetAutostart`는 `true`를 돌려줍니다. `Run` 값 쓰기가 본체이고 이쪽은 보조입니다. 실패는 로그로만 남깁니다.
- `SetAutostart(false)`에서는 `StartupApproved`를 건드리지 마십시오. `Run` 값이 사라지면 판정이 어차피 `false`가 됩니다.

`dock.cpp`의 구현을 참고하되 코드를 복사하지 말고, `autostart.cpp`의 기존 서술 방식에 맞춰 쓰십시오.

---

## 5. 문서 오염을 되돌린다

`TASK-TRAY-MIRROR.md`에 더한 2026-08-31 문단이 **표 중간에 들어가 표를 둘로 갈랐습니다.** 그 문단을 표가 끝난 뒤로 옮기십시오.

같은 편집에서 표 안의 `줄어든 수`를 `줄어난 수`로 바꿨습니다. 원래 표기가 맞으므로 `줄어든 수`로 되돌리십시오. 이번 작업과 관계없는 변경입니다.

---

## 6. 하지 않을 것

다음은 이번 범위가 아닙니다. 착수하지 마십시오.

- `WaitForPriority`가 메시지 루프를 최대 500ms 막는 문제. 실측이 16ms였고, 지금 손대면 우선순위 확보라는 본래 목적이 흔들립니다. 관찰만 합니다.
- explorer가 우선순위를 반복해서 가져가는 문제(4분에 11회). fast 구간이 길어지는 것은 사실이지만, 1절을 고쳐 유휴 비용을 걷어낸 뒤에 다시 재는 것이 순서입니다.
- UIA와 가로채기의 하이브리드 병합. 재부팅 측정 뒤에 정합니다.
- 선행 지시서 11절의 금지 사항은 전부 그대로 유효합니다.

---

## 7. 관찰 하나를 문서에 남긴다

로그에서 이런 줄이 나왔습니다.

```
intercept item tip="배터리 수준" exe=explorer.exe hwnd=0x201F4 uid=1225 guid=1
intercept item tip="오디오 서비스가 실행되고 있지 않습니다." exe=explorer.exe hwnd=0x20820 uid=100 guid=1
intercept item tip="Bluetooth 장치" exe=explorer.exe hwnd=0x3026C uid=0 guid=1
```

`RESEARCH-TRAY-REREGISTER.md` 2절은 시스템 아이콘이 explorer 내부의 XAML 요소이므로 가로채기로 받을 수 없다고 단정했습니다. 그런데 배터리와 오디오와 블루투스가 `Shell_NotifyIcon` 경로로 들어왔습니다.

다만 툴팁이 UIA 쪽과 다릅니다. 가로채기는 `배터리 수준`인데 UIA는 `힘 배터리 상태: 완전히 충전됨 100%`입니다. 화면에 보이는 XAML 아이콘과는 **별개인 숨은 레거시 아이콘**일 가능성이 큽니다.

**결론을 내리지 말고 관찰만 적으십시오.** `RESEARCH-TRAY-REREGISTER.md` 2절에 짧은 단서를 다는 정도로 충분합니다. 이 아이콘들이 화면의 시스템 아이콘과 같은 것인지는 재부팅 측정 때 로스터 두 줄을 대조하면 드러납니다.

---

## 8. 검증

| 확인 항목 | 방법 |
|---|---|
| 빌드 | Release. 경고가 늘지 않아야 합니다 |
| 진단 창 종료 | bamti를 띄우고 3분 넘게 둡니다. `roster` 두 줄이 나온 뒤 `uia item`이 더 이상 나오지 않아야 합니다 |
| 유휴 비용 | 진단 창이 닫힌 뒤 bamti의 CPU를 5분 동안 재서 보고하십시오. 0.2% 안이어야 합니다 |
| 중복 제거 | explorer를 재시작하고 같은 앱의 `intercept item`이 한 줄만 나오는지 봅니다 |
| 줄바꿈 정제 | `tip="` 뒤에 닫는 따옴표가 있는지 봅니다. 갈라진 줄이 없어야 합니다 |
| 자동 시작 복구 | 8-1절 |

### 8-1. 자동 시작 복구 검증

이 순서를 그대로 밟으십시오.

1. 메뉴에서 `로그인 시 bamti 시작`을 켭니다.
2. `reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\StartupApproved\Run" /v bamti /t REG_BINARY /d 03000000000000000000000000000000 /f`로 비활성 상태를 만듭니다. 작업 관리자에서 끈 것과 같은 상태입니다.
3. 메뉴를 열어 체크가 꺼져 있는지 봅니다.
4. 메뉴를 눌러 켭니다.
5. 메뉴를 다시 열어 **체크가 켜져 있는지** 봅니다. 이것이 이번 수정의 핵심입니다.
6. `reg query`로 `StartupApproved\Run`의 `bamti` 값이 사라졌는지 확인합니다.
7. 검증이 끝나면 메뉴에서 다시 꺼서 `Run` 값을 지우고 마칩니다.

### 8-2. 사용자가 할 것

재부팅 측정 절차는 선행 지시서 9-3절 그대로입니다. 다만 이번 수정으로 **줄을 셀 필요가 없어졌습니다.** 절차를 이렇게 갱신해서 보고하십시오.

로그에서 `roster` 두 줄을 찾아 `n=` 값을 비교하고, `uia roster`에만 있고 `intercept roster`에는 없는 이름이 곧 못 받은 앱입니다.

---

## 9. 보고

- 절별로 고친 파일과 함수
- 3절에서 `TipFrom`을 고쳤는지 로그 직전 정제로 갔는지, 그 이유
- 8절 검증의 실제 결과. 로그 줄과 CPU 측정값을 그대로 붙이십시오
- 8-2절 절차를 사용자가 밟을 수 있게 정리한 문단
- 이 지시서와 다르게 판단한 부분이 있으면 그 이유

작업 트리는 커밋하지 말고 그대로 두십시오. 검수 후에 정리합니다.
