# 작업 지시서: 트레이 미러 검수 결함

`TASK-TRAY-MIRROR.md`로 구현한 5단계를 검수해서 나온 결함 넷을 고칩니다. 첫 번째가 가장 중요하고, 나머지 셋은 조건이 맞을 때만 드러나는 잠복 결함입니다.

검수에 쓴 커밋 범위는 `e21b78a..4cf8456`입니다. Debug 클린 빌드는 `/W4` 경고 없이 통과했고, 상주 로그에서 UIA 열거가 15~63ms로 동작하며 서드파티 아이콘 네 개가 미러되고 RuntimeId가 `stable=1 churn=0`으로 확인되었습니다. 즉 열거 경로 자체는 건전합니다.

---

## 1. 알림 영역 잠시 표시가 곧바로 취소된다

### 증상

미러 아이콘을 우클릭해 "알림 영역 잠시 표시"를 골라도 태스크바가 10초를 채우지 못하고 다시 숨습니다. 마우스를 알림 영역 쪽으로 옮기면 그 순간 사라집니다.

### 근본 원인

`MenuBar::StartTrayPeek`이 `taskbar_.Restore()`로 태스크바를 되돌리지만, 다른 세 곳이 그 상태를 모른 채 다시 숨깁니다. 셋 다 정적 함수 `TaskbarController::Rehide()`를 부르고, 이 함수는 인스턴스의 `hidden_`도 임시 표시 여부도 보지 않습니다.

| 위치 | 언제 부르는가 | 임시 표시에 미치는 영향 |
|---|---|---|
| `dock.cpp:1322` | `kTrayWatchTimerId` 타이머, `kTrayWatchMs = 5000` | 늦어도 5초 안에 다시 숨습니다. |
| `dock.cpp:1982` | `Dock::ShowPill()` | 알림 영역은 화면 아래에 있으므로, 그리로 마우스를 옮기면 독이 뜨면서 즉시 숨습니다. |
| `dock.cpp:1242` | `Dock::TrayWinEventProc` | `TraySuppressGuard`의 200ms가 지난 뒤 트레이 이벤트가 하나만 와도 숨습니다. |

두 번째 경로 때문에 사실상 사용할 수 없습니다. 사용자가 알림 영역에 도달하려면 마우스를 화면 아래로 내려야 하는데, 그 동작 자체가 독을 띄우고 `ShowPill`을 부릅니다.

이 기능은 UIA 백엔드에서 앱의 우클릭 메뉴에 도달하는 **유일한 경로**이므로, 동작하지 않으면 5단계의 완료 조건 하나가 비어 있는 것과 같습니다.

### 고치는 방법

`TaskbarController`에 임시 표시 상태를 정적으로 두고, 재숨김 경로가 그것을 존중하게 합니다. `SuppressingTrayEvents()`가 이미 같은 모양의 정적 억제 장치이므로 그 옆에 나란히 둡니다.

```cpp
// taskbar_controller.hpp
static void BeginPeek(UINT ms);   // 임시 표시를 시작하고 만료 시각을 기록한다
static void EndPeek();
static bool Peeking();            // 만료 시각이 지나면 저절로 거짓이 된다
```

- `Rehide()`는 `Peeking()`이 참이면 아무것도 하지 않고 거짓을 돌려줍니다.
- `EnsureHidden()`도 같습니다.
- 만료 시각을 함께 두는 이유는 `EndPeek()`을 부를 주체가 죽거나 타이머를 놓쳐도 플래그가 영원히 남지 않게 하기 위해서입니다. `MenuBar`의 10초 타이머와 별개로 `BeginPeek`에 준 시한이 지나면 `Peeking()`이 저절로 거짓이 됩니다. 시한은 `MenuBar`의 타이머보다 2초 길게 주십시오.

`MenuBar::StartTrayPeek`은 `taskbar_.Restore()` **앞에** `TaskbarController::BeginPeek(kPeekMs + 2000)`을 부르고, `EndTrayPeek`은 `taskbar_.Hide()` **뒤에** `TaskbarController::EndPeek()`을 부릅니다. 순서를 지키지 않으면 `Restore()`와 `Hide()`가 자기 자신의 억제에 걸립니다.

### 검증

- [ ] 미러 아이콘을 우클릭해 "알림 영역 잠시 표시"를 고르고, 마우스를 알림 영역으로 옮겨 아이콘을 우클릭해 앱의 컨텍스트 메뉴를 띄우고, 항목 하나를 실행할 수 있습니다.
- [ ] 그 10초 동안 독을 띄웠다 내려도 태스크바가 사라지지 않습니다.
- [ ] 10초가 지나면 태스크바가 다시 숨고, 그 뒤에는 `Rehide()`가 평소대로 동작합니다.
- [ ] 임시 표시 중에 bamti를 강제 종료한 뒤 다시 실행해도 태스크바가 정상으로 숨습니다.

---

## 2. 폴백 key 모드에서 아이콘 숨기기가 듣지 않는다

### 증상

`use_runtime_id_`가 거짓으로 떨어진 환경에서 "이 아이콘 숨기기"를 골라도 아이콘이 계속 나타납니다. 지금 이 컴퓨터는 `stable=1`이므로 발현하지 않지만, RuntimeId가 흔들리는 환경에서는 바로 드러납니다.

### 근본 원인

`TrayMirror::DoRound`가 `Include()`를 먼저 부르고, 그 뒤에 폴백 key를 계산합니다.

```cpp
if (!Include(icon, overflow, settings)) {   // 여기서 KeyHidden(icon.key, ...)를 본다
  continue;
}
TrayIconInfo copy = icon;
if (!use_runtime) {
  copy.key = 0;                              // 여기서 비로소 key가 바뀐다
}
if (copy.key == 0) {
  copy.key = /* automation_id + class + order 해시 */;
}
```

설정에 저장된 숨김 key는 `Publish` 시점의 key, 즉 폴백 해시입니다. 그런데 `Include`가 검사하는 것은 폴백 계산 전의 `icon.key`입니다. 폴백 모드에서는 두 값이 서로 다르므로 비교가 언제나 어긋납니다.

### 고치는 방법

key를 확정한 뒤에 `Include`를 부르십시오. 순서만 바꾸면 됩니다. `Include`가 받는 것은 key가 이미 채워진 `copy`여야 합니다.

`OverflowOrder`도 같은 목록을 보고 있으므로, key 확정과 필터링을 한 번의 순회로 정리하는 편이 읽기 쉽습니다. 다만 이 절의 요구는 순서 교정 하나이고, 그 이상의 재구성은 하지 마십시오.

### 검증

- [ ] `use_runtime_id_`를 강제로 거짓으로 만든 임시 빌드에서 "이 아이콘 숨기기"가 동작하고, 다시 실행해도 숨어 있습니다.
- [ ] RuntimeId 모드에서도 기존 동작이 그대로입니다.

---

## 3. 오버플로 단추 판정이 정상 아이콘을 버릴 수 있다

### 증상

`tray_system_icons`를 켠 상태에서, 알림 영역에 숨긴 아이콘이 하나도 없어 "숨겨진 아이콘 표시" 단추가 나타나지 않으면, 가장 왼쪽 시스템 아이콘(예: 입력 표시기)이 미러에서 사라집니다.

### 근본 원인

`OverflowOrder`가 첫 번째 `system_icon`을 무조건 오버플로 단추로 봅니다.

```cpp
int OverflowOrder(const std::vector<TrayIconInfo>& icons) {
  for (const TrayIconInfo& icon : icons) {
    if (icon.system_icon) {
      return icon.order;
    }
  }
  return -1;
}
```

`TASK-TRAY-MIRROR.md` 5-1절이 요구한 조건은 셋이었습니다. 가장 왼쪽일 것, `AutomationId`가 `SystemTrayIcon`일 것, **자식 `Image`가 없을 것**입니다. 세 번째 조건이 빠졌습니다. `TrayIconInfo::has_image_child` 필드는 선언되어 있는데 아무도 쓰지 않습니다.

탐침 결과를 보면 오버플로 단추와 입력 표시기는 `ClassName`이 둘 다 `SystemTray.NormalButton`이고, 갈리는 것은 자식 `Image`의 유무뿐입니다. 오버플로 단추에는 자식 `Image`가 없고 입력 표시기에는 있습니다.

### 고치는 방법

두 가지 가운데 하나를 고르되, 무엇을 골랐는지 코드 주석 한 줄로 남기십시오.

1. **권장.** UIA 백엔드가 `has_image_child`를 실제로 채우고, `OverflowOrder`가 `icon.system_icon && !icon.has_image_child`인 첫 항목만 오버플로로 봅니다. 자식 조회가 회차마다 왕복을 늘리지 않도록, 캐시 요청의 `TreeFilter`에 `Image` 컨트롤 타입 조건을 걸고 `GetCachedChildren`으로 읽으십시오.
2. 자식 조회 비용이 실측으로 무겁게 나오면, `has_image_child`를 쓰지 말고 대신 **`BoundingRectangle`의 폭**으로 가릅니다. 탐침에서 오버플로 단추는 48픽셀이고 다른 시스템 아이콘도 48픽셀이므로 이 대안은 이 컴퓨터에서 성립하지 않습니다. 그러므로 1번이 실패했을 때만 검토하고, 그때는 판정을 포기하고 오버플로 단추를 미러에 남기십시오. **정상 아이콘을 버리는 것보다 쓸모없는 아이콘 하나가 남는 편이 낫습니다.**

`has_image_child`를 끝내 채우지 못하면 필드를 지우지 말고, 채우지 못한다는 주석을 달아 두십시오. 다음 사람이 같은 자리를 다시 파는 것을 막습니다.

### 검증

- [ ] `tray_system_icons`를 켠 상태에서 오버플로 단추는 미러되지 않고 입력 표시기와 볼륨은 미러됩니다.
- [ ] 알림 영역의 모든 아이콘을 항상 표시하도록 Windows 설정을 바꾸어 오버플로 단추를 없앤 뒤에도, 입력 표시기가 미러에 남아 있습니다.
- [ ] 열거 소요 시간이 이 변경 전후로 10ms 이상 늘지 않습니다. 로그로 비교합니다.

---

## 4. 작업자 스레드가 메시지 펌프 없는 STA다

### 증상

지금은 드러나지 않습니다. 상주 로그에서 열거가 15~63ms로 정상 동작합니다. 다만 explorer가 느려지거나 UIA가 콜백을 되돌려 보내야 하는 상황에서 지연이나 교착으로 나타날 수 있는 구조입니다.

### 근본 원인

`TrayMirror::WorkerLoop`가 `CoInitializeEx(nullptr, COINIT_APARTMENTTHREADED)`로 STA를 열고, 그 뒤 `WaitForMultipleObjects`로만 대기합니다. STA 스레드는 자기 메시지 큐를 펌프해야 COM이 호출을 마샬링할 수 있습니다. 펌프하지 않는 STA에서 대기 함수로 블로킹하는 것은 COM이 경고하는 전형적인 패턴입니다.

`TASK-TRAY-MIRROR.md` 3-2절은 MTA를 지정했는데, 커밋 `6e78f05`이 STA로 바꾸었습니다. 커밋 메시지에 따르면 MTA에서는 주차된 XAML 알림 영역 버튼이 열거되지 않았다고 합니다. **그 실측 판단 자체는 존중합니다.** MTA로 되돌리지 마십시오. 고쳐야 할 것은 아파트먼트가 아니라 대기 방식입니다.

### 고치는 방법

대기를 `MsgWaitForMultipleObjectsEx`로 바꾸고, `QS_ALLINPUT`이 깨우면 큐를 비웁니다.

```cpp
const DWORD wait = MsgWaitForMultipleObjectsEx(n, waits, timeout, QS_ALLINPUT, MWMO_INPUTAVAILABLE);
if (wait == WAIT_OBJECT_0 + n) {
  MSG msg;
  while (PeekMessageW(&msg, nullptr, 0, 0, PM_REMOVE)) {
    TranslateMessage(&msg);
    DispatchMessageW(&msg);
  }
  continue;
}
```

이 스레드에는 창이 없으므로 펌프는 COM이 보내는 내부 메시지만 처리합니다. 비용은 사실상 없습니다.

`6e78f05`이 함께 넣은 `FindAll` 폴백도 이 자리에서 함께 보십시오. 캐시 경로가 비었을 때 `GetCurrent*`로 다시 읽는 경로인데, 매 회차 크로스 프로세스 왕복을 만듭니다. 펌프를 넣은 뒤에도 폴백이 계속 쓰이는지 로그로 확인하고, 쓰이지 않으면 그대로 두되, 매 회차 쓰이고 있다면 캐시 요청이 왜 비는지 원인을 찾아 적어 주십시오. 폴백이 상시 경로가 되면 6-1절의 주기 예산이 무너집니다.

### 검증

- [ ] 열거 소요 시간이 이 변경 전후로 나빠지지 않습니다.
- [ ] explorer를 디버거로 멈춘 상태에서 상단바가 계속 그려지고 클릭에 반응하며, 미러 작업자 스레드가 교착하지 않습니다. `Log`가 계속 남는지로 확인합니다.
- [ ] 로그에 `uia FindAll fallback`이 매 회차 남지 않습니다.

---

## 5. 함께 정리할 것

### 5-1. 측정 보고서의 원인 설명

`PROBE-TRAY-PARKED.md`의 "적용 메모"는 `kParkedVisible` 실패의 원인을 "Win32 창 사각형만 화면 밖으로 가고, DWM/XAML 합성은 원래 가장자리에 남는다"고 적었습니다. 그런데 같은 보고서의 값이 이 설명과 맞지 않습니다.

| 절 | 값 |
|---|---|
| 3절, 4절 | `Shell_TrayWnd` rect가 `(0,32000,5120,32072)` |
| 5절 UIA | 아이콘 `BoundingRectangle`이 `y=2158` |
| 6절 캡처 | 두 대상 창의 rect가 `(0,2158,5120,2230)` |

즉 캡처 시점에는 대상 창이 원래 자리에 있었고, 그런데도 `non_black_pct`가 0.00이었습니다. 정상 상태 탐침(`PROBE-TRAY.md`)에서 같은 `TrayNotifyWnd`가 99.78%였던 것과 대비됩니다. 따라서 "화면 밖이라서 검다"는 설명은 이 측정으로 뒷받침되지 않습니다.

**결론(캡처 경로 포기)은 그대로 둡니다.** 어느 설명을 택하든 이 컴퓨터에서 픽셀을 얻지 못한다는 사실은 같습니다. 보고서의 원인 문단만 "원인은 확정하지 못했다. 관측된 것은 태스크바를 `SetWindowPos`로 옮긴 뒤에는 대상 창이 원위치로 돌아온 상태에서도 `PrintWindow`가 검정을 돌려준다는 사실이다"로 고치고, 관측값 세 줄을 근거로 함께 적으십시오. 추측을 사실처럼 적어 두면 6단계 이후에 이 문서를 근거로 잘못된 판단을 하게 됩니다.

### 5-2. RuntimeId 안정성 판정의 오탐

`DoRound`의 안정성 검사는 다섯 회차의 key 집합이 완전히 같을 때만 `stable=1`로 봅니다. 그 다섯 회차 사이에 트레이 앱이 하나만 뜨거나 꺼져도 `stable=0`이 되고, `use_runtime_id_`가 영구히 거짓이 됩니다. 실제 아이콘 변화와 RuntimeId 불안정을 구별하지 못합니다.

판정을 이렇게 바꾸십시오. 다섯 회차에 **연속으로 존재한 key만** 후보로 보고, 그 후보들이 회차마다 유지되었는지로 판정합니다. 회차 사이에 항목이 늘거나 준 것 자체는 불안정의 근거가 아닙니다. 판정에 쓸 표본이 두 개도 안 되면 판정을 미루고 다음 다섯 회차를 다시 모으십시오.

지금 이 컴퓨터는 `stable=1 churn=0`이므로 이 변경으로 동작이 달라지지 않습니다. 다른 환경을 위한 것입니다.

### 5-3. 범위를 넘은 문서 삭제

커밋 `4cf8456`이 `ARCHITECTURE.md`의 8.1절과 8.2절을 통째로 지웠습니다. 지운 내용이 v1 초안이고 `docs/STATUS-PROTOCOL.md`가 이미 대체하고 있으므로 **되돌리지 않습니다.** 다만 지시서가 요청한 것은 트레이 미러 절을 더하는 일이었습니다. 앞으로 문서에서 무언가를 지울 때는 지시서에 없더라도 지웠다는 사실과 이유를 커밋 메시지에 한 줄로 남겨 주십시오.

---

## 6. 하지 말아야 할 것

- MTA로 되돌리지 마십시오. STA 선택은 실측 근거가 있습니다.
- `Rehide()`를 부르는 `dock.cpp`의 네 자리를 각각 고치지 마십시오. 상태를 아는 주체는 `TaskbarController` 하나여야 합니다.
- 임시 표시 중에 마우스 위치를 추적하는 판정을 넣지 마십시오. 시한만으로 충분합니다.
- 캡처 경로를 다시 시도하지 마십시오. 이번 범위가 아닙니다.

---

## 7. 커밋

| 순서 | 작업 | 커밋 메시지 |
|---|---|---|
| 1 | `BeginPeek` / `EndPeek` / `Peeking`과 재숨김 억제, `MenuBar` 배선 | `fix: 알림 영역을 잠시 표시하는 동안 재숨김을 멈춘다` |
| 2 | key 확정과 필터 순서 교정 | `fix: 폴백 키 모드에서도 숨긴 아이콘이 숨는다` |
| 3 | 오버플로 단추 판정에 자식 Image 조건 추가 | `fix: 오버플로 단추만 골라 제외한다` |
| 4 | STA 메시지 펌프 | `fix: 미러 작업자 STA에서 메시지를 펌프한다` |
| 5 | 보고서 원인 문단과 안정성 판정, 문서 | `docs: 주차 캡처 실패의 관측과 추측을 구분한다` |

1번 커밋을 마친 시점에 빌드하고 실제로 띄워서, 미러 아이콘 우클릭으로 앱의 컨텍스트 메뉴까지 도달할 수 있는지 직접 확인한 뒤 2번으로 넘어가십시오. 이 경로가 동작하는 것이 5단계의 완료 조건이므로, 나머지 수정보다 먼저 확인되어야 합니다.
