# 작업 지시서: 상단바 그리기 비용의 원인 규명

2단계를 마친 뒤 측정한 값이 계획 문서 5절의 예산을 넘습니다. 이 작업은 그 원인을 **측정으로** 확정하고, 측정이 가리킨 곳만 고칩니다.

작업 순서를 뒤집지 마십시오. 고칠 곳을 먼저 정해 놓고 측정으로 뒷받침하는 방식은 금지합니다. 후보를 여러 개 한꺼번에 고쳐서 좋아졌는지 보는 방식도 금지합니다. 무엇이 얼마나 기여했는지 알 수 없게 되기 때문입니다.

---

## 0. 현재 측정값

```
[perf] bar full=2.8ms seg=2.2ms compute=0.2ms segments=2 overflow=0
```

| 항목 | 측정값 | 예산 | 판정 |
|---|---|---|---|
| 세그먼트 하나 다시 그리기 | 2.2ms | 0.3ms 미만 | **예산의 7배** |
| 전체 다시 그리기 | 2.8ms | 2.0ms 미만 | 초과 |
| 레이아웃 계산 | 0.2ms | 해당 없음 | 문제 없음 |

`compute`가 예산 안이므로 `BarLayout::Compute`는 용의선상에서 제외합니다. 문제는 318x48 픽셀 영역을 그리는 데 2.2ms가 든다는 것입니다. 이 크기의 텍스트 두 줄을 그리는 비용으로는 지나치게 큽니다.

지금 당장 체감되는 문제는 아닙니다. 초당 한 번 2.2ms면 코어 하나의 0.2% 수준입니다. 이 작업을 지금 하는 이유는 3단계부터 트레이 아이콘과 사용량 항목이 얹히면서 다시 그리는 빈도와 세그먼트 수가 함께 늘기 때문입니다. **원인을 모르는 2ms 위에 그리기 작업을 더 쌓지 않으려는 것입니다.**

---

## 1. 먼저 고칠 것: 측정값이 끈적합니다

`menu_bar.cpp`의 `NotePerf`가 종류별 마지막 값을 그대로 들고 있습니다. 전체 다시 그리기는 드물게 일어나므로, 프로그램이 켜진 직후의 콜드 프레임 값이 이후 로그 줄에 계속 따라붙습니다. 실제로 로그에 `full=27.3ms`가 여러 줄 반복되었는데 그것은 최초 한 프레임의 값이었습니다.

측정을 고치는 것이 이 작업의 첫 커밋입니다. 측정을 믿을 수 없으면 나머지가 전부 무의미합니다.

- 100프레임 창 안에서 종류별로 **표본 수, 평균, 최댓값**을 모으고, 창이 끝나면 로그로 남긴 뒤 **0으로 초기화**합니다.
- 표본이 하나도 없는 종류는 로그에서 `-`로 적습니다. 이전 창의 값을 끌고 오지 마십시오.
- 콜드 프레임을 구분할 수 있도록 첫 창은 `cold` 표시를 붙입니다.

```
perf bar full[n=1 avg=27.3 max=27.3]ms seg[n=99 avg=2.2 max=3.1]ms compute[n=100 avg=0.2 max=0.4]ms segments=2 cold
perf bar full[n=0]ms seg[n=100 avg=2.1 max=2.6]ms compute[n=100 avg=0.2 max=0.3]ms segments=2
```

---

## 2. 구간 측정

`ClockRenderer::Draw`를 다섯 구간으로 나누어 각각 `QueryPerformanceCounter`로 잽니다. 임시 계측이 아니라 이 작업이 끝난 뒤에도 남길 코드이므로, 1절과 같은 창 통계 방식으로 모읍니다.

| 구간 | 대상 |
|---|---|
| `bind` | `rt_->BindDC(hdc, &dirty)` |
| `brush` | `rt_->CreateSolidColorBrush(...)` |
| `begin` | `BeginDraw` + `Clear` + `SetTransform` |
| `draw` | 세그먼트 순회 전체. `LayoutFor`, `GetMetrics`, `DrawTextLayout` 포함 |
| `end` | `rt_->EndDraw()` |

`MenuBar::Paint`에서도 두 구간을 더 잽니다.

| 구간 | 대상 |
|---|---|
| `bpbegin` | `BeginBufferedPaint` + `BufferedPaintClear` |
| `bpend` | `EndBufferedPaint` |

로그는 다음 형태로 100프레임마다 한 줄이면 충분합니다.

```
perf draw bind=0.05 brush=0.01 begin=0.10 draw=0.30 end=1.70 bpbegin=0.05 bpend=0.10 (ms, avg)
```

**이 로그를 얻는 것이 이 작업의 중간 산출물입니다.** 로그 한 줄을 확보한 뒤에 3절로 넘어가십시오. 그 전에는 어떤 최적화도 하지 마십시오.

---

## 3. 가설과 대응

2절의 로그가 어느 구간을 가리키는지에 따라 대응이 달라집니다. 아래는 가설이며, **측정이 가리키지 않은 항목은 손대지 마십시오.**

### 가설 A. `end`가 지배한다

가장 유력한 후보입니다. 지금 렌더 타깃은 `D2D1_RENDER_TARGET_TYPE_DEFAULT`로 만든 `ID2D1DCRenderTarget`입니다. `DEFAULT`는 가능하면 하드웨어를 고르는데, DC 렌더 타깃은 결과를 시스템 메모리의 DC로 되돌려야 하므로 `EndDraw`에서 GPU 읽기 대기가 발생할 수 있습니다. 이 영역은 318x48로 아주 작아서 GPU를 쓸 이유가 없습니다.

대응은 렌더 타깃 종류를 `D2D1_RENDER_TARGET_TYPE_SOFTWARE`로 바꾸는 것입니다. 한 줄 수정이며, 바꾸기 전과 후의 `end` 값을 같은 조건에서 각각 로그로 남겨 비교하십시오.

주의할 점이 있습니다. 소프트웨어 렌더 타깃은 텍스트 렌더링 품질이 달라질 수 있습니다. 바꾼 뒤 화면을 확대해 글자 가장자리를 확인하고, 다르면 그 사실을 보고하십시오. 품질이 눈에 띄게 나빠지면 성능보다 품질을 우선합니다.

### 가설 B. `bind`가 지배한다

`BindDC`를 프레임마다 부르는 비용입니다. 이 경우 근본 대응은 DC 렌더 타깃을 버리고 창에 붙는 렌더 타깃을 쓰는 것인데, 그것은 버퍼드 페인트 구조 전체를 바꾸는 큰 작업입니다. **이 작업에서는 하지 마십시오.** 측정값과 함께 그 사실만 보고하고, 계획 문서 5절에 후속 과제로 적어 두십시오.

### 가설 C. `brush`가 지배한다

프레임마다 솔리드 브러시를 새로 만드는 비용입니다. 대응은 브러시를 `ClockRenderer`의 멤버로 캐시하고, `rt_`를 새로 만들 때만 함께 다시 만드는 것입니다. `rt_.Reset()`을 하는 모든 자리에서 브러시도 함께 버려야 합니다. 빠뜨리면 파괴된 타깃에 속한 브러시를 쓰게 됩니다.

### 가설 D. `draw`가 지배한다

`DrawTextLayout`에 넘기는 `D2D1_DRAW_TEXT_OPTIONS_ENABLE_COLOR_FONT`가 의심 대상입니다. 상단바 텍스트는 시계와 상태 문자열이며, 색 이모지가 실제로 필요한 곳은 상태 항목의 글리프뿐입니다. 대응은 세그먼트 종류에 따라 옵션을 나누는 것입니다. 시계와 경고 문구는 `D2D1_DRAW_TEXT_OPTIONS_NONE`으로 그립니다.

이 경우에도 바꾸기 전후를 각각 측정하고, 상태 항목의 이모지가 여전히 색으로 나오는지 눈으로 확인하십시오.

### 가설 E. `bpbegin` 또는 `bpend`가 지배한다

버퍼드 페인트가 프레임마다 버퍼를 할당하고 되돌리는 비용입니다. 대응은 자체 DIB를 만들어 재사용하는 것인데, 이것도 구조 변경에 가깝습니다. **이 작업에서는 하지 마십시오.** 보고만 하십시오.

---

## 4. 예산을 못 맞추면

가설 A, C, D에 해당하는 대응을 하고도 세그먼트 그리기가 0.3ms를 넘을 수 있습니다. 그때는 다음을 하십시오.

1. 도달한 최솟값과 그때의 구간별 분해를 기록합니다.
2. 남은 비용의 구조적 원인(가설 B, E 등)을 한 문단으로 적습니다.
3. 계획 문서 5절의 예산 표에서 해당 줄을 **측정으로 확인된 값에 맞춰 고치고**, 원래 목표와 그 목표에 도달하려면 무엇을 바꿔야 하는지를 각주로 남깁니다.

**예산을 못 맞춘 것을 숨기지 마십시오.** 지키지 못할 숫자를 표에 남겨 두면 이후 단계에서 그 표를 아무도 믿지 않게 됩니다. 근거 있는 숫자로 고치는 편이 낫습니다.

---

## 5. 하지 말아야 할 것

1. **측정 없이 고치지 않습니다.** 2절의 로그를 확보하기 전에는 최적화 코드를 한 줄도 쓰지 마십시오.
2. **여러 후보를 한 커밋에서 고치지 않습니다.** 각 대응은 별도 커밋이고, 커밋 메시지에 바꾸기 전후 측정값을 적습니다.
3. **렌더 타깃 구조를 바꾸지 않습니다.** 가설 B와 E의 근본 대응은 이 작업의 범위 밖입니다.
4. **모양을 바꾸지 않습니다.** 글꼴, 간격, 색은 그대로입니다. 가설 A와 D는 렌더링 품질에 영향을 줄 수 있으므로 반드시 눈으로 확인하십시오.
5. **예산을 맞추려고 측정 방법을 바꾸지 않습니다.** 표본에서 느린 프레임을 빼거나, 창을 늘려 평균을 낮추는 식의 조정을 하지 마십시오.
6. **2단계에서 만든 세그먼트 모델과 무효화 논리를 건드리지 않습니다.** 이 작업은 그리기 비용만 다룹니다.

---

## 6. 검증

- [ ] `/W4` 경고 없이 Debug와 Release가 빌드됩니다.
- [ ] 1절의 창 통계 로그가 나오고, 표본이 없는 종류가 `-`로 나옵니다. 이전 창의 값이 따라붙지 않습니다.
- [ ] 2절의 구간 분해 로그가 나옵니다.
- [ ] 각 대응마다 바꾸기 전후 측정값이 커밋 메시지에 있습니다.
- [ ] 다크와 라이트 테마에서 글자 모양과 가장자리가 작업 전과 같습니다.
- [ ] 상태 항목에 이모지 글리프를 넣었을 때 색이 유지됩니다(가설 D에 대응한 경우).
- [ ] 최종 측정값이 계획 문서 5절의 표와 일치합니다. 예산을 고쳤다면 각주가 함께 있습니다.

---

## 7. 커밋

| 순서 | 작업 | 커밋 메시지 |
|---|---|---|
| 1 | 창 통계 방식으로 측정 교정 | `fix: 상단바 성능 로그가 이전 창의 값을 물고 가지 않게 한다` |
| 2 | 구간 측정 추가 | `chore: 상단바 그리기를 구간별로 계측한다` |
| 3 이후 | 측정이 가리킨 대응마다 한 커밋 | `perf: <무엇을 바꿔서 어느 구간이 얼마나 줄었는지>` |
| 마지막 | 계획 문서 갱신 | `docs: 상단바 그리기 예산을 측정값으로 고친다` |

3번 이후의 커밋 개수는 측정 결과에 달려 있습니다. 고칠 것이 없다면 없어도 됩니다. 그 경우 4절대로 보고하고 문서만 고치십시오.
