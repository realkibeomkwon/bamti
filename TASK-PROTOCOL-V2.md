# 작업 지시서: Status Item 프로토콜 v2와 공급자 레지스트리

`PLAN-TRAY-TO-TOPBAR.md`의 3단계를 구현하는 지시서입니다.

이 단계가 프로젝트 전체의 중심입니다. 앞으로 상단바에 올라올 것들, 곧 LLM 사용량과 트레이 미러와 내장 위젯과 비트코인 시세가 **전부 이 프로토콜 하나를 통과**합니다. 여기서 규격을 느슨하게 잡으면 이후 단계마다 예외 경로가 생기고, 5절의 성능 예산도 함께 무너집니다.

프로토콜 명세 본문은 계획 문서 3절에 이미 있습니다. 이 지시서는 그 명세를 코드로 옮기는 방법과 판정 기준만 다룹니다. **명세와 이 지시서가 어긋나면 계획 문서 3절이 우선입니다.**

---

## 0. 중간 점검 지점

이 작업은 커밋 여섯 개로 나뉩니다. **3번 커밋을 마친 뒤 멈추고 보고해 주십시오.** 계속하라는 지시를 받은 뒤에 4번으로 넘어갑니다.

이유는 단순합니다. 1번부터 3번까지는 모델과 파서와 아이콘 경로이고, 4번부터 6번까지는 그 위에 얹는 계층입니다. 파서가 잘못된 채로 패널 렌더링까지 진행하면 어디가 원인인지 가릴 수 없습니다.

| 커밋 | 내용 | 절 |
|---|---|---|
| 1 | v2 모델과 v1 변환 | 2 |
| 2 | v2 파서, hello, patch, 상한 | 3 |
| 3 | 아이콘 캐시 추출과 바 아이콘 그리기 | 4 |
| **점검** | **여기서 멈추고 보고합니다** | |
| 4 | 공급자 레지스트리 | 5 |
| 5 | 패널 행 렌더링과 토글 | 6 |
| 6 | 명세 문서와 예제 | 7 |

---

## 1. 지금 코드에서 확인된 것

다시 조사하지 않아도 됩니다.

| 항목 | 사실 |
|---|---|
| JSON 파서 | `json_line.hpp`에 `GetString`, `GetInt`, `GetUint32`, `GetDouble`, `GetRaw`, `ForEachField`, `ForEachArray`가 이미 있습니다. **새 파서를 쓰지 마십시오.** |
| 문자열 변환 | `pipe_server.cpp` 익명 영역의 `Utf8ToWide`, `WideToUtf8`, `TruncateWide`, `TruncateLabel`, `ValidId` |
| 패널 내용 | `menu_bar.cpp` 익명 영역의 `StatusPanelContent`. 게이지와 액션 목록만 그립니다 |
| 팝업 인터페이스 | `popup_surface.hpp`의 `PopupContent`. `Measure`, `Render`, `HitTest`, `Invoke`, `RowCount`, `StickyRow` |
| 아이콘 변환 | `dock.cpp`의 `WicFactory()`, `BgraImage`, `BitmapFromIcon()`, `RenderLayered()` 안의 `d2d_icons_` 캐시 |
| 바 레이아웃 | `bar_layout.{hpp,cpp}`의 `BarLayout::Compute`. 세그먼트 사각형을 여기서 정합니다 |
| 이벤트 송신 | `pipe_server.cpp`의 `SendClick(id, button)`. 이것을 일반화합니다 |

---

## 2. 커밋 1: v2 모델과 v1 변환

### 2-1. status_item.hpp

계획 문서 3-1절의 구조체를 그대로 씁니다. 요점만 옮기면 다음과 같습니다.

```cpp
enum class StatusState { kNormal, kWarn, kError, kOn, kOff, kBusy };
enum class IconKind { kNone, kGlyph, kPng, kFile, kHicon };
enum class RowType { kGauge, kKeyValue, kText, kSeparator, kToggle, kButton };
```

`StatusIcon`, `StatusRow`, `StatusPanel`, `StatusItem`의 필드는 계획 문서 3-1절을 그대로 따르되, 두 가지를 추가합니다.

```cpp
struct StatusItem {
  // ... 계획 문서 3-1절의 필드 ...
  std::string source;      // 공급자 이름. 커밋 4에서 쓴다. 지금은 "pipe"로 채운다
  uint64_t revision = 0;   // 갱신될 때마다 1씩 올린다. 커밋 5의 토글 확인에 쓴다
};
```

`StatusIcon::cache_key`는 내용 해시입니다. FNV-1a 64비트로 `kind` 한 바이트와 내용 바이트열을 이어서 계산하십시오. `kGlyph`는 글리프의 UTF-8 바이트, `kPng`는 데이터 바이트, `kFile`은 경로의 UTF-8 바이트, `kHicon`은 핸들 값입니다. 이 값이 같으면 같은 아이콘으로 취급합니다.

**기존 `StatusGauge`와 `StatusPanel::actions`는 지웁니다.** 모델을 둘 유지하지 마십시오. v1 입력은 파서에서 v2 모델로 바꿉니다.

### 2-2. v1을 v2로 바꾸는 규칙

기존 클라이언트가 수정 없이 그대로 동작해야 합니다. `examples/status_push.py`의 현재 예제가 회귀 시험입니다.

| v1 필드 | v2 대응 |
|---|---|
| `text` | `text`. 상한 `kStatusTextMaxChars`(32자) 그대로 |
| `icon_glyph` 또는 `icon` | `icon{kind=kGlyph, glyph=...}`. 8자 상한 그대로 |
| `tooltip` | `tooltip` |
| `accent` (정수) | `accent` |
| `priority` | `priority` |
| `panel.title` / `subtitle` / `updated` | 그대로 |
| `panel.gauges[]` | `rows[]`의 `kGauge` 행. `label`, `value`, `detail`, `note`를 옮깁니다. `value_text`는 비웁니다 |
| `panel.actions[]` | 앞에 `kSeparator` 행 하나를 넣고, 각 항목을 `kButton` 행으로 만듭니다. `row_id`는 원래 인덱스를 십진수 문자열로 씁니다 |

`row_id`를 인덱스로 정하는 이유는 v1의 `SendClick` 동작과 호환되어야 하기 때문입니다. v1 클라이언트는 액션 인덱스가 아니라 액션 문자열을 받고 있었으므로(현재 `SendClick(id, WideToUtf8(actions[index]))`), **v1 클라이언트에게 보내는 이벤트는 지금 형식을 그대로 유지하십시오.** v2로 접속한 클라이언트에게만 새 이벤트 형식을 보냅니다. 클라이언트별 프로토콜 버전을 `Client` 구조체에 저장해 두십시오.

**검증**
- [ ] `examples/status_push.py`의 기존 예제가 수정 없이 같은 모양으로 표시됩니다.
- [ ] 그 예제의 액션 버튼을 누르면 v1 형식의 클릭 이벤트가 그대로 갑니다.

---

## 3. 커밋 2: v2 파서

### 3-1. 버전 분기

`PipeServer::HandleLine`을 `v` 값으로 나눕니다. **v1 경로의 코드를 고치지 말고 그대로 두십시오.** v2 경로를 새 함수로 작성한 뒤, 두 경로가 같은 `StatusItem`을 만들어 합류하게 합니다.

```cpp
if (*ver == 1) {
  return HandleV1(client, line);
}
if (*ver == 2) {
  return HandleV2(client, line);
}
return;   // 모르는 버전은 조용히 버린다
```

### 3-2. 연결 직후 hello 보내기

클라이언트가 붙으면 bamti가 먼저 한 줄을 보냅니다. 계획 문서 3-2절의 형식을 그대로 쓰십시오. `WriteLine`이 이미 있습니다.

클라이언트가 `hello`를 읽지 않고 바로 `upsert`를 보내도 정상 동작해야 합니다. 응답을 기다리지 마십시오.

### 3-3. op 처리

| op | 처리 |
|---|---|
| `upsert` | 항목 전체 교체. `revision`을 1 올립니다 |
| `patch` | 존재하는 항목만 부분 갱신. 없으면 무시합니다. `revision`을 1 올립니다 |
| `remove` | 소유자가 같을 때만 제거합니다. 지금 코드와 같습니다 |
| `ping` | 아무 상태도 바꾸지 않습니다 |

`patch`의 핵심은 **없는 필드와 빈 값을 구분하는 것**입니다. `json::GetString`이 `std::optional`을 돌려주므로, `nullopt`면 손대지 않고 값이 있으면 빈 문자열이라도 반영합니다. 이 구분을 흐리면 `text`를 지우는 요청을 표현할 수 없습니다.

### 3-4. 검증과 상한

계획 문서 3-8절의 표를 그대로 적용합니다. **파서에서 전부 끝내십시오.** UI 스레드에 도달하는 데이터가 이미 안전하다는 것이 불변식입니다.

- `value`는 0.0에서 1.0으로 클램프합니다. NaN과 무한대는 0으로 바꿉니다. `std::isfinite`로 확인하십시오.
- 모든 문자열은 상한으로 자릅니다. 바 텍스트 32자, 패널 문자열 128자, 글리프 8자입니다.
- `rows`는 32개까지입니다. 초과분은 버립니다.
- 클라이언트당 항목 16개, 전체 64개를 넘으면 새 `upsert`를 무시하고 로그에 **한 번만** 남깁니다.
- 한 줄이 64KB를 넘으면 그 연결을 끊습니다.
- 모르는 `type`의 행은 `fallback_text`가 있으면 `kText` 행으로 바꾸고, 없으면 버립니다. **패널 자체는 정상적으로 엽니다.**
- 모르는 필드는 오류가 아닙니다. 무시하십시오.

`accent`는 `"#RRGGBB"` 문자열과 v1의 정수를 모두 받습니다. 문자열 파싱에 실패하면 0으로 둡니다.

### 3-5. 갱신 빈도 제한

항목당 초당 10회를 넘는 갱신은 마지막 값만 남깁니다. 구현은 단순하게 하십시오. 항목은 언제나 저장하되(마지막 값이 이깁니다), `NotifyUi()` 호출을 **50ms에 한 번**으로 묶습니다. UI 쪽 16ms 합치기 타이머가 이미 있으므로 이것으로 충분합니다.

타이머를 새로 만들지 말고, 마지막 통지 시각을 기억했다가 비교하는 방식으로 하십시오. 다만 마지막 갱신이 통지 없이 묻히면 안 되므로, 묶인 구간이 끝난 뒤 한 번은 반드시 통지되어야 합니다.

### 3-6. 이벤트 송신

`SendClick(id, button)`을 일반화합니다.

```cpp
void SendEvent(const std::string& id, std::string_view event, std::string_view row_id = {},
               std::string_view button = {}, bool on = false);
```

계획 문서 3-6절의 여섯 가지 이벤트를 보냅니다. `click`, `panel_open`, `panel_close`, `invoke`, `toggle`입니다. v1 클라이언트에게는 2-2절대로 기존 형식을 유지하십시오.

**검증**
- [ ] v2 `upsert`로 보낸 항목이 바에 표시됩니다.
- [ ] `patch`로 `segment.text`만 보내면 그 값만 바뀌고 패널은 유지됩니다.
- [ ] 없는 `id`에 `patch`를 보내면 아무 일도 없고 크래시하지 않습니다.
- [ ] `value`에 `NaN`, `-1`, `2`, 문자열을 넣어도 크래시하지 않고 각각 0, 0, 1, 0으로 처리됩니다.
- [ ] 모르는 `type`의 행을 섞으면 패널이 열리고 그 행만 대체되거나 생략됩니다.
- [ ] 64KB를 넘는 줄을 보내면 그 연결만 끊기고 다른 클라이언트는 멀쩡합니다.
- [ ] 깨진 UTF-8과 잘린 JSON을 보내도 그 줄만 버려집니다.
- [ ] 초당 200회 `patch`를 보내도 CPU 사용률이 1%를 넘지 않습니다.

---

## 4. 커밋 3: 아이콘 캐시와 바 아이콘

2단계에서 미뤄 둔 작업입니다. 이제 소비자가 생겼으므로 추출합니다.

### 4-1. 추출 범위

```
src/icon_cache.hpp / .cpp
```

`dock.cpp`에서 옮길 것은 `WicFactory()`, `BgraImage` 구조와 알파 보정 코드, `BitmapFromIcon()`입니다. `dock.cpp`는 이 함수들을 새 헤더에서 가져다 쓰도록 고치고, 자기 안의 사본을 지웁니다.

**`dock.cpp`의 `d2d_icons_` 캐시는 그대로 두십시오.** 독과 바는 아이콘 수명과 키가 다르므로 캐시까지 합치지 않습니다. 공유하는 것은 변환 함수뿐입니다. 이 경계를 넘지 마십시오. `dock.cpp`는 2985줄이고 이 작업의 주제가 아닙니다.

```cpp
class IconCache {
 public:
  // 렌더 타깃이 바뀌면 캐시를 통째로 버린다. D2D 비트맵은 타깃에 매인다.
  void SetRenderTarget(ID2D1RenderTarget* rt);
  // 없으면 만들고, 있으면 돌려준다. 실패하면 nullptr.
  ID2D1Bitmap* Get(const StatusIcon& icon, int px);
  void Clear();
};
```

키는 `(icon.cache_key, px)`입니다. 상한 64개의 LRU로 두십시오.

**가장 흔한 사고는 렌더 타깃이 다시 만들어진 뒤에도 예전 비트맵을 쓰는 것입니다.** `clock_renderer.cpp`에서 `rt_.Reset()`을 하는 모든 자리와 `SetDpi`에서 `IconCache::Clear()`를 함께 부르십시오. 빠뜨리면 화면이 깨지거나 죽습니다.

### 4-2. 아이콘 종류별 처리

- `kGlyph`: **비트맵으로 만들지 않습니다.** 지금처럼 글리프와 텍스트를 한 문자열로 이어서 그립니다. 현재 모양이 그대로 유지되고 위험이 없습니다.
- `kPng`: 바이트열을 WIC 디코더로 읽습니다. 8KB 초과, 64x64 초과, 디코딩 실패면 아이콘 없이 텍스트만 그리고 로그에 남깁니다.
- `kFile`: 경로를 읽습니다. `.ico`와 `.png`를 받습니다. 읽기 실패면 아이콘을 생략합니다. **파일을 감시하지 마십시오.** 아이콘을 바꾸려면 다시 `upsert` 해야 합니다.
- `kHicon`: 프로세스 내부 전용입니다. 파이프에서 받지 마십시오. 지금은 소비자가 없으므로 경로만 열어 둡니다.

### 4-3. 레이아웃과 그리기

`BarLayout::Compute`가 아이콘 폭을 함께 잡아야 합니다. 아이콘 크기는 16 DIP 정사각형이고 텍스트와의 간격은 4 DIP입니다. 아이콘이 있으면 세그먼트 폭이 그만큼 넓어집니다.

`ClockRenderer::Draw`는 세그먼트에 비트맵 아이콘이 있으면 왼쪽에 그리고 텍스트를 그 오른쪽에 그립니다. 아이콘만 있고 텍스트가 없는 항목도 유효합니다.

`BarSegment`에 아이콘을 담을 때도 포인터를 쓰지 마십시오. `cache_key`와 `IconKind`, 그리고 필요한 최소 정보만 복사합니다.

**검증**
- [ ] 독의 아이콘이 추출 전과 똑같이 나옵니다. 회귀가 없는지 확인하십시오.
- [ ] PNG 아이콘을 보낸 항목이 바에 아이콘과 함께 나옵니다.
- [ ] 8KB를 넘는 PNG를 보내면 텍스트만 나오고 크래시하지 않습니다.
- [ ] 없는 파일 경로를 보내면 텍스트만 나옵니다.
- [ ] DPI를 바꿔도 아이콘이 깨지지 않습니다.
- [ ] 같은 아이콘을 초당 열 번 보내도 디코딩은 한 번만 일어납니다. 로그로 확인하십시오.
- [ ] 아이콘 20개를 띄운 상태에서 세그먼트 그리기 시간이 3단계 시작 전보다 0.3ms 넘게 늘지 않습니다.

### 4-4. 여기서 멈추고 보고합니다

0절의 중간 점검 지점입니다. 다음을 보고해 주십시오.

- 세 커밋의 해시와 제목
- 위 검증 항목의 통과 여부
- `perf bar` 로그 한 줄(아이콘을 띄운 상태)
- 판단이 필요했던 지점과 그때 고른 선택

계속하라는 지시를 받은 뒤에 5절로 넘어갑니다.

---

## 5. 커밋 4: 공급자 레지스트리

지금은 `MenuBar`가 `PipeServer`를 직접 들고 있습니다. 앞으로 트레이 미러와 내장 위젯이 같은 자리에 붙어야 하므로, 그 사이에 레지스트리를 넣습니다.

```
src/status_source.hpp     계획 문서 3-9절의 인터페이스
src/status_registry.hpp / .cpp
```

```cpp
class StatusRegistry : public StatusSink {
 public:
  void SetNotify(HWND hwnd);
  void Register(StatusSource* source);      // 소유하지 않는다
  bool StartAll();
  void StopAll();
  void SetActive(bool active);              // 전체화면 가림, 세션 잠금에서 false
  std::vector<StatusItem> Snapshot() const; // priority 내림차순, 같으면 id 오름차순
  void Dispatch(const StatusEvent& ev);     // 항목을 소유한 공급자에게 전달한다
  void DropStale();
};
```

- `Upsert`와 `Remove`는 어느 스레드에서 불려도 안전해야 합니다. 내부에서 잠그고 UI에는 `PostMessage`만 합니다.
- 정렬은 `Snapshot()`이 책임집니다. `BarLayout`에서 다시 정렬하지 마십시오. 정렬 지점이 둘이 되면 언젠가 어긋납니다.
- `Dispatch`는 `id`로 소유 공급자를 찾아 `OnEvent`를 부릅니다. 찾지 못하면 조용히 버립니다.
- `PipeServer`가 `StatusSource`를 구현합니다. `Name()`은 `"pipe"`입니다.
- `MenuBar`는 `PipeServer`를 직접 부르지 않습니다. 레지스트리만 봅니다.

**검증**
- [ ] 기존 동작이 그대로입니다. 표시, 클릭, 툴팁, 만료 제거가 모두 됩니다.
- [ ] 항목 정렬이 `Snapshot()` 한 곳에서만 일어납니다.
- [ ] 전체화면에 가리면 `SetActive(false)`가 전달됩니다.

---

## 6. 커밋 5: 패널 행 렌더링과 토글

### 6-1. 파일 분리

`menu_bar.cpp`의 익명 영역에 있는 `StatusPanelContent`와 2단계에서 만든 `OverflowContent`를 `src/status_panel.{hpp,cpp}`로 옮깁니다. `menu_bar.cpp`가 지나치게 커졌습니다. 이때 `MenuBar`의 `friend` 선언 대신 필요한 값을 인자로 넘기는 방식으로 정리하십시오.

### 6-2. 행 종류

계획 문서 3-4절의 여섯 종류를 그립니다. 기존 게이지 그리기 코드가 `kGauge`의 기반이 되므로, 간격 상수(`kPanelGaugeLabelDip` 등)를 그대로 재사용하십시오. 패널 모양이 지금과 크게 달라지면 안 됩니다.

- `kGauge`: 지금 그리는 모양에 `value_text`를 오른쪽 위에 추가합니다. 비어 있으면 지금처럼 `detail`만 나옵니다.
- `kKeyValue`: 왼쪽 레이블, 오른쪽 값. 한 줄입니다.
- `kText`: 한 줄 문단. `style`이 `note`면 흐린 색입니다.
- `kSeparator`: 지금의 구분선 그리기를 그대로 씁니다.
- `kToggle`: 왼쪽 레이블, 오른쪽 스위치. 스위치는 둥근 사각형 트랙과 원형 손잡이로 그립니다. 켜짐은 강조색, 꺼짐은 흐린 회색입니다.
- `kButton`: 지금의 액션 행 그리기를 씁니다. `style`이 `danger`면 글자를 경고색으로 그립니다.

연속한 `kButton` 행은 한 줄에 나란히 놓습니다. 세 개를 넘으면 줄을 바꿉니다. 첨부 이미지의 `Settings...`와 `Quit` 배치가 이것입니다.

히트 영역은 `kToggle`과 `kButton`만 가집니다. 나머지 행은 `HitTest`에서 `-1`을 돌려줍니다.

### 6-3. 토글의 낙관적 갱신

계획 문서 3-7절의 규칙을 구현합니다. 확인 수단으로 커밋 1에서 넣은 `revision`을 씁니다.

1. 사용자가 스위치를 누르면 패널이 **즉시** 시각 상태를 뒤집고 다시 그립니다.
2. `toggle` 이벤트를 보냅니다.
3. 누른 시점의 `revision`을 기억하고 2초짜리 타이머를 겁니다.
4. 타이머가 만료될 때 그 항목의 `revision`이 그대로면, 시각 상태를 되돌리고 항목을 `kError` 상태로 표시합니다. 값이 올라갔으면 공급자가 응답한 것이므로 아무것도 하지 않습니다.

패널이 닫힌 뒤에 타이머가 만료될 수 있습니다. 그 경우에도 항목의 상태 표시는 갱신되어야 하므로, 타이머는 패널이 아니라 `MenuBar`가 들고 있어야 합니다.

**검증**
- [ ] 여섯 종류 행이 모두 그려집니다.
- [ ] 토글을 누르면 지연 없이 뒤집힙니다.
- [ ] 응답하는 클라이언트에 대해서는 되돌아가지 않습니다.
- [ ] 클라이언트를 죽인 채 토글을 누르면 2초 뒤 원래대로 돌아가고 항목이 오류 상태가 됩니다.
- [ ] 토글 직후 패널을 닫아도 2초 뒤 상태 표시가 갱신됩니다.
- [ ] 연속한 버튼 두 개가 한 줄에 나란히 나옵니다.
- [ ] 패널을 100번 여닫아도 GDI 객체와 핸들이 늘지 않습니다.

---

## 7. 커밋 6: 명세와 예제

### 7-1. docs/STATUS-PROTOCOL.md

계획 문서 3절을 옮겨 공개 명세로 만듭니다. 계획 문서는 내부 문서이고 이쪽은 외부 개발자가 보는 문서이므로, 구현 사정(어느 파일에서 파싱하는지 등)은 빼고 규격만 남깁니다. 구현하면서 명세와 달라진 부분이 있으면 **명세를 실제 동작에 맞게 고치십시오.** 문서가 거짓말을 하면 안 됩니다.

### 7-2. examples

- `status_push.py`: v2 예제를 추가합니다. 기존 v1 예제는 회귀 시험이므로 지우지 마십시오.
- `ticker.py`: 새로 만듭니다. 30초마다 값을 갱신하고, 패널에 `kv` 행과 `toggle` 행을 하나씩 둡니다. 토글을 받으면 상태를 바꿔 `patch`로 되돌려 보냅니다. 30줄 안팎이면 충분합니다.

`ticker.py`가 이 단계의 진짜 검증입니다. **아이콘과 값, 아이콘과 토글이 같은 규격으로 표현된다는 것을 증명하는 회귀 시험**이기 때문입니다.

---

## 8. 하지 말아야 할 것

1. **새 JSON 파서를 들이지 않습니다.** `json_line.hpp`를 확장하십시오.
2. **모델을 둘 유지하지 않습니다.** v1 입력은 파서에서 v2로 바꿉니다.
3. **v1 클라이언트를 깨뜨리지 않습니다.** 이벤트 형식도 유지합니다.
4. **검증을 UI 쪽으로 미루지 않습니다.** 파서를 통과한 데이터는 이미 안전해야 합니다.
5. **`dock.cpp`의 아이콘 캐시를 바와 합치지 않습니다.** 변환 함수만 공유합니다.
6. **정렬을 두 곳에서 하지 않습니다.**
7. **패널 모양을 크게 바꾸지 않습니다.** 새 행 종류를 더하는 것이지 기존 배치를 다시 디자인하는 것이 아닙니다.
8. **플러그인 방식을 만들지 않습니다.** 확장은 파이프로만 받습니다.
9. **중간 점검 지점을 건너뛰지 않습니다.**

---

## 9. 커밋

| 순서 | 커밋 메시지 |
|---|---|
| 1 | `refactor: 상태 항목 모델을 프로토콜 v2 형태로 넓힌다` |
| 2 | `feat: 상태 항목 프로토콜 v2 파서와 이벤트를 추가한다` |
| 3 | `refactor: 아이콘 비트맵 변환을 공용 모듈로 옮기고 상단바에 아이콘을 그린다` |
| 4 | `refactor: 상태 항목 공급자를 레지스트리로 통합한다` |
| 5 | `feat: 상태 패널에 여섯 가지 행과 토글을 그린다` |
| 6 | `docs: 상태 항목 프로토콜 명세와 예제를 추가한다` |

3번을 마치면 멈추고 보고하십시오.
