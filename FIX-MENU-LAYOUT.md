# 작업 지시서 13: 우클릭 메뉴를 macOS 독 메뉴 구조로 바꾼다

드래그와 반투명 제거는 사용자 확인으로 완료되었습니다. 이번에는 메뉴의 항목 구성과 순서를 바꿉니다.

**이 지시서는 팝업 시스템을 건드립니다.** 지난 여러 차례의 멈춤이 전부 이 부분에서 났습니다. 5장의 금지 사항과 6장의 회귀 검증을 반드시 지키십시오.

---

## 1. 목표 구조

사용자가 macOS 독 메뉴 화면 두 장을 근거로 제시했습니다.

### 1.1 창이 열려 있을 때

```
✓ 창 제목 1
  창 제목 2
──────────────────
새 창
──────────────────
옵션                ▸
──────────────────
모두 보기
가리기
종료
```

### 1.2 창이 열려 있지 않을 때

```
옵션                ▸
──────────────────
열기
```

### 1.3 옵션 서브메뉴 (두 경우 공통)

```
✓ 독에 유지
  로그인 시 열기
  파일 위치 열기
```

### 1.4 현재 구조와의 차이

지금은 `DockMenuContent::Reset`(dock.cpp:694)이 이렇게 만듭니다.

```
새 창
──────────
창 제목들
──────────
고정 해제 / 독에 고정
──────────
모두 보기
가리기
종료
```

바뀌는 점은 넷입니다.

1. **창 목록이 맨 위로** 올라가고, `새 창`이 그 아래로 내려갑니다.
2. **고정 관련 항목이 `옵션` 서브메뉴 안으로** 들어갑니다.
3. **체크 표시**가 생깁니다. 현재 활성 창과 고정 여부에 붙습니다.
4. 창이 없을 때 `새 창` 대신 **`열기`**가 나오고, `옵션`이 맨 위에 옵니다.

---

## 2. 표시 문구와 명령

원본이 영어이지만 이 프로그램의 기존 메뉴는 한국어입니다(`모두 보기`, `가리기`, `종료`). **한국어를 유지하십시오.**

| 표시 문구 | 상수 | 값 | 동작 |
| --- | --- | --- | --- |
| 창 제목 | `kWindowCommandBase` | 100~ | 기존과 같음 |
| `새 창` | `kNewWindowCommand` | 6 | 기존과 같음 (`LaunchDockApp`) |
| `열기` | `kOpenCommand` | 7 | `LaunchDockApp`. 창이 없을 때만 표시 |
| `옵션` | `kOptionsCommand` | 8 | 서브메뉴를 여는 행. 직접 실행되지 않음 |
| `독에 유지` | `kPinCommand` / `kUnpinCommand` | 1 / 2 | 기존 동작. 고정되어 있으면 체크 |
| `로그인 시 열기` | `kToggleLoginCommand` | 9 | 3-4 참조 |
| `파일 위치 열기` | `kShowInFolderCommand` | 10 | 3-5 참조 |
| `모두 보기` / `가리기` / `종료` | 기존 | 4 / 5 / 3 | 기존과 같음 |

`MenuCmdName`(dock.cpp:87)에 새 상수를 **모두** 추가하십시오. 빠뜨리면 로그가 `none`으로 찍혀 진단이 막힙니다.

`독에 유지`는 한 행으로 두고 체크 표시로 상태를 나타냅니다. 고정되어 있으면 체크가 붙고 누르면 해제되며, 고정되어 있지 않으면 체크가 없고 누르면 고정됩니다. **`독에서 제거`라는 별도 문구를 만들지 마십시오.** 행이 하나여야 위치가 흔들리지 않습니다.

---

## 3. 수정 지시

### 3-1. 행에 체크와 서브메뉴 표시를 추가한다 (필수)

`DockMenuRow`(dock.cpp:685 부근)에 두 개를 더하십시오.

```cpp
bool checked = false;    // 왼쪽에 체크 표시를 그린다
bool submenu = false;    // 오른쪽에 ▸ 를 그린다
```

`DockMenuContent::Measure`와 `Render`를 이에 맞게 고치십시오.

- **왼쪽에 체크 칸을 항상 비워 두십시오.** 폭은 `DipToPx(16, dpi)` 정도로 하고, 체크가 없는 행도 같은 만큼 들여씁니다. 행마다 텍스트 시작 위치가 달라지면 안 됩니다.
- 체크 표시는 `DrawPopupText`로 `L"✓"`(✓)를 체크 칸에 그리면 됩니다. 새 글꼴이나 도형을 만들지 마십시오.
- `▸`는 `L"›"`를 행의 오른쪽 끝에 그리십시오. 오른쪽 여백도 `Measure`의 폭 계산에 넣어야 글자가 잘리지 않습니다.
- `Measure`의 텍스트 폭 계산에 체크 칸과 화살표 칸을 더하십시오. 지금 계산(dock.cpp:789)은 텍스트 폭만 봅니다.

### 3-2. 메뉴 구성을 1장의 순서로 바꾼다 (필수)

`DockMenuContent::Reset`을 다시 쓰십시오. 순서는 1.1과 1.2 그대로입니다.

- 창 목록은 지금처럼 `app.windows`를 훑어 만들되 **맨 위**에 놓습니다.
- **현재 활성 창에 체크를 붙이십시오.** 판정은 `Reset` 시점의 `GetForegroundWindow()`와 비교합니다. 독 창은 `WS_EX_NOACTIVATE`라 포커스를 가져가지 않으므로, 메뉴를 연 시점에도 앱의 창이 그대로 전경입니다. 일치하는 창이 없으면 아무 데도 체크하지 않습니다.
- `옵션` 행은 `submenu = true`로 두고, **항상 넣으십시오.** 창이 있든 없든 나옵니다.
- 창이 있으면 `새 창`, 없으면 `열기`입니다. **둘이 동시에 나오면 안 됩니다.**
- `모두 보기`, `가리기`, `종료`는 지금처럼 창이 있을 때만 넣습니다.
- 구분선 규칙은 1장의 그림과 같습니다. 기존 `add_sep()` 람다를 그대로 쓰고, 마지막이 구분선으로 끝나면 제거하는 처리도 유지하십시오.
- 자기 자신(`IsSelfExecutable(app.exe_path)`)에는 `새 창`과 `열기`를 넣지 마십시오. 기존 판정을 유지합니다.

### 3-3. 옵션 서브메뉴를 만든다 (필수, 이 지시서에서 가장 위험한 부분)

`옵션` 행에 커서를 올리면 오른쪽에 두 번째 팝업이 열려야 합니다.

**구조**

- `DockSubmenuContent`를 `PopupContent`를 상속해 새로 만드십시오. 행은 1.3의 셋입니다.
- `Dock`에 두 번째 `PopupSurface submenu_;`를 두십시오. 기존 `popup_`은 그대로입니다.
- `PopupSurface::Anchor`에 `RightOf`를 추가하십시오. `Place()`(popup_surface.cpp:305 부근)에서 앵커 점의 오른쪽에 세로를 맞춰 배치합니다. 화면 오른쪽 끝을 넘으면 왼쪽으로 뒤집으십시오. 기존 `AboveAt`, `BelowAt`의 동작을 바꾸지 마십시오.

**여는 시점**

- 부모 팝업의 `hot` 행이 `옵션`으로 바뀌면 엽니다. `hot`이 다른 행으로 바뀌면 닫습니다.
- 클릭으로 여는 경로도 넣으십시오. 마우스가 지나가는 것만으로 여닫히면 조작이 어렵습니다.

**닫힘 판정 (여기가 핵심입니다)**

지금까지의 멈춤이 전부 이 판정에서 났습니다. 다음을 반드시 지키십시오.

- **서브메뉴는 `SetCapture`를 부르지 마십시오.** 캡처는 부모 팝업이 계속 쥡니다. 두 창이 캡처를 주고받으면 `WM_CAPTURECHANGED`가 서로를 닫는 고리가 만들어집니다.
- **부모의 "바깥" 판정에 서브메뉴 영역을 포함시키십시오.** 커서나 클릭이 서브메뉴 위에 있으면 부모는 바깥으로 보면 안 됩니다. 부모의 기존 판정 코드를 새로 쓰지 말고, 판정에 쓰는 사각형에 서브메뉴 창의 사각형을 더하는 방식으로 하십시오.
- **부모가 닫히면 서브메뉴도 닫으십시오.** 순서는 서브메뉴가 먼저입니다.
- 서브메뉴 항목을 실행하면 **둘 다 닫습니다.**
- `Tick()`의 재진입 방지, `armed_`, 가드 타이머를 **건드리지 마십시오.** 서브메뉴에도 같은 장치가 필요하면 부모의 것을 재사용하십시오. 새로 설계하지 마십시오.

**계측**

서브메뉴를 열고 닫을 때마다 한 줄씩 남기십시오.

```cpp
Log(L"popup", L"submenu open rows=%d %ums", ...);
Log(L"popup", L"submenu close reason=%s", ...);
```

### 3-4. `로그인 시 열기` (필수)

`HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`을 씁니다.

- 값 이름은 앱을 구분할 수 있어야 합니다. `L"bamti-dock-" + 실행 파일 이름` 같은 형태로 하되, 같은 앱에 두 값이 생기지 않게 하십시오.
- 값 데이터는 따옴표로 감싼 실행 파일 경로입니다. AUMID만 있는 앱은 `explorer.exe shell:AppsFolder\<AUMID>` 형태를 쓰십시오.
- 체크 상태는 **`Reset` 시점에 레지스트리를 읽어** 정합니다. 캐시하지 마십시오. 메뉴는 자주 열리지 않으므로 비용이 문제되지 않습니다.
- 실패하면 조용히 넘어가지 말고 로그를 남기십시오. `Log(L"dock", L"login toggle failed err=%lu", ...)`.
- **다른 프로그램이 만든 `Run` 값을 지우거나 고치지 마십시오.** 우리가 만든 값 이름만 다룹니다.

### 3-5. `파일 위치 열기` (필수)

`app.exe_path`가 있으면 탐색기에서 그 파일을 선택한 상태로 엽니다.

```cpp
ShellExecuteW(nullptr, L"open", L"explorer.exe", (L"/select,\"" + path + L"\"").c_str(), nullptr, SW_SHOWNORMAL);
```

- `app.exe_path`가 비어 있으면 이 행을 **아예 넣지 마십시오.** 비활성 행을 만들지 말고 목록에서 빼십시오.
- 경로에 따옴표가 들어 있으면 그대로 넘기지 마십시오.

### 3-6. 하지 않는 것

macOS 화면에 있지만 이번에 넣지 않는 것입니다. **시도하지 마십시오.**

- `New Incognito Window` 같은 앱별 항목. Windows에서는 점프 목록을 읽어야 하고 범위가 큽니다.
- 서브메뉴의 서브메뉴.
- 항목 비활성(회색) 상태. 넣지 않을 항목은 목록에서 빼는 것으로 처리합니다.
- 아이콘이나 단축키 표시.

---

## 4. 커밋을 나누십시오

**서브메뉴가 불안정하면 그 커밋만 되돌릴 수 있어야 합니다.** 반드시 셋으로 나누십시오.

```
feat: 메뉴 행에 체크와 서브메뉴 표시를 그린다
```
3-1입니다.

```
feat: 우클릭 메뉴 순서를 창 목록 우선으로 바꾼다
```
3-2입니다. **이 시점에서 `옵션` 행은 아직 서브메뉴를 열지 않아도 됩니다.** 고정 관련 항목을 임시로 본 메뉴에 두어도 좋습니다. 다만 순서와 체크는 1장대로여야 합니다.

```
feat: 옵션 서브메뉴와 로그인 시 열기, 파일 위치 열기를 추가한다
```
3-3부터 3-5까지입니다. 본문에 6장의 안정성 측정값을 적으십시오.

---

## 5. 하지 말아야 할 것

- **`PopupSurface`의 닫힘 판정 구조를 새로 설계하지 마십시오.** `armed_`, `Tick()`의 재진입 방지, 가드 타이머, `SetCapture` 사용 방식을 그대로 둡니다. 서브메뉴는 기존 판정에 영역을 더하는 방식으로 붙입니다.
- 서브메뉴에서 `SetCapture`를 부르지 마십시오.
- `Anchor::AboveAt`와 `BelowAt`의 배치 동작을 바꾸지 마십시오.
- 지금까지의 안정성 수정과 계측을 되돌리지 마십시오. 감시 스레드, `msg flood`, 트레이 억제, 굶주림 방지, 호버, 드롭 순서 즉시 반영을 모두 유지합니다.
- `WH_MOUSE_LL`, `WH_KEYBOARD_LL`을 도입하지 마십시오.
- 3-6의 항목을 넣지 마십시오.
- 다른 프로그램의 `Run` 레지스트리 값을 건드리지 마십시오.

---

## 6. 검증

**안정성 (가장 중요합니다. 여기서 실패하면 서브메뉴 커밋을 버립니다)**
- [ ] 메뉴를 열고 바깥 클릭으로 닫기를 **스무 번** 반복해도 멈추지 않는다.
- [ ] `옵션` 위에 커서를 올려 서브메뉴를 열고, 커서를 다른 행으로 옮기면 서브메뉴만 닫힌다. 부모는 열려 있다.
- [ ] 서브메뉴가 열린 상태에서 바깥을 클릭하면 **둘 다** 닫힌다.
- [ ] 서브메뉴 항목을 실행하면 둘 다 닫힌다.
- [ ] 부모와 서브메뉴를 오가며 커서를 10초간 움직여도 CPU가 0.1 코어 미만이다.
- [ ] `[perf] msg flood` 줄이 남지 않는다.
- [ ] `watchdog.log`에 `alive stage=idle ping=ok`만 쌓인다.

**구조**
- [ ] 창이 있는 앱: 창 목록 → 새 창 → 옵션 → 모두 보기/가리기/종료 순이다.
- [ ] 창이 없는 앱: 옵션 → 열기 순이다. `새 창`이 보이지 않는다.
- [ ] 현재 활성 창에 체크가 붙는다.
- [ ] 고정된 앱의 `독에 유지`에 체크가 붙고, 누르면 해제된다.
- [ ] 체크가 없는 행도 텍스트 시작 위치가 같다.
- [ ] `옵션` 오른쪽에 화살표가 잘리지 않고 보인다.

**동작**
- [ ] `로그인 시 열기`를 켠 뒤 `Run` 키에 값이 생기고, 다시 누르면 사라진다.
- [ ] 재부팅 없이도 메뉴를 다시 열면 체크 상태가 유지된다.
- [ ] `파일 위치 열기`가 탐색기에서 해당 파일을 선택한 채로 연다.
- [ ] `exe_path`가 없는 앱에는 `파일 위치 열기`가 보이지 않는다.
- [ ] `[dock] menu cmd=` 줄에 새 명령 이름이 제대로 찍힌다.

**회귀**
- [ ] 창 목록에서 항목을 고르면 그 창이 활성화된다.
- [ ] 드래그 재정렬과 호버 강조가 그대로 동작한다.
- [ ] 유휴 60초 누적 CPU가 1초 미만이다.
- [ ] Release 빌드가 `/W4` 경고 없이 통과한다.
