# 작업 지시서: 제어 센터 패널을 다시 디자인한다

`FIX-POPUP-RADIUS.md` 다음에 하십시오. **순서를 지켜야 합니다.** 패널의 바깥 배경과 모서리는 그 지시서가 `PopupSurface`에서 그리도록 바꾸고, 이 지시서는 그 안쪽 내용만 다룹니다.

건드리는 파일은 `src/control_center.cpp`, `src/control_center.hpp`, `src/theme.cpp`, `src/theme.hpp`, `src/slider_geom.hpp`입니다.

---

## 1. 무엇이 문제인가

지금 패널(`f3a4886`부터 `5495cfd`까지)은 기능은 다 들어갔지만 짜임새가 나쁩니다.

1. **타일 이름이 타일 바깥 아래에 붙어 있습니다.** `TileRect` 아래에 `kTileNameHDip = 18`짜리 띠를 따로 두고 거기에 글자를 그립니다. 타일과 글자가 따로 놀고, 긴 이름(`kibeomkwon_5G`)은 잘려서 `kibeomkwon_...`이 됩니다.
2. **아이콘이 타일에 비해 너무 작습니다.** 56 DIP 타일 안에 16 DIP 글리프 하나가 떠 있습니다.
3. **켜짐 표시가 회색입니다.** `DockIndicatorColor`를 쓰고 있어서 켜진 타일이 그냥 밝은 회색 덩어리로 보입니다. 무엇이 켜졌는지 한눈에 안 들어옵니다.
4. **꺾쇠(`U+E76C`)가 타일마다 붙어 있습니다.** 실제로는 전부 설정 앱을 열 뿐인데 여섯 개가 다 화살표를 달고 있어 시끄럽습니다.
5. **슬라이더가 얇습니다.** 트랙 6 DIP에 손잡이 14 DIP입니다. 옆의 큰 타일들과 굵기가 맞지 않습니다.
6. 화면에서 "절전 모드" 글자 왼쪽에 배터리처럼 생긴 무언가가 함께 그려져 있습니다. 의도한 것이 아닙니다. **원인을 찾아 없애십시오.**

첨부된 macOS 제어 센터를 참고합니다. 유리 재질은 흉내 내지 않습니다. **가져올 것은 배치와 비율입니다.**

- 아이콘은 원형 배지 안에 들어가고, 배지는 켜졌을 때 강조색으로 찹니다.
- 이름과 상태가 배지 오른쪽에 두 줄로 붙습니다. 타일 바깥으로 나가지 않습니다.
- 슬라이더는 굵은 알약이고, 채워진 부분 자체가 값을 나타냅니다.
- 같은 성격의 항목은 하나의 카드로 묶입니다.

**모서리 곡률은 macOS를 따라가지 말고 지금 값을 유지하십시오.** 사용자가 명시했습니다.

---

## 2. 색을 먼저 정리한다

`src/theme.hpp`와 `src/theme.cpp`에 넷을 더합니다.

```cpp
D2D1_COLOR_F AccentFillColor(bool dark);    // 켜진 배지와 슬라이더 채움
D2D1_COLOR_F AccentOnColor(bool dark);      // 강조색 위에 올리는 글리프
D2D1_COLOR_F CardFillColor(bool dark);      // 카드 배경
D2D1_COLOR_F BadgeOffFill(bool dark);       // 꺼진 배지 배경
```

값은 다음으로 시작하십시오.

```
AccentFillColor  dark: 0x0A84FF   light: 0x0078D4
AccentOnColor    양쪽 다 흰색 (1, 1, 1, 1)
CardFillColor    dark: 흰색 알파 0.06   light: 검정 알파 0.045
BadgeOffFill     dark: 흰색 알파 0.14   light: 검정 알파 0.09
```

시스템 강조색을 레지스트리에서 읽어 오는 것은 **이번에 하지 마십시오.** 사용자가 고른 강조색이 채도가 낮으면 흰 글리프의 대비가 무너집니다. 별도 사안입니다.

보조 글자색은 `ClockTextColor(dark)`의 알파를 0.55배로 낮춰 쓰십시오. 색을 새로 만들지 마십시오.

---

## 3. 새 배치 (단위는 전부 DIP)

패널 폭은 **340**, 바깥 여백은 **14**입니다. 카드 폭은 `340 - 14*2 = 312`입니다. 카드와 타일의 모서리 반지름은 **12**로 통일합니다(지금 `kTileRadiusDip = 8`을 12로 올립니다).

세로로 다음 순서입니다. 각 구획 사이 간격은 **10**입니다.

### 3-1. 연결 카드 (높이 104)

카드 하나 안에 행 두 개가 들어갑니다. 각 행은 높이 **52**입니다.

| | 내용 |
|---|---|
| 0행 | Wi-Fi. 부제는 SSID 또는 `연결 안 됨` |
| 1행 | Bluetooth. 부제는 `켜짐` / `꺼짐` / `알 수 없음` |

행 하나의 구성입니다.

```
배지    지름 34인 원. 중심 x = 행.left + 12 + 17, 중심 y = 행의 세로 중앙
글리프  17. 배지 중앙
제목    x = 행.left + 58. Segoe UI Semibold 13. 배지 중심보다 위쪽 줄
부제    x = 행.left + 58. Segoe UI 11. 제목 아래 줄. 전경색 알파 0.55배
꺾쇠    U+E76C, 12. 오른쪽 끝에서 12 안쪽. 세로 중앙. 전경색 알파 0.45배
```

- 켜짐: 배지 = `AccentFillColor`, 글리프 = `AccentOnColor`.
- 꺼짐: 배지 = `BadgeOffFill`, 글리프 = `ClockTextColor`.
- 마우스가 올라간 행은 반지름 12의 둥근 사각형으로 `MenuItemHoverFill(dark, false)`를 칠합니다. 지금 `hot_index`를 받고도 `(void)hot;`으로 버리고 있습니다. 살리십시오.
- 제목과 부제는 **말줄임으로 잘라야 합니다.** `IDWriteTextLayout`에 `DWRITE_TRIMMING_GRANULARITY_CHARACTER`와 줄임표 기호를 걸고, 폭은 `꺾쇠 왼쪽 - 8`까지로 제한하십시오. 지금처럼 글자가 잘려 나가면 안 됩니다.

### 3-2. 빠른 설정 타일 (높이 114)

2열 2행입니다. 카드로 묶지 말고 타일 네 개를 따로 둡니다.

```
타일 크기  가로 151 = (312 - 10) / 2,  세로 52
간격       가로 10, 세로 10
```

| | 0열 | 1열 |
|---|---|---|
| 0행 | 비행기 모드 | 절전 모드 |
| 1행 | 야간 모드 | 접근성 |

타일 하나의 구성입니다.

```
배경    반지름 12의 둥근 사각형. CardFillColor
배지    지름 30인 원. 중심 x = 타일.left + 10 + 15, 중심 y = 세로 중앙
글리프  15
이름    x = 타일.left + 50, 오른쪽 끝에서 10 안쪽까지. Segoe UI 12. 한 줄. 세로 중앙. 말줄임
꺾쇠    없음
```

켜짐과 꺼짐, 마우스 올림 처리는 3-1과 같습니다. 지금 상태를 실제로 읽는 것은 절전 모드뿐이므로 나머지 셋은 항상 꺼짐으로 그립니다.

### 3-3. 디스플레이 카드 (높이 56, `brightness_ok_`일 때만)

```
카드    312 x 56, 반지름 12, CardFillColor
제목    "디스플레이". x = 카드.left + 12, y = 카드.top + 8, 높이 14. Segoe UI 11. 전경색 알파 0.55배
트랙    left = 카드.left + 12, right = 카드.right - 12
        top = 카드.top + 26, 높이 24, 반지름 12
```

### 3-4. 사운드 카드 (높이 56)

3-3과 같고 제목만 "사운드"입니다.

### 3-5. 푸터 (높이 28)

지금과 같습니다. 왼쪽에 배터리 아이콘 16과 백분율, 오른쪽 끝에 톱니 `U+E713`. 배터리가 없는 기기에서는 왼쪽을 비웁니다.

### 3-6. 전체 높이

```
14 + 104 + 10 + 114 + 10 + [56 + 10] + 56 + 10 + 28 + 14
```

대괄호는 밝기 슬라이더가 있을 때만 더합니다. `HeightDip()`을 이 식과 정확히 같게 고치십시오.

---

## 4. 슬라이더를 macOS 비율로 다시 그린다

### 4-1. 손잡이를 없앤다

지금은 얇은 트랙 위에 원형 손잡이가 올라가 있습니다. 새 모양은 **굵은 알약이고, 채워진 부분의 둥근 오른쪽 끝 자체가 값의 위치**입니다. 별도의 손잡이를 그리지 마십시오.

```
h = 24 (트랙 높이)
lo = left + h/2
hi = right - h/2
x  = lo + (hi - lo) * v
배경 = [left, right] 알약, 반지름 h/2, 색은 ClockTextColor의 알파 0.12배
채움 = [left, x + h/2] 알약, 반지름 h/2, 색은 AccentFillColor
```

`v = 0`이면 채움이 왼쪽 끝의 원 하나가 되고, `v = 1`이면 트랙을 꽉 채웁니다.

### 4-2. 기하를 한 곳에서 계산한다

지금 `Render`와 `DragTo`가 트랙의 좌우 끝을 각자 계산하고 있습니다. 값이 어긋나면 손이 닿은 자리와 그려지는 자리가 달라집니다. `FIX-SLIDER-ENDS.md`에서 이미 같은 종류의 결함을 고쳤습니다.

`ControlCenterContent`에 `RECT SliderTrackRect(UINT dpi, bool brightness) const`를 만들고 **`Render`와 `DragTo`가 모두 이것만 보게 하십시오.**

`src/slider_geom.hpp`에는 두께를 인자로 받는 갈래를 더하십시오. 기존 `SliderGeom`은 상태 패널이 쓰고 있으므로 그대로 두십시오.

```cpp
inline SliderGeometry SliderGeomThick(float left, float right, float thickness_px) {
  const float r = thickness_px * 0.5f;
  return SliderGeometry{left + r, right - r, r};
}
```

### 4-3. 트랙 안의 글리프

macOS처럼 트랙 왼쪽 안에 작은 글리프를 둡니다.

```
글리프  볼륨은 U+E767, 밝기는 U+E706. 크기 14
자리    x = left + 8, 세로 중앙
색      채움이 글리프를 덮고 있으면 AccentOnColor
        덮지 않으면 ClockTextColor의 알파 0.7배
```

덮는지 판정하는 식은 `x + h/2 >= left + 8 + 14 + 2`입니다. 값이 아주 작을 때 흰 글리프가 밝은 배경 위에 놓여 안 보이는 것을 막습니다.

### 4-4. 끌기

`DragTo`는 지금 그대로 0.02 단위로 반올림해 `slide` 이벤트를 보냅니다. 좌표를 값으로 바꾸는 식만 4-2의 `SliderTrackRect`와 `SliderGeomThick`을 쓰도록 고치십시오. 이벤트 규격은 바꾸지 마십시오.

---

## 5. 그리는 비용

`ControlCenterContent::Render`가 **프레임마다** `DWriteCreateFactory`를 부르고 텍스트 형식 두 개를 새로 만듭니다. 항목이 늘어나는 이번 작업에서 이 비용을 그대로 두면 안 됩니다.

- DWrite 팩토리와 텍스트 형식(Fluent 17, Fluent 15, Fluent 14, Semibold 13, Regular 12, Regular 11)을 멤버로 올려 한 번만 만드십시오.
- DPI가 바뀌면 다시 만들어야 하는지 확인하십시오. 크기를 DIP가 아니라 픽셀로 넘기고 있다면 다시 만들어야 합니다.
- 한 프레임을 그리는 데 걸린 시간을 `Log(L"cc", L"render %.2fms rows=%d")`로 남기십시오. `QpcMs`가 이미 이 파일에 있습니다.

---

## 6. 6번 항목: 정체불명의 배터리 모양

첨부 화면에서 "절전 모드" 글자 왼쪽에 배터리 비슷한 그림이 함께 보입니다. 다음 순서로 확인하십시오.

1. 그 자리는 `TileRect(dpi, 0, 1)` 아래의 이름 띠입니다. 이름 띠는 `DrawLabel`만 부릅니다.
2. `FooterRect(dpi)`가 계산하는 y가 `HeightDip()`과 어긋나 배터리 아이콘이 위로 올라온 것은 아닌지 보십시오. `HeightDip()`은 `brightness_ok_`에 따라 값이 바뀌는데, `Measure`가 부른 시점과 `Render`가 부른 시점 사이에 이 값이 바뀌면 두 계산이 어긋납니다.
3. 원인을 확정하고 고친 뒤, **무엇이었는지 보고하십시오.** 새 배치로 갈아엎으면 저절로 사라질 수도 있지만, 원인을 모른 채 넘어가면 다른 자리에서 다시 나옵니다.

---

## 7. 검증

1. 라이트 테마와 다크 테마에서 각각 열어 화면을 찍으십시오.
2. Wi-Fi가 연결된 상태에서 SSID가 잘리지 않고, 아주 긴 SSID에서는 말줄임표로 끝나야 합니다.
3. Wi-Fi를 껐다 켜고 Bluetooth를 껐다 켜서, 배지 색이 강조색과 회색 사이를 오가는지 보십시오.
4. 볼륨 슬라이더를 0까지 내렸을 때 트랙 안 글리프가 보여야 합니다. 흰색으로 남아 사라지면 4-3의 판정이 틀린 것입니다.
5. 볼륨 슬라이더를 끌었을 때 손이 닿은 자리와 채움의 끝이 어긋나지 않아야 합니다. 양 끝에서 0%와 100%에 정확히 닿아야 합니다.
6. 밝기를 지원하지 않는 화면에서 디스플레이 카드가 사라지고, 패널 높이가 그만큼 줄어들며, 푸터가 잘리지 않아야 합니다.
7. 100%, 150%, 200% 배율에서 각각 확인하십시오. 배지 원이 타원으로 찌그러지면 안 됩니다.
8. 각 타일과 각 행에 마우스를 올렸을 때 그 하나만 밝아져야 합니다.
9. 여섯 항목을 눌러 각각 맞는 설정 페이지가 열리는지 확인하십시오.
10. `cc render` 로그로 한 프레임 비용을 재고, 5절을 적용하기 전후를 함께 보고하십시오.

## 8. 보고할 것

- 라이트/다크 두 테마의 패널 화면.
- 6절의 원인.
- `cc render` 측정값의 이전과 이후.
- 3절의 수치를 바꿨다면 바꾼 값과 이유.
