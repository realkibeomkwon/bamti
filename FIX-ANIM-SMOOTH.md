# 작업 지시서 16: 드래그 애니메이션의 프레임 간격을 줄인다

애니메이션은 동작합니다. 아이콘이 튀지 않고 미끄러집니다. 남은 것은 **프레임이 성겨서 끊겨 보이는 것**입니다.

---

## 1. 보고된 증상

> 툭 튀지 않고 미끄러지긴 하는데 애니메이션 프레임이 낮아서 끊기듯이 보이는 게 아쉽습니다. 맥 수준으로 부드러웠으면 좋겠습니다.

---

## 2. 원인 후보

코드에 두 곳이 있습니다. **어느 쪽이 지배적인지는 아직 모릅니다.** 3-1로 확정한 뒤에 고치십시오.

### 2.1 `SetTimer`의 최소 간격

`dock.cpp:66`입니다.

```cpp
constexpr UINT kAnimTimerMs = 16;
...
SetTimer(hwnd_, kAnimTimerId, kAnimTimerMs, nullptr);
```

USER 타이머는 시스템 타이머 틱보다 촘촘하게 울릴 수 없습니다. 기본 틱은 약 **15.6ms**입니다. 16ms를 요청하면 한 틱 안에 들어가지 못해 **다음 틱까지 밀려 약 31ms**가 될 수 있습니다. 그러면 초당 32프레임입니다.

여기에 `WM_TIMER`는 메시지 큐에서 **우선순위가 가장 낮습니다.** 마우스를 움직이는 동안에는 `WM_MOUSEMOVE`가 계속 들어오므로 더 밀릴 수 있습니다.

### 2.2 `dt`를 재는 시계가 거칠다

`Dock::TickDragAnim`(dock.cpp:2709)입니다.

```cpp
const ULONGLONG now = GetTickCount64();
double dt = last_anim_tick_ == 0 ? 16.0 : static_cast<double>(now - last_anim_tick_);
```

`GetTickCount64()`의 해상도도 약 **15.6ms**입니다. **재려는 대상과 자의 눈금이 같은 크기입니다.** 두 프레임이 같은 틱에 들어가면 `dt`가 0이 되어 `dt < 1.0` 분기에서 1.0으로 잘리고, 그 프레임은 거의 움직이지 않습니다. 다음 프레임은 31이 되어 크게 움직입니다.

즉 **프레임 간격이 일정해도 움직임의 크기가 들쭉날쭉해집니다.** 실제 프레임 수보다 더 끊겨 보이는 원인이 될 수 있습니다.

바로 아래에서 `QueryPerformanceCounter`를 쓰고 있으므로(dock.cpp:2739) 정밀한 시계는 이미 있습니다.

### 2.3 프레임 수가 절대적으로 적다

감쇠 계수는 `1 - 0.8^(dt/16)`이고, 90%를 덮는 데 약 165ms가 걸립니다. 32fps라면 그동안 **약 5프레임**입니다. 사람 눈에 다섯 단계는 명확히 계단으로 보입니다.

---

## 3. 수정 지시

### 3-1. 실제 프레임 간격을 재십시오 (필수, 선행)

**고치기 전에 지금 몇 fps인지 확정하십시오.**

`TickDragAnim`에서 `QueryPerformanceCounter`로 이전 프레임과의 간격을 재고, 드래그가 끝날 때 한 줄로 남기십시오.

```cpp
Log(L"perf", L"drag anim frames=%u interval avg=%.1f min=%.1f max=%.1f fps=%.1f render avg=%.1fms", ...);
```

- `GetTickCount64`가 아니라 **`QueryPerformanceCounter`로** 재야 합니다. 2.2의 이유입니다.
- 화면 주사율도 함께 보고하십시오. 60Hz 모니터에서는 60fps가 천장입니다. `GetDeviceCaps(hdc, VREFRESH)` 또는 `DwmGetCompositionTimingInfo`를 쓰십시오.

**이 값을 보고한 뒤에 3-2로 넘어가십시오.** 실측 fps가 목표 판단의 기준입니다.

### 3-2. `dt`를 정밀한 시계로 재십시오 (필수)

`TickDragAnim`의 `GetTickCount64()`를 `QueryPerformanceCounter`로 바꾸십시오.

- `last_anim_tick_`의 자료형도 함께 바꾸십시오.
- `dt < 1.0 → 1.0` 하한을 없애거나 훨씬 작은 값으로 낮추십시오. 정밀한 시계에서는 짧은 간격이 정상입니다.
- `dt > 100.0` 상한은 남기십시오. 창이 멈췄다 돌아올 때 한 번에 튀는 것을 막습니다.

이것만으로도 움직임의 고르기가 개선될 수 있습니다.

### 3-3. 타이머 간격을 실제로 줄이십시오 (필수)

3-1에서 잰 간격이 30ms 근처라면 2.1이 원인입니다. 다음 순서로 시도하고, **각 단계마다 3-1의 측정을 다시 하십시오.**

**1단계: 타이머 해상도를 올린다**

- 드래그 애니메이션이 도는 동안에만 `timeBeginPeriod(1)`을 걸고, 끝나면 `timeEndPeriod(1)`로 되돌리십시오.
- `winmm.lib`를 링크해야 합니다. `bamti.vcxproj`와 CMake **양쪽에** 추가하십시오. 한쪽만 고치면 다른 빌드가 깨집니다.
- `kAnimTimerMs`를 **8**로 낮추십시오.

**해제를 절대 빠뜨리지 마십시오.**

- `timeBeginPeriod`를 건 상태로 두면 시스템 전체의 전력 소비가 늘어납니다.
- RAII 또는 명확한 짝으로 관리하고, `StopDragAnimTimer`의 **모든 경로**에서 풀리게 하십시오. `TickDragAnim` 첫머리의 조기 반환 경로도 포함됩니다.
- 건 횟수와 푼 횟수를 세어 로그로 남기십시오. 드래그를 열 번 한 뒤 두 값이 같아야 합니다.

  ```cpp
  Log(L"perf", L"timer period begin=%u end=%u", ...);
  ```

**2단계: 그래도 부족하면 메시지 루프에서 깨웁니다**

1단계 뒤에도 간격이 16ms를 크게 넘으면, `WM_TIMER` 자체가 밀리는 것입니다. 그때만 다음을 검토하십시오.

- 고해상도 대기 타이머(`CreateWaitableTimerExW`에 `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION`)를 만들고, `host.cpp`의 메시지 루프에서 `MsgWaitForMultipleObjectsEx`로 함께 기다립니다.
- **이것은 메시지 루프를 건드리는 변경입니다.** 지난 멈춤이 전부 이 근처에서 났으므로, 1단계로 충분하면 하지 마십시오.
- 하게 된다면 별도 커밋으로 분리하고, 6장의 안정성 검증을 전부 다시 수행하십시오.

### 3-4. `WM_TIMER`가 밀려도 프레임을 잃지 않게 하십시오 (필수)

마우스를 빠르게 움직이면 `WM_MOUSEMOVE`가 몰려 `WM_TIMER`가 밀립니다. 정확히 드래그 중에 일어나는 상황입니다.

`Dock::HandleMessage` 진입부에 이미 같은 문제를 푸는 장치가 있습니다(`last_popup_tick_`, dock.cpp:1112 부근). **같은 방식**으로 애니메이션도 처리하십시오.

```cpp
// 어떤 메시지를 처리하든, 애니메이션이 돌고 있고 마지막 프레임에서 충분히 지났으면 한 프레임 진행한다.
```

- 간격 판정은 3-2의 정밀한 시계로 하십시오.
- **새로운 장치를 설계하지 마십시오.** 팝업 쪽에서 검증된 패턴을 그대로 따르십시오.
- 이렇게 하면 메시지가 몰릴수록 오히려 프레임이 더 자주 나옵니다.

### 3-5. 그래도 계단으로 보이면 지속 시간을 늘리십시오 (선택)

3-2부터 3-4까지 마친 뒤에도 부족하면, 그때만 손대십시오.

- 감쇠 계수를 조정해 90% 도달 시간을 **약 200ms**로 늘리십시오. 프레임 수가 늘어 계단이 완만해집니다.
- 지금보다 느려 보이면 안 됩니다. **250ms를 넘기지 마십시오.**
- 계수를 바꿀 때 `dt`에 따라 속도가 달라지지 않는 지금의 형태(`1 - a^(dt/기준)`)를 유지하십시오.

---

## 4. 하지 말아야 할 것

- 3-1의 측정 없이 고치지 마십시오. 지금 몇 fps인지 모르는 채로 손대면 무엇이 효과가 있었는지 알 수 없습니다.
- **`timeBeginPeriod`를 풀지 않는 경로를 남기지 마십시오.** 프로그램이 도는 내내 걸려 있으면 안 됩니다.
- `DwmFlush()`나 `WaitForVBlank`으로 UI 스레드를 **막지 마십시오.** 이 프로그램의 UI 스레드가 멈추면 독과 메뉴가 전부 굳습니다.
- 1단계로 충분하면 3-3의 2단계(메시지 루프 변경)를 하지 마십시오.
- 애니메이션을 별도 스레드로 옮기지 마십시오. 창 그리기는 UI 스레드의 일입니다.
- 반투명 고스트를 다시 넣지 마십시오. 사용자가 제거를 요청했습니다.
- 확대(magnification)나 튕김(bounce) 같은 다른 효과를 넣지 마십시오. 이번 요청은 부드러움뿐입니다.
- `d672d59`의 렌더 캐시를 되돌리지 마십시오. 프레임 비용을 4분의 1로 낮춘 수정입니다.
- 지금까지의 안정성 수정과 계측을 되돌리지 마십시오.

---

## 5. 검증

**프레임 간격 (핵심)**
- [ ] 수정 전 `drag anim ... interval avg/min/max fps=` 값을 보고한다.
- [ ] 수정 후 같은 값을 보고한다. **두 값을 나란히 적는다.**
- [ ] 화면 주사율을 보고한다.
- [ ] 수정 후 평균 간격이 주사율 주기에 가깝다. 60Hz라면 16.7ms 근처다.
- [ ] 마우스를 빠르게 흔들면서 끌어도 간격의 최댓값이 크게 튀지 않는다.

**타이머 해상도**
- [ ] 드래그를 **열 번** 한 뒤 `timer period begin=` 과 `end=` 가 같다.
- [ ] 드래그가 끝난 뒤 5초간 CPU가 유휴 수준이다.
- [ ] 프로그램을 유휴로 60초 두었을 때 누적 CPU가 1초 미만이다. 타이머 해상도가 남아 있으면 여기서 드러난다.

**움직임**
- [ ] 아이콘이 계단이 아니라 연속으로 미끄러진다.
- [ ] 지금보다 느려 보이지 않는다.
- [ ] 다섯 칸을 옮기는 동안 CPU가 0.3 코어 미만이다.

**회귀**
- [ ] 드롭한 순서가 저장되고 재시작 후에도 유지된다.
- [ ] `order after-drop`과 `order after-rebuild`가 같다.
- [ ] 드래그를 취소하면 원래 자리로 미끄러져 돌아온다.
- [ ] 클릭으로 앱이 활성화된다.
- [ ] 호버 강조가 동작하고, 드래그 중에는 그려지지 않는다.
- [ ] 우클릭 메뉴와 옵션 서브메뉴가 열리고 닫힌다.
- [ ] `[perf] msg flood` 줄이 남지 않는다.
- [ ] `watchdog.log`에 `alive stage=idle ping=ok`만 쌓인다.
- [ ] Release 빌드가 `/W4` 경고 없이 통과한다. CMake 빌드도 통과한다.

---

## 6. 커밋

셋으로 나누십시오. 3-3의 2단계까지 갔다면 넷입니다.

```
fix: 애니메이션 경과 시간을 정밀한 시계로 잰다
```
3-1과 3-2입니다. 본문에 수정 전 `interval` 측정값을 적으십시오.

```
perf: 드래그 애니메이션의 프레임 간격을 줄인다
```
3-3의 1단계와 3-4입니다. 본문에 수정 전후의 `interval`과 `fps`, 그리고 `timer period` 짝이 맞는지를 적으십시오.

```
tune: 밀어내는 움직임의 지속 시간을 조정한다
```
3-5입니다. 하지 않았다면 이 커밋은 없습니다.
