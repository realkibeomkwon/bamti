# 작업 지시서: 토글이 한 번의 클릭에 두 번 실행되는 결함

토글을 누르면 잠깐 뒤집혔다가 원래대로 돌아오는 결함의 원인을 확정했습니다. **`PopupSurface`가 한 번의 클릭에 `StickyInvoke`를 두 번 호출합니다.**

---

## 1. 확정 근거

주고받은 모든 줄을 기록하는 진단 클라이언트를 붙여 놓고 토글을 한 번 눌렀습니다. bamti가 보낸 이벤트입니다.

```
02:11:01.160 RECV {"op":"event","event":"toggle","row_id":"sw","on":false}
02:11:01.161 RECV {"op":"event","event":"toggle","row_id":"sw","on":true}
```

**클릭 한 번에 토글 이벤트가 두 번 나갔고, 값이 서로 반대입니다.** 첫 번째가 켜짐에서 꺼짐으로 뒤집고, 두 번째가 다시 켜짐으로 되돌립니다. 그래서 사용자에게는 눌렀다가 저절로 돌아오는 것으로 보입니다.

같은 시각의 `bamti.log`입니다.

```
02:11:00.992 [popup] msg=lbuttondown pt=377,95 inside=1
02:11:01.117 [popup] msg=lbuttonup   pt=377,95 inside=1
02:11:01.159 [popup] poll sticky row=0
```

`WM_LBUTTONUP`에서 한 번, 42ms 뒤 가드 타이머 폴링에서 또 한 번 실행됐습니다.

## 2. 원인

`popup_surface.cpp`에 `StickyInvoke`를 부르는 자리가 두 곳 있습니다.

**메시지 경로** (`WM_LBUTTONUP` 처리, 755행 부근)

```cpp
const int index = content_->HitTest(pt, Dpi());
if (index >= 0 && content_->StickyRow(index)) {
  content_->StickyInvoke(index);
  InvalidateRect(hwnd_, nullptr, FALSE);
  if (after_tick_ != nullptr) {
    after_tick_(after_tick_ctx_);
  }
  return 0;
}
```

**폴링 경로** (`Tick`, 509행 부근)

```cpp
if (content_->StickyRow(row)) {
  Log(L"popup", L"poll sticky row=%d", row);
  content_->StickyInvoke(row);
  press_inside_ = false;
  InvalidateRect(hwnd_, nullptr, FALSE);
}
```

폴링 경로는 실행한 뒤 `press_inside_`를 내립니다. **메시지 경로는 `press_inside_`도 `mouse_down_`도 내리지 않습니다.** 그래서 마우스를 뗀 뒤 다음 폴링이 돌 때 여전히 눌린 것으로 보고 같은 행을 한 번 더 실행합니다.

토글은 실행할 때마다 값을 뒤집으므로 두 번 실행되면 제자리로 돌아옵니다. 짝수 번 실행되면 아무 일도 없었던 것처럼 보이는 것이 이 결함의 성질입니다.

## 3. 고칠 것

메시지 경로의 sticky 분기에서 눌림 상태를 내리십시오.

```cpp
if (index >= 0 && content_->StickyRow(index)) {
  press_inside_ = false;
  mouse_down_ = false;
  content_->StickyInvoke(index);
  ...
}
```

다만 이것은 증상을 막는 최소 수정입니다. **두 경로가 같은 동작을 중복해서 책임지는 구조 자체가 원인**이므로, 다음 중 하나를 함께 하십시오.

1. sticky 행 실행을 메시지 경로 한 곳으로 모으고, 폴링 경로에서는 실행하지 않습니다. 폴링은 팝업이 살아 있는지 확인하는 안전망이지 입력 처리 수단이 아닙니다.
2. 그것이 어려우면 마지막으로 실행한 `(행 번호, 클릭 식별자)`를 기억해 같은 클릭에 대한 중복 실행을 막습니다.

**1번을 권장합니다.** 폴링이 입력을 처리하면 앞으로 sticky 행이 늘어날 때마다 같은 종류의 중복이 재발합니다. 폴링 경로에서 실행을 없앨 때, 그 경로가 왜 생겼는지(마우스 캡처가 풀린 상황 대비로 보입니다)를 확인하고 그 상황에서도 클릭이 유실되지 않는지 검증하십시오.

## 4. 함께 볼 것

- `button` 행도 같은 두 경로를 지납니다. 버튼은 실행 뒤 팝업이 닫히므로 중복이 드러나지 않았을 뿐, 명령이 두 번 실행될 수 있습니다. **독 컨텍스트 메뉴의 명령도 같은 경로를 씁니다.** 창 닫기 같은 명령이 두 번 실행되면 눈에 띄지 않게 문제가 됩니다. 이 부분도 함께 점검하십시오.
- 클라이언트가 이벤트를 받을 때 중복을 걸러야 한다는 요구를 명세에 넣지 마십시오. **중복을 보내지 않는 것이 bamti의 책임입니다.**

## 5. 명세에 추가할 것

이번 조사에서 나온 별개의 사실입니다. 두 클라이언트가 동시에 연결하면 20회 중 20회 한쪽이 `ERROR_PIPE_BUSY`를 받습니다. 수신 인스턴스가 한 번에 하나뿐이라 생기는 경합입니다.

5단계 트레이 미러와 6단계 사용량 헬퍼가 시작할 때 동시에 붙으므로, `docs/STATUS-PROTOCOL.md`에 **클라이언트는 연결 실패 시 짧은 간격으로 재시도해야 한다**는 의무를 넣으십시오. 예제들은 이미 재시도하고 있어서 드러나지 않았습니다.

## 6. 검증

- [ ] `/W4` 경고 없이 Debug와 Release가 빌드됩니다.
- [ ] 토글을 한 번 누르면 **이벤트가 정확히 한 번** 나갑니다. 진단 클라이언트로 확인하십시오.
- [ ] 누른 상태가 유지됩니다.
- [ ] 다시 누르면 반대로 바뀌고 그 상태도 유지됩니다.
- [ ] 패널을 닫았다 열어도 마지막 상태가 남습니다.
- [ ] 클라이언트를 죽인 뒤 누르면 2초 뒤 되돌아가고 `[status] toggle timeout revert`가 찍힙니다.
- [ ] 패널의 버튼을 누르면 명령이 한 번만 실행됩니다.
- [ ] 독 컨텍스트 메뉴의 명령이 한 번만 실행됩니다.
- [ ] 마우스를 누른 채 팝업 밖으로 끌었다가 놓아도 실행되지 않습니다.

## 7. 커밋

| 순서 | 커밋 메시지 |
|---|---|
| 1 | `fix: 팝업의 고정 행이 한 번의 클릭에 두 번 실행되지 않게 한다` |
| 2 | `docs: 동시 연결 시 재시도 의무를 명세에 적는다` |
