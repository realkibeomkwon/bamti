# 작업 지시서 9: 계측되지 않는 메시지 폭주와 핀 매칭 실패

멈춤은 해결되었습니다. 그러나 **폭주 자체는 남아 있고**, 계측이 닿지 않는 경로에서 일어나고 있습니다. 여기에 핀 매칭 실패가 별건으로 확인되었습니다.

이 문서는 **원인을 지목하지 않습니다.** 원인을 스스로 드러내게 만드는 계측을 지정합니다.

---

## 1. 측정된 사실

### 1.1 멈춤은 해결되었다

```
watchdog.log:  alive stage=idle ping=ok   (5초 간격으로 계속)
[popup] alive tick=1..5 src=timer         (메뉴를 열 때마다 정상)
[popup] dismiss reason=invoke index=2     (항목 실행됨)
[dock]  menu cmd=1 pin name=explorer.exe  (명령 수행됨)
```

메뉴가 열리고 닫히고 명령이 실행됩니다. 트레이 되먹임 수정과 굶주림 방지가 효과를 냈습니다. **되돌리지 마십시오.**

### 1.2 그러나 코어 하나를 계속 태우고 있다

멈추지 않은 상태에서 측정한 값입니다.

```
Responding = True
3초 동안 CPU 2.70초  →  코어 0.90개를 계속 점유
```

이전 멈춤 때와 **같은 수치**입니다. 증상만 가려졌을 뿐 폭주는 그대로입니다. 사용자가 보고한 "간헐적으로 메뉴가 느리게 뜬다"가 이것 때문일 가능성이 높습니다.

### 1.3 폭주가 어떤 경로인지 알 수 없다

`bamti.log`의 마지막 줄은 **11:17:50.879**입니다. 그런데 `watchdog.log`는 **11:19:57**까지 정상으로 쌓였고 그동안 CPU를 계속 태웠습니다.

즉 **2분 넘게 아무 로그도 남기지 않으면서 코어 하나를 소모했습니다.**

`3-4`에서 넣은 폭주 감지가 걸리지 않았습니다.

```
11:12:20.306 [dock] task storm 11 msgs in 1s     ← 이전에는 걸렸음
(그 뒤로 storm 줄 없음)
```

`kTasksChangedMsg`와 `kTrayChangedMsg`에만 계수기가 있고, **다른 게시 메시지에는 없습니다.** 지금 폭주하는 것은 계수기가 없는 경로입니다.

`watchdog`의 `stage`가 계속 `idle`인 것은, 메시지가 빠르게 처리되고 곧바로 `GetMessage`로 돌아가기 때문으로 보입니다. 어떤 메시지인지는 지금 계측으로 알 수 없습니다.

### 1.4 핀이 매칭되지 않는다 (별건)

```
11:17:43.079 [perf] rebuild items=14 pins=11 shown=1 47ms
11:17:48.794 [dock] menu open index=13 name=explorer.exe running=1 windows=1 pinned=0
```

`explorer.exe`가 `pinned=0`이고 인덱스 13, 즉 **맨 오른쪽**입니다. 고정된 항목이 아니라 실행 중인 항목으로 분류된 것입니다.

그런데 `~/.bamti/dock-pins.txt`에는 `C:\Windows\explorer.exe`가 첫 줄에 있습니다.

**핀 목록에는 있는데 실행 중인 창과 이어지지 않습니다.** 사용자가 본 "탐색기 아이콘이 맨 왼쪽에서 맨 오른쪽으로 이동" 현상이 이것입니다.

`pins=12`에서 `pins=11`로 줄어든 것은 사용자가 11:13:24에 Grok을 고정 해제한 결과이며 정상입니다. 이것과 혼동하지 마십시오.

---

## 2. 수정 지시

### 2-1. 어떤 메시지가 몰리는지 이름을 밝힌다 (필수, 최우선)

`src/host.cpp`의 메시지 루프에 처리량 계측을 넣으십시오.

- 1초 창으로 처리한 메시지 수를 셉니다.
- 초당 **500건을 넘으면** 그 1초 동안 가장 많았던 메시지 상위 3개를 한 줄로 남깁니다.
- 메시지는 `(hwnd의 클래스 이름, message 값)` 쌍으로 구분합니다. 클래스 이름은 `GetClassNameW`로 얻되, **집계 시점이 아니라 로그를 남길 때 한 번만** 조회하십시오. 매 메시지마다 부르면 그 자체가 부하가 됩니다.
- `WM_APP` 이상의 사용자 정의 메시지는 값을 그대로 적으십시오. 어느 상수인지는 사람이 대조합니다.

```cpp
Log(L"perf", L"msg flood %u/s top=[%s:0x%04X x%u] [%s:0x%04X x%u] [%s:0x%04X x%u]", ...);
```

- 로그는 **1초에 한 줄**을 넘지 않게 하십시오.
- 집계는 고정 크기 배열이나 작은 해시로 하고, 매 메시지마다 할당하지 마십시오. `std::map`을 쓰면 그 비용이 측정을 왜곡합니다.

이 한 줄이 폭주의 정체를 말해 줍니다.

### 2-2. 모든 게시 메시지에 계수기를 단다 (필수)

`3-4`에서 `kTasksChangedMsg`와 `kTrayChangedMsg`에만 넣었습니다. 나머지에도 같은 계수기를 다십시오.

- `kFullscreenMsg`
- `kPopupClosedMsg`
- `kMenuCommandMsg`
- `kStatusChangedMsg`
- `kToggleStartMsg`, `kToggleSpotlightMsg`
- `kIconReadyMsg`, `kFileSearchDoneMsg`

기존 `NotePostedStorm`을 그대로 재사용하십시오. 임계값은 같게 두고 태그만 다르게 합니다.

계수기를 다는 것이 목적이지 동작을 막는 것이 아닙니다. **어떤 경우에도 메시지를 버리지 마십시오.**

### 2-3. 핀 매칭 실패를 계측한다 (필수)

`CollectDockApps()`에서 실행 중인 앱이 어떤 핀과도 매칭되지 않았을 때, 판정에 쓴 값을 남기십시오.

- 매칭에 사용한 키(`DockPinId` 결과 또는 그에 해당하는 값)
- 창에서 얻은 실행 파일 경로
- `CanonicalPath()` 적용 후의 값
- 그 경로가 캐시에서 나온 것인지 새로 조회한 것인지

```cpp
Log(L"dock", L"pin miss key=%s path=%s canon=%s cached=%d", ...);
```

로그가 넘치지 않도록, **한 번의 `Rebuild`당 최대 3줄**로 제한하십시오.

핀 목록 쪽도 한 번 남기십시오. `Rebuild`에서 `pins_`의 각 항목을 `SameDockPin`이 비교하는 형태로 정규화해 한 줄에 이어 붙여 기록합니다. 이것도 **`pin miss`가 발생했을 때만** 남기십시오.

### 2-4. HWND 캐시가 빈 값을 고착시키지 않게 한다 (필수)

`task_list.cpp`의 `g_window_cache`는 창별 경로와 프로퍼티를 캐시합니다. 조회가 실패해서 **빈 문자열이 캐시되면 그 창은 영원히 빈 값**을 갖게 됩니다.

- 경로가 비어 있으면 **캐시에 넣지 마십시오.** 다음 기회에 다시 조회하게 둡니다.
- `IsTaskWindow()` 판정 결과는 창의 상태에 따라 바뀌므로 **캐시하지 마십시오.** 매번 계산하십시오. 이 함수가 비싸서 캐시했다면, 어느 부분이 비싼지 측정해 보고하십시오.

이것이 1.4의 원인이라고 단정하지 마십시오. 2-3의 로그로 확인한 뒤 판단합니다. 다만 빈 값을 고착시키는 구조는 그 자체로 결함이므로 고칩니다.

---

## 3. 하지 말아야 할 것

- **폭주의 원인을 추측해서 고치지 마십시오.** 이번 작업은 계측입니다. `msg flood` 줄과 `pin miss` 줄을 얻는 것이 성과물입니다.
- 지금까지의 수정을 하나도 되돌리지 마십시오. 특히 트레이 억제, 굶주림 방지, `armed_`, 감시 스레드를 유지하십시오.
- 메시지를 버리거나 게시를 막지 마십시오. 계수만 합니다.
- 2-1의 집계에서 메시지마다 `GetClassNameW`를 부르거나 메모리를 할당하지 마십시오. 측정이 대상을 바꿉니다.
- 2-3과 2-4를 근거로 핀 로직을 새로 설계하지 마십시오. 원인이 확인되면 그때 별도로 다룹니다.
- `WH_MOUSE_LL`, `WH_KEYBOARD_LL`을 도입하지 마십시오.

---

## 4. 검증

**폭주 (핵심)**
- [ ] 프로그램을 띄우고 1분간 방치한 뒤 CPU를 잰다. 코어 점유가 0.1 미만이면 폭주가 없는 것이고, 그래도 높으면 `[perf] msg flood` 줄이 남아 있어야 한다.

  ```powershell
  $p = Get-Process bamti; $c=$p.TotalProcessorTime.TotalSeconds; Start-Sleep 10; $p.Refresh()
  "{0:N2} cores" -f (($p.TotalProcessorTime.TotalSeconds-$c)/10)
  ```
- [ ] **`msg flood` 줄 전체를 보고한다. 이것이 이번 작업의 성과물이다.**
- [ ] `storm` 줄이 남으면 어느 태그인지 보고한다.

**핀**
- [ ] 독을 띄우고 1분 방치한 뒤 다시 띄운다. 탐색기 아이콘이 맨 왼쪽에 남아 있는지 확인한다.
- [ ] 자리가 바뀌면 `[dock] pin miss ...` 줄 전체를 보고한다.

**회귀**
- [ ] 메뉴가 열리고 닫히고 항목 실행이 된다.
- [ ] `watchdog.log`에 `alive stage=idle ping=ok`만 쌓인다.
- [ ] Release 빌드가 `/W4` 경고 없이 통과한다.

---

## 5. 커밋

두 개로 나누십시오.

```
chore: 메시지 처리량과 게시 메시지 계수기를 추가한다
```
2-1과 2-2입니다.

```
fix: 창 정보 캐시가 빈 값을 고착시키지 않게 한다
```
2-3과 2-4입니다. 본문에 `IsTaskWindow` 캐시를 뺀 뒤의 `Rebuild` 소요 시간을 적으십시오.
